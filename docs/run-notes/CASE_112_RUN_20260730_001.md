# CASE_112 — RUN_20260730_001 — Execution Log

Append-only. Do not edit or delete prior entries; corrections are added as new entries referencing the original timestamp. No PII, no raw quotes, no ground-truth content. All facts below come from DAO command output and agent summaries only — `outputs/` and `data/` were never read directly.

## Run metadata

- run_id: `RUN_20260730_001`
- case_id: `CASE_112`
- Orchestration mode: sub-agent pipeline per `loss-adjustment-pipeline` SKILL.md (no standalone `run_pipeline.py`)
- Branch: `beta/pipeline-demo-2026-07-31`
- Explicit user authorization on record for this run: pre-redaction page images may be sent to the configured claude-cli vision provider for P8 cross-validation. `cross_validation_mode` will be recorded honestly as `single_technology_weak_p8_poc` (or whatever the actual mode is) — not represented as resolved dual-technology validation. External-provider PII exposure risk applies and is not mitigated by this run.

## Timeline (append-only)

| timestamp | stage | event | result | implication |
|---|---|---|---|---|
| 2026-07-30T (session start) | phase0 | Read AGENTS.md, harness-guardrails, harness-guardrails-dev, loss-adjustment-pipeline SKILL.md, pipeline.md | done | Guardrails loaded before any tool use |
| 2026-07-30T | phase0 | `git status` / branch check | clean, branch `beta/pipeline-demo-2026-07-31`, 80 commits ahead of shared/main | Safe to proceed, no uncommitted work at risk |
| 2026-07-30T | phase0 | `pytest tests/test_segment_case.py tests/test_llm_providers.py` | 172 passed | Stage 1 implementation verified before use |
| 2026-07-30T | phase0 | `dao.py get-last-passed-stage CASE_112` | NONE | Fresh run, no prior passed stage to resume from |
| 2026-07-30T | phase0 | `dao.py check-conflicts-clear CASE_112` | clear: true, 0 pending | No conflict-ledger blockers |
| 2026-07-30T | phase0 | `dao.py check-source-ledger-clear CASE_112` | clear: true, 0 pending, 0 rejected | Intake/D2 ledger already fully reviewed (approved) for all 6 source files prior to this session |
| 2026-07-30T | phase0 | `dao.py check-lock` on document_manifest.json and _run_state.json | both unlocked | No abnormal-termination lock to report |
| 2026-07-30T | phase0 | `dao.py check-segmentation-ready CASE_112` | clear: false — DOC_001..DOC_005 all `pending_review` | Stage 1 human bundle-decision gate not yet exercised for this case; halting here per P7/D2 |
| 2026-07-30T | phase0 | Reviewed `_source_ledger.json` via `dao.py read-ledger` | 6 files: 4 raw (approved, 3 with D2 content_warning reviewed and cleared by human `pyun`), 1 ground_truth (approved), 1 raw (approved) | One of two same-titled "손해사정서" files (p001-012) is classified `ground_truth` and correctly did NOT enter `data/raw`/the manifest; the other (p013-031) is classified `raw` and is DOC_005. Manifest confirms only 5 documents (DOC_001-005) exist under `data/raw/CASE_112`, matching the 5 ledger entries classified `raw`. D1 boundary appears correctly enforced at intake — noted here for the record, not re-litigated |

## Stage 1 — Case Intake & Document Segmentation

### Intake status
- `_source_ledger.json`: all 6 entries `approved`, 0 `pending`, 0 `rejected` (confirmed via `check-source-ledger-clear`). Intake already complete prior to this session; not re-run.
- `document_manifest.json`: 5 documents (DOC_001–DOC_005), all `segmentation_status: pending_review`, `ocr_status: pending`. No prior segmentation or OCR work exists for this case.

### Document inventory (structural metadata only — page counts read from PDF headers, not content)

| document_id | file_size_bytes | page_count | segmentation_status |
|---|---|---|---|
| DOC_001 | 140,277 | 10 | pending_review |
| DOC_002 | 611,442 | 15 | pending_review |
| DOC_003 | 4,900,811 | 145 | pending_review |
| DOC_004 | 4,887,155 | 178 | pending_review |
| DOC_005 | 1,409,526 | 19 | pending_review |

### Human bundle-decision gate — resolved

The human reviewer requested a DOC_ID↔original-filename mapping. File size alone could not disambiguate DOC_003 (145p, 4.90MB) vs DOC_004 (178p, 4.89MB) — sizes are too close to assign with confidence without opening `_intake_record.json` (a human-readable audit crosswalk, not read by any agent in normal pipeline operation). The user reviewed this and decided to proceed on the size-based estimate for DOC_001/DOC_002, accepting the risk, with the explicit instruction that if Stage 1's actual page content later contradicts the estimate, re-segmentation is required.

Human decision recorded 2026-07-30 (reviewer: pyun, via `dao.py set-segmentation-status`, `--run-id RUN_20260730_001`):

| document_id | decision | note |
|---|---|---|
| DOC_001 | not_required | single logical document (est. 법률질의회신서 환자측 반박자료), 10p |
| DOC_002 | not_required | single logical document (est. 보험사측 법률자문 결과), 15p |
| DOC_003 | required | multi-document bundle candidate, 145p |
| DOC_004 | required | multi-document bundle candidate, 178p |
| DOC_005 | required | multi-document bundle candidate, 19p |

Post-decision `check-segmentation-ready CASE_112`: `clear: false`, blockers now only DOC_003/DOC_004/DOC_005 ("marked as a bundle and has not been split") — DOC_001/DOC_002 no longer block. Proceeding to `segment_case.py sheets`/`propose` for the 3 required bundles.

**Caveat carried forward**: DOC_001/DOC_002↔filename identity is an estimate, not a confirmed mapping. If Stage 1 content review contradicts it, this run re-segments per the user's explicit instruction rather than proceeding on a wrong assumption.

### Contact sheets rendered (`segment_case.py sheets`, no model call)

Geometry (production default, all 3 documents): grid 4x4 (16 pages/sheet), crop_ratio 0.33, 3 orientation variants (as_scanned/cw/ccw) per sheet.

| document_id | page_count | sheet_count |
|---|---|---|
| DOC_003 | 145 | 10 |
| DOC_004 | 178 | 12 |
| DOC_005 | 19 | 2 |

Next: `propose` (real vision-model calls via claude-cli, provider default `claude-cli`, binary overridden to the Windows npm-installed `claude.exe` executable per user instruction via `HARNESS_CLAUDE_COMMAND`). User has explicitly authorized sending pre-redaction page images from CASE_112 to the configured claude-cli vision provider for this run.

### `propose` — DOC_003 (real claude-cli vision calls, production defaults: no `--refine`, no `--fallback-cap-ratio` override)

| timestamp | stage | event | result | implication |
|---|---|---|---|---|
| 2026-07-30T~15:06-15:07 KST | document_segmentation | `segment_case.py propose CASE_112 DOC_003 --held-by pyun --run-id RUN_20260730_001` | status: proposed | 135 segments proposed from 145 pages; see detail below |

- 10 real vision calls, one per contact sheet, all `[ok]`, 0 failed sheets (structured-output contract held for all 10)
- Automatic full-page fallback triggered for 9 crop-ambiguous pages (fallback_triggered: true, fallback_saturated: false — within the default 25% cap): p1, p8, p55, p61, p102, p115, p116, p142, p145. All 9 resolved (0 unresolved). Confidences: p1 NEW 0.97, p8 cont 0.75, p55 cont 0.9, p61 cont 0.85, p102 NEW 0.5 (low), p115 cont 0.85, p116 cont 0.8, p142 cont 0.85, p145 cont 0.85
- `--refine` not used (not yet judged necessary per user instruction — will reassess after `show`)
- segment_count: 135 (from 145 pages — a high boundary density, consistent with this being a real multi-document bundle)
- 8 warnings: pages flagged as both boundary-and-continuation within a sheet were force-resolved toward boundary (sheets 0,1,2,3,5,6,7,8) — conservative default behavior, **not yet human-reviewed**. Full list of affected pages carried into the human review step, not resolved by this agent.
- unassigned_pages: [] — no page left without a segment assignment
- Output written to `outputs/CASE_112/segmentation_proposal_DOC_003.json` via the DAO-backed tool

**Flag for human review**: p102's full-page fallback NEW-boundary call has confidence 0.5 (the lowest of the run) — worth extra scrutiny at `show`/approval time.

### `propose` — DOC_004 (real claude-cli vision calls, production defaults: no `--refine`, no `--fallback-cap-ratio` override)

| timestamp | stage | event | result | implication |
|---|---|---|---|---|
| 2026-07-30T~15:14-15:28 KST | document_segmentation | `segment_case.py propose CASE_112 DOC_004 --held-by pyun --run-id RUN_20260730_001` | status: proposed | 141 segments proposed from 178 pages; see detail below |

- 12 real vision calls, one per contact sheet, all `[ok]`, 0 failed sheets
- Automatic full-page fallback triggered for 10 crop-ambiguous pages (within default 25% cap, not saturated): p7, p30, p77, p80, p102, p118, p123, p135, p171, p172. All 10 resolved, 0 unresolved. Confidences: p7 NEW 0.95, p30 NEW 0.95, p77 cont 0.85, p80 cont 0.9, p102 cont 0.85, p118 cont 0.7, p123 cont 0.6 (lowest), p135 NEW 0.75, p171 NEW 0.9, p172 NEW 0.85
- `--refine` not used
- segment_count: 141 (from 178 pages)
- 8 warnings: same both-boundary-and-continuation ambiguity pattern as DOC_003 (sheets 2,3,4,5,6,8,9,10), force-resolved toward boundary — carried to human review, not resolved here
- unassigned_pages: []
- Output written to `outputs/CASE_112/segmentation_proposal_DOC_004.json`

**Flag for human review**: p123's full-page fallback continuation call has confidence 0.6 (lowest of this document's run).

### `propose` — DOC_005 (real claude-cli vision calls, production defaults) — FALLBACK SATURATED, proposal not reliable as-is

| timestamp | stage | event | result | implication |
|---|---|---|---|---|
| 2026-07-30T~15:29 KST | document_segmentation | `segment_case.py propose CASE_112 DOC_005 --held-by pyun --run-id RUN_20260730_001` | status: proposed, but `fallback_saturated: true` | 9 of 19 pages need full-page fallback but the default 25% cap (4 pages for a 19-page doc) blocked all 9 — none were resolved |

- 2 real vision calls (2 sheets), both `[ok]`, 0 failed sheets
- `needs_full_page` / `fallback_unresolved_pages`: [5, 9, 10, 11, 12, 13, 14, 15, 16] — 9 of 19 pages, i.e. nearly half the document, left unresolved
- Tool's own warning: "full-page fallback saturated: 9 page(s) needed a full-page look but the cap is 4; the crop ratio or grid is likely wrong for this bundle — retune rather than spend the extra calls. Fallback skipped; these pages stay flagged."
- segment_count: 19 (== page_count — nearly every page proposed as its own boundary, an unusually high split rate)
- 2 warnings: same both-boundary-and-continuation pattern (sheet 0 pages [5,10,11,12,13,14,15], sheet 1 page [19]) — notably, these are almost the exact same pages left unresolved by fallback saturation, suggesting the crop-ratio/grid combination is genuinely a poor fit for this document's layout, not incidental noise
- Output written to `outputs/CASE_112/segmentation_proposal_DOC_005.json` (exists but not usable for `approve`/`split` as-is — 9 unassigned-confidence pages)

**Not proceeding to `show`/human-approval for DOC_005 on this proposal.** Per the user's explicit instruction not to default to benchmark settings (`--fallback-cap-ratio=1.0`) and to use current production defaults, and to reach for `--refine`/cap overrides only when the document and current operating standards actually call for it — a majority-unresolved proposal is exactly that case, not a cosmetic tuning choice. Flagging to the human rather than unilaterally re-running with a raised cap.

### DOC_005 re-attempt with `--refine` (human-directed) — did not resolve the saturation

| timestamp | stage | event | result | implication |
|---|---|---|---|---|
| 2026-07-30T~15:32 KST | document_segmentation | `segment_case.py propose CASE_112 DOC_005 --held-by pyun --run-id RUN_20260730_001 --refine` (human decision, not agent-initiated) | status: proposed, fallback_saturated still true | `--refine` is a no-op here: `refinement.long_segments_refined: 0`, `refinement.vision_calls: 0` |

- Sheets served from cache (2 sheets, both `(cached)` — geometry/prompt/schema/provider/model unchanged from the prior run, correctly reused per the 2026-07-30 cache-fingerprint fix)
- `--refine` re-examines long *merged* segments (length ≥ 4) to recover over-merged boundaries — it does not touch fallback-saturated pages. Since DOC_005's current proposal already has 19 segments across 19 pages (no long merged runs exist), refine had nothing to act on
- `needs_full_page` / `fallback_unresolved_pages` unchanged: [5, 9, 10, 11, 12, 13, 14, 15, 16] — still 9/19 pages unresolved
- **Root cause unchanged**: this is a fallback-cap saturation problem (too many crop-ambiguous pages for the default 25% spend cap on a 19-page document), not an over-merge problem — `--refine` targets a different failure mode. Reporting back to the user rather than trying further options unprompted.

### Human review finding — DOC_003/DOC_004 self-contradicting boundary verdicts (systematic, not incidental)

User inspection of the `show` output for DOC_003/DOC_004 surfaced a real, systematic segmentation-tool finding, not a one-off: every segment with `provisional_type_label: null` had `boundary_evidence` text explicitly describing a continuation ("no title", "continues from p[N-1]") while still being classified as a `boundaries` entry rather than `continuations` by the underlying model call. Verified by full enumeration, not sampling: **29/29 (100%) of DOC_003's null-label segments and 39/39 (100%) of DOC_004's** show this exact pattern — zero counterexamples. Recorded as `known-gaps.md` item 34 (new). Root cause: `merge_sheet_proposals()` builds segments strictly from the `boundaries` array and never checks a boundary claim's own evidence text against its own verdict; `--refine` cannot catch this either since it only re-examines segments *above* a length threshold to fix over-merging, the opposite failure shape from this bug's all-1-page-segments symptom.

**Human decision (2026-07-30, not agent-initiated)**: record the bug, do not fix `segment_case.py` in this run; the agent merges the 68 affected segments (29 + 39) into their preceding titled segment at the approve step for CASE_112 only.

**Merge applied** via a one-off script calling `segment_case.py`'s own DAO-governed `_read_proposal`/`_write_proposal` (same lock+schema-validate+atomic-write contract as `approve --edit`; the CLI itself has no segment-removal flag, only single-segment range edits, so a direct script against the governed read/write functions was necessary — not a hand-edit of the JSON file):

| document_id | before | after | absorbed | marked `edited` |
|---|---|---|---|---|
| DOC_003 | 135 segments | 106 segments | 29 | 19 (segments whose range actually changed) |
| DOC_004 | 141 segments | 102 segments | 39 | 25 |

Post-merge verification: `validate_segments()` 0 errors both documents, full page coverage (145/145, 178/178), 0 remaining null-label segments. Untouched segments retain `review_status: pending` for normal human review; only ranges that actually absorbed a page were marked `edited` per the schema's own definition ("a human changed the proposed range and approves the changed one").

**Still pending human review**: the 87 (DOC_003) / 77 (DOC_004) untouched segments and the 19+25 edited-range segments all still need case-level or per-segment approval before `split` can run — this merge corrected structure, it did not grant approval.

### Human review finding — DOC_005 label + boundary correction (applied)

User visually confirmed DOC_005 pages 9-15 (the model's proposal idx 8-14, previously split into 7 separate 1-page segments due to the same fallback-saturation gap from the earlier `propose` run) are actually **one single discharge (퇴원)-related document**, not "진료비 세부산정내역(입원)" (inpatient itemized fee statement) as the model proposed.

Applied via the same DAO-governed `_read_proposal`/`_write_proposal` path used for DOC_003/004:

| before | after |
|---|---|
| 19 segments (idx 8-14 = 7 separate p9,p10,...,p15 segments, type "진료비 세부산정내역(입원)", confidences 0.75/0.5×6) | 13 segments (idx 8 = single p9-15 segment, type corrected to "퇴원 관련 서류", `review_status: edited`) |

`provisional_type_label`/`boundary_evidence` on the merged segment explicitly record the correction as human-sourced (reviewer pyun, 2026-07-30), not model output, so the record does not misattribute the corrected label to the vision provider. Post-edit `validate_segments()`: 0 errors, full 19/19 page coverage.

**Resolved 2026-07-30 (human-directed)**: user instructed extending the same known-gaps-item-34 merge to DOC_005's remaining 2 null-label segments. Applied via the same DAO-governed path: idx4 (p5) absorbed into idx3 (p4, operation record) → new range p4-5, `review_status: edited`; idx12 (p19) absorbed into idx11 (p18, exhibit table) → new range p18-19, `review_status: edited`.

| before | after |
|---|---|
| 13 segments | 11 segments (2 absorbed) |

Post-merge `validate_segments()`: 0 errors, full 19/19 page coverage, 0 remaining null-label segments. **DOC_005 segmentation is now fully clean of the known-gaps item 34 pattern.**

### Stage 1 proposal state — final, before case-level approval

| document_id | final segment count | edited segments (human-corrected) | pending segments | null-label remaining |
|---|---|---|---|---|
| DOC_003 | 106 | 19 | 87 | 0 |
| DOC_004 | 102 | 25 | 77 | 0 |
| DOC_005 | 11 | 3 (p4-5, p9-15, p18-19) | 8 | 0 |

Next: case-level `approve` (sweeps remaining `pending` segments to `approved`, advances case-level `review_status`) for all three documents, then `split`.

### Case-level approval and split — completed

`segment_case.py approve CASE_112 {DOC_003,DOC_004,DOC_005} --reviewer pyun` (case-level, no `--segment`/`--edit`): all three returned `status: approval_applied`, `review_status: approved`, `not_ready: []` — every remaining `pending` segment swept to `approved`, no readiness blockers.

`segment_case.py split CASE_112 {DOC_003,DOC_004,DOC_005} --held-by pyun --run-id RUN_20260730_001`:

| source bundle | logical documents created | new document_id range |
|---|---|---|
| DOC_003 (145p) | 106 | DOC_006–DOC_111 |
| DOC_004 (178p) | 102 | DOC_112–DOC_213 |
| DOC_005 (19p) | 11 | DOC_214–DOC_224 |

219 new logical `DOC_XXX.pdf` files total, plus DOC_001/DOC_002 (`not_required`, retained as single documents). Each source bundle retained as `superseded_bundle` per the tool's own design (not deleted, not further used downstream).

**`dao.py check-segmentation-ready CASE_112` → `{"clear": true, "blockers": []}`.** Stage 1 (Case Intake & Document Segmentation) is complete. Stage 2 (document-pipeline) preflight gate is open.

## Stage 2 — Document Processing (document-pipeline) — sample validation, HALTED

Per user decision (2026-07-30), before running checkpoint 1 across all 221 eligible documents, validated the pipeline on a 6-document sample first: DOC_001 (10p, not_required original), DOC_002 (15p, not_required original), DOC_006 (1p, DOC_003-derived), DOC_007 (3p, merged DOC_003-derived), DOC_214 (1p, DOC_005-derived), DOC_221 (7p, merged discharge document, DOC_005-derived) — chosen to cover both un-split originals and split/merged logical documents of varying length.

Dev-phase P8 same-provider fallback per `harness-guardrails-dev`: `--reader-a claude-cli --reader-b claude-cli --comparator claude-cli --classifier-provider claude-cli`, binary overridden to the Windows npm `claude.exe` path.

| doc_id | pages | result |
|---|---|---|
| DOC_214 | 1 | **PASS** — page agreed, classified `diagnosis_certificate` |
| DOC_006 | 1 | **PASS** — page agreed, classified `insurance_policy` |
| DOC_007 | 3 | **FAILED** — p1 agreed, then `error: claude-cli call failed: You've hit your monthly spend limit` |
| DOC_221 | 7 | **FAILED** — p1 **disagreed** (real P8 disagreement, unresolved), then same monthly-spend-limit error on the next call |
| DOC_001 | 10 | not yet run (halted before reaching it) |
| DOC_002 | 15 | not yet run (halted before reaching it) |

**Halt reason: external account constraint (claude-cli monthly spend limit reached), not a pipeline or code defect.** Both failures are the identical error string. Verified post-failure state via DAO: `document_manifest.json` shows DOC_007/DOC_221 still `ocr_status: pending` (no partial/contaminated write — the tool did not patch the manifest before failing), no lock held on the manifest. DOC_006/DOC_214's successful results are intact and unaffected.

**Not retried automatically.** Per P9 (partial/failed stage → retry up to 3 fixed attempts, then halt for audit) — a spend-limit error is not the kind of transient failure P9's retry loop is meant for (retrying without raising the limit would just reproduce the same error), so retrying blindly would waste calls without resolving anything. Reporting to the user instead of looping.

**Also flagged, not yet resolved**: DOC_221 page 1 showed a genuine P8 disagreement before the spend-limit error hit — this needs real human resolution (`run_checkpoint1.py resolve-disagreement`) once the account constraint clears, not an assumption that either reading is correct.

### Stage 1 summary

- Input: 5 raw PDFs (DOC_001–DOC_005), page counts 10/15/145/178/19
- Human segmentation decisions: DOC_001/DOC_002 `not_required` (estimated filename mapping, size-based — DOC_003/DOC_004 sizes were too close to disambiguate without opening the audit-only intake crosswalk); DOC_003/DOC_004/DOC_005 `required`
- Contact-sheet geometry: 4x4 grid, crop_ratio 0.33 (production default) for all 3 required bundles; sheet counts 10/12/2
- `propose` (production defaults, no `--fallback-cap-ratio` override): DOC_003 135 segments (9/9 fallback pages resolved), DOC_004 141 segments (10/10 fallback pages resolved), DOC_005 19 segments (**0/9 fallback pages resolved — fallback saturated**, default 25% cap too low for a 19-page document with 9 crop-ambiguous pages)
- `--refine` attempted once on DOC_005 at human direction; measured no-op (0 vision calls) since refine targets over-merged long segments, not fallback-saturated singletons — the two failure modes are orthogonal
- **Real finding, not incidental**: known-gaps.md item 34 (new) — 100% of null-provisional-type-label segments in DOC_003 (29/29) and DOC_004 (39/39) had `boundary_evidence` text self-describing a continuation of the prior page while still being classified as a `boundaries` entry. `merge_sheet_proposals()` has no check against a boundary claim's own evidence text, and `--refine` cannot catch this shape either (all-1-page-segments, nothing meets the refine length threshold). Bug recorded, not fixed in code this run — human-directed workaround applied at the approve step for CASE_112 only
- Human corrections applied (all via `segment_case.py`'s own DAO-governed proposal read/write, not hand-edited files): DOC_003 29 segments merged into 19 edited ranges; DOC_004 39 segments merged into 25 edited ranges; DOC_005 9 segments (p9-15) merged and relabeled from the model's incorrect "진료비 세부산정내역(입원)" to human-confirmed discharge-related content, plus 2 more null-label segments (p5, p19) merged per the same known-gaps-34 pattern
- Final segment counts after correction: DOC_003 106, DOC_004 102, DOC_005 11 — down from proposed 135/141/19
- Case-level approval: all 3 documents `approved`, 0 `not_ready` blockers
- Split output: 219 new logical documents (DOC_006–DOC_224); source bundles retained as `superseded_bundle`
- Stage 2 preflight (`check-segmentation-ready`): **PASS**, 0 blockers

## Stage 2 — Document Processing (document-pipeline) — sample validation, RESUMED and COMPLETE

Claude-cli monthly spend limit cleared (external account constraint, user-confirmed). Resumed the halted 6-document sample batch from where it stopped: DOC_007 and DOC_221 retried, then the 2 not-yet-attempted documents (DOC_001, DOC_002) run to complete the batch. Same dev-phase P8 same-provider fallback config as before (`--reader-a claude-cli --reader-b claude-cli --comparator claude-cli --classifier-provider claude-cli`, binary overridden to the Windows npm `claude.exe` path). Pre-resume check: `dao.py check-lock` on `document_manifest.json` — unlocked, safe to proceed.

| timestamp | doc_id | pages | result |
|---|---|---|---|
| 2026-07-30T (resume) | DOC_007 | 3 | **PASS** — all 3 pages agreed (p1 cached from the pre-halt attempt), classified `insurance_policy` |
| 2026-07-30T (resume) | DOC_001 | 10 | **PASS** — all 10 pages agreed, classified `other` |
| 2026-07-30T (resume) | DOC_221 | 7 | **FAILED — real P8 disagreement, not a transient error.** p1 disagreed (cached from pre-halt attempt), plus p3/p4/p5/p6/p7 all newly disagreed on retry — 6 of 7 pages total. `status: blocked_disagreement`. Halted per P8 zero-tolerance (any disagreement halts immediately) |
| 2026-07-30T (resume) | DOC_002 | 15 | **PASS** — all 15 pages agreed, classified `other` |

**DOC_221 detail**: `document_manifest.json` post-halt state verified via DAO — `ocr_status: failed`, `cross_validation_status: disagreed_pending_review`, no lock held, no partial/corrupted write (consistent with the same clean-halt behavior confirmed for the pre-limit DOC_221/DOC_007 halt). `ocr_result_DOC_221.json` and a raw-reading scratch file (`_ocr_scratch/CASE_112_DOC_221_raw.json`) were written per the tool's own disagreement-preservation design, so a future resolution pass does not need to re-run OCR.

Note: DOC_221 is the same document as the DOC_005-derived p9-15 discharge-related segment that required human relabeling during Stage 1 (see above) — the segment content itself was already flagged as unusual/hard-to-classify by a human reviewer before OCR ever ran on it. A 6-of-7-page disagreement rate is far above anything else seen in this run's sample (0 disagreements across DOC_001/002/006/007/214, 260 total agreed pages) and is consistent with this being a genuinely difficult-to-read source document (e.g., handwriting, poor scan quality, dense tabular content), not a fluke. This is a hypothesis, not a finding — the actual cause needs a human to open the page images and compare against `ocr_result_DOC_221.json`'s two stored readings.

### 6-document sample — final tally

| doc_id | pages | segmentation origin | result |
|---|---|---|---|
| DOC_001 | 10 | not_required (single doc) | PASS |
| DOC_002 | 15 | not_required (single doc) | PASS |
| DOC_006 | 1 | DOC_003-derived | PASS |
| DOC_007 | 3 | DOC_003-derived (merged, known-gaps-34 workaround) | PASS |
| DOC_214 | 1 | DOC_005-derived | PASS |
| DOC_221 | 7 | DOC_005-derived (merged, known-gaps-34 + human relabel) | **BLOCKED — genuine P8 disagreement, needs human resolution** |

5/6 PASS, 1/6 blocked on a real (not infrastructure) P8 disagreement. Sample validates the pipeline mechanism itself (OCR → cross-validation → classification → manifest patch all worked correctly end to end for 5 documents of varying length: 1p, 3p, 7p-attempted, 10p, 15p), and correctly caught a genuine hard case rather than silently mis-resolving it.

**Next decision needed (human)**: DOC_221's disagreement must be resolved via `run_checkpoint1.py resolve-disagreement` (or equivalent DAO-governed path) with a human comparing the two stored readings against the source page images — not something to resolve by assumption. Pending that, the open question is whether to (a) resolve DOC_221 now and then proceed to the full 221-document batch, or (b) proceed with the full batch first and resolve DOC_221 in parallel/after, given it's an isolated single-document blocker that doesn't block other documents' checkpoint-1 runs.

**User decision (2026-07-30)**: proceed with the full batch, DOC_221 held out for later human resolution.

## Stage 2 — Document Processing (document-pipeline) — full batch (213 remaining documents)

Ran `run_checkpoint1.py run` sequentially (not parallel — each call takes the `document_manifest.json` lock) across all 213 documents not yet processed (221 total eligible minus DOC_006/007/214/001/002 already passed in the sample, minus DOC_221 held out). Same dev-phase P8 same-provider config throughout (`--reader-a claude-cli --reader-b claude-cli --comparator claude-cli --classifier-provider claude-cli`, binary overridden to the Windows npm `claude.exe` path). Driven by a scratch shell script (not part of the repo) that calls the DAO-backed tool per document and classifies each result by parsing its JSON status output; continues past individual failures rather than aborting the whole batch, so one hard document does not block the rest.

**Tooling note (not a pipeline defect)**: the first script draft mis-parsed `run_checkpoint1.py`'s JSON output because the Windows child-process output carried CRLF line endings and a scratch document-list file (written by a prior Python `open()` call without newline normalization) also carried CRLF, corrupting a file-path argument passed to the tool (`OSError: [Errno 22] Invalid argument`). Caught immediately from the run's own log before it affected real document processing (DOC_009's manifest entry was verified still clean/`pending`, no corruption), fixed by stripping `\r` from both the list file and captured subprocess output before parsing.

### Batch run 1 — 213 documents

| result | count |
|---|---|
| passed | 181 |
| blocked_disagreement (real P8 disagreement) | 17 |
| error (external: claude-cli monthly spend limit hit mid-batch, recovered partway through) | 15 |

Spend-limit errors were contiguous: DOC_127–DOC_141 (15 documents in a row), then DOC_147 onward passed normally again — consistent with the account limit being temporarily exhausted and recovering mid-run, not a persistent block. Verified via DAO: all 15 documents' manifest entries remained cleanly `ocr_status: pending` (no corruption), so a clean retry was possible without any repair step.

**Documents with real P8 disagreement (18 total including DOC_221, all held for human resolution, not resolved by assumption):**

DOC_039 (p1), DOC_042 (p1), DOC_096 (p1), DOC_110 (p1), DOC_112 (p3,p5), DOC_115 (p3), DOC_149 (p2,p3), DOC_150 (p4), DOC_154 (p3), DOC_166 (p2), DOC_173 (p3), DOC_175 (p1), DOC_176 (p1), DOC_190 (p1), DOC_198 (p1), DOC_208 (p1), DOC_222 (p1), DOC_221 (p1,p3,p4,p5,p6,p7 — the pre-existing 6/7-page outlier from the sample validation).

Aside from DOC_221's outlier severity, the other 17 are each single- or few-page disagreements (1-2 pages out of each document), consistent with normal OCR ambiguity rather than a systemic reading problem — a materially different profile from DOC_221.

### Batch run 2 — retry of the 15 spend-limit-failed documents (DOC_127–DOC_141)

Spend limit had visibly recovered by the time DOC_147+ processed normally in run 1, so a clean retry was attempted rather than waiting further or switching provider. **Result: 15/15 passed** — confirms the run-1 failures were purely the external spend-limit constraint, not a document-content or tooling problem.

### Checkpoint 1 — final state for CASE_112 (all 221 eligible documents)

Confirmed via `document_manifest.json` (DAO read, not direct file access):

| status | count |
|---|---|
| `ocr_status: completed` (checkpoint 1 fully passed) | 203 |
| `ocr_status: failed` (real P8 disagreement, human resolution required) | 18 |
| `ocr_status: pending` (unprocessed) | 0 |
| `downstream_disposition: superseded_bundle` (original bundles, correctly skipped) | 3 |
| **total manifest entries** | **224** |

**All 18 documents needing human P8-disagreement resolution** (`run_checkpoint1.py resolve-disagreement` or equivalent DAO-governed path, comparing the two stored readings against source page images — not to be resolved by assumption):

DOC_039 (p1), DOC_042 (p1), DOC_096 (p1), DOC_110 (p1), DOC_112 (p3,p5), DOC_115 (p3), DOC_149 (p2,p3), DOC_150 (p4), DOC_154 (p3), DOC_166 (p2), DOC_173 (p3), DOC_175 (p1), DOC_176 (p1), DOC_190 (p1), DOC_198 (p1), DOC_208 (p1), DOC_221 (p1,p3,p4,p5,p6,p7 — outlier severity, flagged separately since Stage 1), DOC_222 (p1).

Checkpoint 1 (OCR + P8 cross-validation + classification) is now complete for every document that can complete without a human decision. Checkpoint 2 (redaction) and checkpoint 3 (chunking) have not yet been run for any document — next step, pending a decision on whether to proceed past the 18 held-out documents or resolve them first.

### P8 disagreement resolution — human review, 16 of 18 resolved

Reviewer (pyun) inspected each disagreement by reading both stored transcriptions from `_ocr_scratch/CASE_112_{doc_id}_raw.json` (page-level `reading_a`/`reading_b`, plus the comparator's own `disagreement_details`) — not by re-viewing source page images for this pass. Resolutions applied via `run_checkpoint1.py resolve-disagreement --page N --chosen-reading {reading_a|reading_b} --resolved-by pyun --note "..."`.

| doc_id | page(s) | chosen | reviewer rationale (summarized) |
|---|---|---|---|
| DOC_039 | 1 | reading_a | Identical content; readings differ only in trailing whitespace/indentation |
| DOC_042 | 1 | reading_b | reading_b judged the accurate transcription |
| DOC_096 | 1 | reading_a | reading_a judged the accurate transcription |
| DOC_110 | 1 | reading_a | reading_a judged the accurate transcription |
| DOC_112 | 3, 5 | reading_a (both) | reading_a judged accurate on both disagreed pages |
| DOC_115 | 3 | reading_a | reading_a judged the accurate transcription |
| DOC_149 | 2, 3 | reading_a (both) | reading_a judged accurate on both disagreed pages |
| DOC_150 | 4 | reading_a | reading_a judged the accurate transcription |
| DOC_154 | 3 | reading_a | reading_a judged the accurate transcription |
| DOC_166 | 2 | reading_b | reading_b judged the accurate transcription |
| DOC_173 | 3 | reading_a | Readings appeared identical in content; reading_a picked |
| DOC_175 | 1 | reading_a | reading_a judged the accurate transcription |
| DOC_176 | 1 | reading_a | reading_a judged the accurate transcription |
| DOC_190 | 1 | reading_a | reading_a judged the accurate transcription |
| DOC_198 | 1 | reading_b | reading_b judged accurate (no spurious mid-content line break) |
| DOC_208 | 1 | reading_b | reading_b judged the accurate transcription |

All 16 documents transitioned to `status: passed` / `cross_validation_status: disagreed_resolved` immediately upon their last page's resolution (multi-page documents DOC_112/DOC_149 correctly stayed `partially_resolved` after their first page and completed only after the second), and each was reclassified as `insurance_policy` by the tool's post-resolution classification step, consistent with all 16 being policy-clause-bundle documents.

**Held out, not resolved — human-flagged for further review, per explicit user instruction:**

- **DOC_221** (7 pages, 6 disagreed: p1,3,4,5,6,7) — reviewer judged `reading_b` generally better but noted it has some missing content the other reading captures; not a clean pick either direction. This is the same document flagged since Stage 1 (a merged discharge-related segment that required human relabeling before OCR ever ran).
- **DOC_222** (1 page disagreed: p1) — reviewer found both `reading_a` and `reading_b` have real problems (format and content), neither cleanly usable as-is.

Both remain `ocr_status: failed` / `cross_validation_status: disagreed_pending_review` in the manifest — correctly still blocking those two documents from checkpoint 2/3 and downstream stages, without blocking the other 219.

### Checkpoint-1 final tally after resolution round

| status | count |
|---|---|
| `ocr_status: completed` | 219 |
| `ocr_status: failed` (DOC_221, DOC_222 — held for further review) | 2 |
| `downstream_disposition: superseded_bundle` | 3 |
| total manifest entries | 224 |

### Design observation recorded (not fixed) — `insurance_policy` OCR redundancy

Reviewing all 16 resolved disagreements surfaced a consistent pattern: every one was a formatting-only difference (whitespace/indentation, or presence/absence of a trailing boilerplate line such as an insurer slogan) between two readings that agreed on all substantive clause content — never a real content disagreement. All 16 are `insurance_policy`-typed documents. Recorded as `known-gaps.md` item 35 (new): for documents already known/classified as policy-clause bundles, a full second independent vision read may be low marginal value against its cost (extra vision call + human-resolution overhead for formatting noise), and embedded-text extraction or a single read might be a better-suited path for this document class specifically — flagged as a design idea for a future decision, not implemented or decided this session. No code changed.

## Stage 2 — Document Processing (document-pipeline) — Checkpoint 2 (redaction)

219 documents eligible (`ocr_status: completed`, no `redacted_text_path` yet) — DOC_221/DOC_222 correctly excluded (still `ocr_status: failed`), superseded bundles correctly excluded.

**Provider deviation, recorded honestly**: `harness-guardrails-dev`'s documented dev-phase default for checkpoint 2 is `codex-cli`. Tested first — `codex` is not on this shell's PATH (confirmed via `which codex`, no result; unlike `claude`, which resolves via the npm global install). Substituted `claude-cli` (`--provider claude-cli`, same `HARNESS_CLAUDE_COMMAND` binary override as checkpoint 1) for all 219 documents in this run. This is a deviation from the documented default, not a silent substitution — recorded here per the same honesty requirement applied to `cross_validation_mode` elsewhere in this run. Functionally, `redact_document.py`'s design (span-substitution via the `Redactor` abstraction, hard-fail on any detected PII leak, `review_required` flag for over-redaction risk) is provider-agnostic; the deviation only changes which model identifies PII spans, not the enforcement mechanism around it.

DOC_001 run individually first as a smoke test (`items_redacted: 1, review_required: false` — passed), then the remaining 218 run as a sequential batch (redaction takes per-document locks; each call redacts one document's full page set in one model call, not one call per page like checkpoint 1's P8 reads).

### Real bug found and fixed — `redact_document.py` never set `reviewer_role`

Batch run 1 processed 4 documents cleanly, then failed on **DOC_002** (the first document in the run whose redaction actually produced `review_required: true` — an over-redaction-risk case, a span left un-redacted to avoid corrupting kept text). `dao.py write-contract` correctly rejected the write: `common_component_output.schema.json` requires `reviewer_role` whenever `review_required` is `true`, and `redact_document.py`'s contract builder never set it (unlike `run_checkpoint1.py`, which already sets `reviewer_role: 손해사정사` for its own `review_required` case, a P8 disagreement). This is exactly the fail/don't-persist contract working as designed — no corrupted state was written; the document simply blocked.

Fix: `redact_document.py` now sets `contract["reviewer_role"] = "손해사정사"` and a `review_reason` explaining the over-redaction-risk rationale whenever `review_required` is true, routed the same way `run_checkpoint1.py` routes its own review case (over-redaction risk is a masking-completeness question, not a medical/legal judgment call). Verified against the real failing case: re-ran DOC_002 after the fix — passed cleanly (`review_required: true`, schema-valid write). Existing `tests/test_redact_document.py` suite (4 tests) passed unchanged both before and after, confirming no prior test covered the `review_required: true` branch — new regression coverage for this case is deferred, not added this session. Recorded as `known-gaps.md` item 36.

### Checkpoint-2 batch execution (3 passes)

- **Pass 1** (36 documents, before the DOC_002 fix): succeeded on the first 4 documents (including the DOC_001 smoke test), halted cleanly at DOC_002's schema-validation rejection (no corrupted write). After the fix, re-ran DOC_002 directly — passed.
- **Pass 2** (183 documents, `run_batch_checkpoint2_v2.sh`, background task `b73l17zuw`): list re-derived from manifest state to cover the genuinely-remaining documents. Completed, but **all 183 entries logged `error: dao.py read-contract CASE_112 failed: NOT_FOUND: ...\ocr_result_DOC_NNN`** — including documents (e.g. DOC_040) whose `ocr_result_DOC_040.json` file was confirmed present on disk and whose `read-contract` call succeeded immediately when re-run directly afterward. Root cause: a transient environment condition present only at that batch's launch (not reproduced on re-test of the same documents moments later; no stale lock files found; no code defect identified). Treated as an execution-environment issue, not a logic bug — no code changed for this failure.
- **Pass 3** (145 documents, `run_batch_checkpoint2_v3.sh`, background task `bjkp5q6ug`): list recomputed directly from actual output state (`ocr_result_*.json` present, no matching `redaction_result_*.json` yet) rather than reusing pass 2's stale list. First 3 documents verified individually before backgrounding the rest, to confirm the transient failure mode was not still present. Result: 142/145 succeeded; the remaining 3 were DOC_221 and DOC_222 (expected — still `cross_validation_status: disagreed_pending_review`, correctly rejected by checkpoint 2's eligibility check: `checkpoint 2 blocked: {doc_id} cross_validation_status is 'disagreed_pending_review'`) plus one duplicate-flagged entry, not a new failure mode.

### Checkpoint-2 final tally

219/219 eligible documents (all `ocr_status: completed` documents, i.e. every document except DOC_221/DOC_222 which remain blocked at checkpoint 1) now have `redaction_result_{doc_id}.json` and populated `redacted_text_path`. Verified directly from output-file state (`ls outputs/CASE_112/redaction_result_DOC_*.json` count = 219, cross-checked against the 221 `ocr_result_*.json` minus the 2 blocked documents).

Of the 219, exactly **1** (`DOC_002`) has `review_required: true` — the case that surfaced the `reviewer_role` bug; its `review_reason` records the over-redaction-risk rationale per the fix above. All other 218 completed with `review_required: false`.

Checkpoint 2 (redaction) is complete. Proceeding to checkpoint 3 (chunking).

## Stage 2 — Document Processing (document-pipeline) — Checkpoint 3 (chunking)

`tools/chunk_text.py` invoked once, case-wide, across all 219 documents with `redacted_text.md` (the same set completed in checkpoint 2; DOC_003/DOC_004/DOC_005 correctly excluded as `superseded_bundle`, DOC_221/DOC_222 correctly excluded as still blocked at checkpoint 1). Deterministic page-slicer — no model call, one chunk per page (`page_start == page_end` always), verbatim substrings sliced on the `<<<PAGE page=N>>>` markers checkpoint 2 embeds.

Output: 359 chunks (`CHUNK_1`..`CHUNK_359`) covering all 219 documents, one combined `page_chunks.json` for the case (not per-document, per the schema's case-scoped framing). The tool itself only prints the `{"chunks": [...]}` payload to stdout — wrapped with the standard contract envelope (`case_id`, `run_id`, `component: document-pipeline`, `status: success`) before writing, matching `page_chunks.schema.json`'s `allOf`/`common_component_output.schema.json` composition (no `model_info` needed or included — deterministic, not model-generated). Written via `dao.py write-contract CASE_112 page_chunks.json --schema-name page_chunks.schema.json` — schema-PASS on first attempt after the envelope wrap.

Checkpoint 3 is complete. **Document-pipeline's full three-checkpoint sequence (OCR/P8 → redaction → chunking) is now finished for CASE_112**, covering 219 of 221 OCR-completed documents; DOC_221/DOC_222 remain held for further human review and are excluded from all three checkpoints' outputs.

## Human classification correction — DOC_001 / DOC_002

While scoping Stage 4 (policy-pipeline), the human reviewer (pyun) inspected DOC_001/DOC_002's actual redacted content and found a real classification gap. Both had been classified `predicted_document_type: other` at checkpoint 1 (real inference, not pre-flagged).

- **DOC_002** ("손해사정 업무 협조요청 공문") — content includes an explicit denial rationale: "법률자문 상 피보험자의 법률상배상책임이 발생하지 않는다" (per legal advice, the insured has no legal liability). The schema's `document_type` enum has a dedicated category, `insurer_response`, specifically for this class of document, and `denial-response` is dependency-triggered off documents classified that way per pipeline.md. Left as `other`, this document would never have reached `denial-response`. **Corrected**: `predicted_document_type` → `insurer_response`, `document_type_label` updated, a second `evidence_references` entry added citing the denial-rationale quote directly. Written via `dao.py write-contract` (schema-PASS) plus `patch-manifest-document` to keep `document_manifest.json`'s `document_type` field in sync.
- **DOC_001** ("법률질의회신서") — a lawyer's legal-opinion letter analyzing occupier/owner liability and comparative fault for the underlying accident; substantively the source that DOC_002's denial rationale draws on, and plausible rebuttal-relevant material for `denial-validation` later. The schema's `document_type` enum has no dedicated category for claimant-side legal opinion/rebuttal material (checked the full enum: `insurance_certificate`, `insurance_policy`, `application_form`, `diagnosis_certificate`, `medical_record`, `imaging_report`, `receipt`, `insurer_response`, `other`) — `other` remains the schema-accurate type. **Corrected**: `document_type_label` only, to "피보험자측 법률자문(반박근거) - 손해배상책임/과실비율 검토" for downstream legibility; `predicted_document_type` left as `other` per explicit user decision (adding a new enum value was considered and declined as disproportionate for a single case).

Both corrections are human-reviewed relabeling of real checkpoint-1 output, not a new inference pass — no new model call was made.

## Stage 4 — Policy Pipeline — P0-8 structural blocker, override, and a deeper Stage-1/2 lineage gap

### P0-8: OCR-sourced documents cannot pass real table-boundary verification

All 203 `insurance_policy`-classified documents in CASE_112 (two segmented physical parents: `DOC_003.pdf` → 106 segments `DOC_006`-`DOC_111`; `DOC_004.pdf` → 102 segments `DOC_112`-`DOC_213`) are OCR-sourced (`extraction_method: ocr`, no embedded text layer). `tools/table_region_provenance.py`'s `VERIFIABLE_EXTRACTION_METHODS = frozenset({"embedded_text"})` is a deliberate fail-closed design: `scan-table-candidates` (the real deterministic table-boundary detector) categorically refuses to run on any OCR-sourced document, since there is no reliable text layer to scan. This blocked policy-pipeline finalization for the entire corpus.

Flagged to the user rather than worked around silently. User made an explicit, informed risk-acceptance decision to proceed without real table-boundary verification for this corpus, with the risk logged first (`known-gaps.md` item 37).

**Implementation** (human-authorized override, not a relaxation of the existing check):
- New `scan_status: "unverifiable_ocr_source"` (schema: `table_region_index.schema.json`) — never `complete_no_candidates`, which is reserved for a genuine scan that looked and found nothing. Requires a schema-mandated `human_override: {authorized_by, authorized_at, reason}` object.
- New `dao.py record-unverifiable-table-scan CASE_ID --doc-id DOC_ID --authorized-by NAME --reason TEXT --held-by NAME --run-id RUN_ID`: verifies source identity (document registered, `extraction_method` is genuinely non-`embedded_text`, PDF digest current, revision bound, physical page mapping resolves) but never opens the PDF or runs a detector.
- `_table_region_finalize_blockers` treats `unverifiable_ocr_source` as a non-blocker by design (never silent — `human_override` is permanently on record).
- Two secondary validation-site gaps found and fixed via test-first development before touching real CASE_112 data: `table_region_provenance`'s inventory-integrity checker didn't recognize the new status (false "unknown scan_status" error); `inventory_currency_errors` rejected the override's placeholder detector-profile value (fixed by skipping only the detector/sentinel-profile checks for this status, keeping all source-identity currency checks active).
- 9 new tests, full suite 1739 passed at implementation time.

**Batch execution — a genuine, unresolved environmental flakiness pattern.** Recording the override for all 203 documents required far more attempts than expected:
- Attempt 1 (no retry, no delay, 199 docs): 0/199 succeeded. Every failure read `BLOCKED: {doc_id} is not registered in document_manifest.json` — but the documents WERE registered (confirmed directly), and every failing document succeeded instantly on manual direct retry.
- Attempt 2 (retry logic, up to 3x per doc, no delay, 195 remaining): same failure signature, now failing consistently rather than intermittently for large contiguous blocks.
- Attempt 3 (retry + inter-call delay, 195 remaining): same failure signature; delay did not fix it.
- Diagnostic isolation: single direct calls always succeeded, including immediate re-tries of documents the batch had just failed on. A user-run 6-document loop (`diag_loop.sh`, no delay, run directly in the user's own terminal rather than through the agent's background-task tool) succeeded cleanly end to end. A larger 167-document run through the same direct-terminal path also hit the identical failure signature starting partway through. No logic-level root cause was ever identified: `read_contract_data`/`load_json` (`tools/dao.py:571,230`) is a plain synchronous `path.read_text()` with no caching; `case_dir`/`ROOT` resolve via `__file__`, not `cwd`; no stray `.lock` files were found at failure time; locking code (`acquire_lock`/`release_lock`, atomic `O_EXCL`) reviewed and appears correct. The one structural note worth flagging without more evidence: `load_json` (`dao.py:230`) does a separate `path.exists()` check before `path.read_text()` — a narrow TOCTOU window — but this was not confirmed as the actual cause.
- Resolution was pragmatic, not diagnostic: the user ran the override in small batches directly from their own terminal (recomputing the "remaining" list fresh from actual `_table_region_index.json` state each time, so repeated runs never duplicated work), rather than through this agent's background-task execution path, and completed all 203 documents this way.
- **Final verified state**: all 203 policy documents carry `scan_status: "unverifiable_ocr_source"` with a valid `human_override` block, confirmed by reading `_table_region_index.json`'s `candidates` list directly (203/203 present, 0 remaining).
- **Not yet done**: root-causing why this specific batch pattern failed so much worse than prior transient-failure incidents this session; no code fix applied since no code-level cause was found.

### Two policy-pipeline subagent dispatches (post-override) — real clause extraction blocked by a Stage-1/2 lineage gap, not P0-8

With all 203 documents overridden, two `policy-pipeline` subagents were dispatched, scoped to each segmented parent. **Neither produced any policy contract output.** Both independently reached the same root finding via different documents (DOC_003.pdf's parent vs. DOC_004.pdf's parent), which corroborates it as a real, general defect rather than a one-off:

**Confirmed directly against `document_manifest.json`** (not just agent-reported): `DOC_003` and `DOC_004` (the physical parents) both show `document_type: None`, `source_total_pages: None`, `document_role: None` — i.e., the parents themselves were never fully classified/measured by document-pipeline. Every one of the 208 in-scope child segments shows `document_role`, `page_map`, `source_document_id`, `segment_page_ranges`, `derivation_method`, `derived_text_path`, `derived_text_sha256` all `None`/absent — only the informal Stage-1 fields (`source_file_name`, `source_page_start`, `source_page_end`) are populated anywhere in the manifest (224/224 entries).

**Root cause, confirmed via `_run_state.json`**: the `document_processing` stage is stuck at `status: "in_progress"` (`attempt_count: 219`, `backup_path: null`, never `passed`). `policy_clause_processing` is `status: "failed"`. Document-pipeline's checkpoints 1-3 (OCR, redaction, chunking) genuinely completed for 219/221 documents (verified earlier this run), but the stage was never formally finalized as `passed` in run-state — and the first-class segment-lineage fields appear to be written only as part of that finalization step, which never ran.

**Consequence — this blocks more than `policy_parent_coverage`, contrary to how it was originally scoped:**
- `register-segment-derivation` refuses for every segment: `BLOCKED: {doc_id} is not a segment (document_role=None)`.
- `_physical_page_for` (`dao.py:1891`), when `document_role != "segment"`, falls back to treating the *logical* page as the *physical* page. Concretely verified: DOC_008 is physically pages 5-6 of `DOC_003.pdf` (per its own `source_page_start`/`source_page_end`), but resolves to physical pages 1-2 under this fallback — and its identity digest resolves to the segment's own carved single-document PDF rather than the immutable 145-page parent.
- **`_policy_write_scheme_blockers` refuses every policy-layer contract write (not just `policy_parent_coverage`) for any document with `uid_scheme != canonical_v1`.** Since canonical UID migration itself depends on correct segment lineage, and 208 of 208 in-scope segments have none, no `normalized_policy_clause_*.json` could be written for any of them either — the DOC_004.pdf agent's briefing assumption that clause extraction was independent of the lineage gap was incorrect, and the agent correctly discovered and reported this rather than proceeding.
- **A false-positive diagnostic from an earlier (pre-override) subagent dispatch is now understood to be unsound**: `enable-canonical-uids` (a one-way, non-reversible flag) was run on `DOC_007` and `DOC_008` (DOC_003.pdf's parent) and `DOC_112` (DOC_004.pdf's parent) as what was believed to be a harmless diagnostic probe. It is not harmless: the segment precondition check that `enable-canonical-uids` should have enforced (`dao.py:4323`) is itself gated on `document_role == "segment"` — the exact field that's missing. With `document_role: None`, the check silently passed instead of correctly refusing, so these 3 documents are now permanently flagged `canonical_v1` with UIDs minted against wrong physical-page identity (logical page mistaken for physical page, digest bound to the segment's own carved PDF rather than the true parent). This cannot be undone by re-running anything; it requires an explicit, deliberate repair path once Stage-1/2 lineage is fixed. **User decision needed**: how to handle DOC_007/DOC_008/DOC_112's premature UID flip.
- Both subagents correctly wrote **zero** contract files rather than produce output with fabricated/incorrect provenance — this was the right call, not a stall. `normalized_policy_clause_DOC_008.json` (DOC_003.pdf agent) failed DAO schema validation and nothing was persisted, matching the fail/don't-persist contract.

**What is genuinely fine, confirmed by both agents:**
- P0-8 override holds cleanly across all 203 documents — zero new P0-8 refusals hit during either dispatch.
- Both parents' segment ranges are contiguous and correctly bounded: DOC_003.pdf → 106 segments, pages 1-145, zero gaps (104 in-scope after excluding DOC_077=`other`, DOC_091=`insurance_certificate`); DOC_004.pdf → 102 segments, DOC_112-DOC_213 (99 in-scope after excluding DOC_124=`application_form`, DOC_183=`other`/low-confidence, DOC_194=`insurance_certificate` — corrects an earlier briefing that reported no exceptions in this range).
- Underlying OCR text is real and usable: DOC_003.pdf's corpus (~71.5k non-whitespace chars, 381 `제N조` clause anchors, 삼성화재 영업배상책임보험) and DOC_004.pdf's corpus (123,821 chars, 삼성화재 배상책임보험 약관) both read cleanly in Korean. One agent's own trial extraction (DOC_008: 4 clauses, 6 exclusions, offsets verified against page bodies) was substantively correct — only its identity/provenance basis was unsound, which is exactly why it wasn't persisted.

**Status: Stage 4 (policy-pipeline) cannot produce any real output for CASE_112's 203-document policy corpus until `document_processing` is properly finalized** (parents' `document_type`/`source_total_pages` set by document-pipeline, not by hand; segment-lineage fields written for all 208 in-scope segments) **and `register-segment-derivation` + `enable-canonical-uids` are run correctly afterward.** This is a real Stage-1/2 defect, not a scoping decision — raised to the user rather than resolved unilaterally, pending: (1) how to repair DOC_007/DOC_008/DOC_112's premature UID flip, and (2) how to proceed on completing `document_processing` finalization so the lineage fields get written.

## Correction — the "segment lineage gap" diagnosis above was wrong; DOC_006-213 are `physical`, not `segment`

Everything in the section above (the two subagent dispatches' shared conclusion, the DOC_112 "false-positive UID flip" finding, and the plan to fix `document_processing` finalization) was built on an incorrect premise, discovered and corrected later in this same run. Recorded here rather than deleted, per this run-notes file's append-only discipline.

**What actually happened, in order:**

1. **`_run_state.json` was edited directly, bypassing the DAO** (a hard-rule violation: "No agent reads or writes outputs/, data/, or a ledger/run-state file directly"). The user set `document_processing` to `status: "passed"` by hand, believing the stuck `in_progress` status (`attempt_count: 219`, DOC_221/DOC_222 held back) was the reason segment-lineage fields were never written. **Confirmed this had zero effect on the actual blocker**: `document_role`/`page_map`/`source_document_id` were still `None` on every entry after the edit — `register-segment-derivation` still refused identically. Restored to `in_progress` via the proper DAO path (`dao.py update-run-state CASE_112 RUN_20260730_001 document_processing in_progress --held-by pyun`) once this was established.

2. **Traced why no code path sets `document_role: "segment"`.** Grepped every reference across `dao.py`/`segment_lineage.py`/agent specs: the field is read everywhere but written nowhere — not in `segment_case.py`'s `build_manifest_entries` (its own docstring says "sets only fields this stage owns," and lineage fields are documented `owner: document-pipeline`), not in any agent spec, not automatically by `register-segment-derivation` itself (which requires the field to already be `"segment"` as a precondition, not an outcome).

3. **Found a second, independent structural blocker while investigating (1)**: `register-segment-derivation`'s own verification (`tools/segment_derivation.py`, P0-6) categorically refuses `derivation_method: "ocr_segment"` — `VERIFIABLE_METHODS = frozenset({"embedded_text_segment", "page_extraction"})` excludes it on purpose, with the module docstring stating "there is deliberately no override field." This is the same category of fail-closed design as P0-8 (OCR has no deterministic re-verifiable layer), but a second, separate gate. Flagged to the user exactly as P0-8 was; the user authorized building an equivalent human-override path.

4. **Began implementing a full P0-6 override system**, mirroring P0-8's `unverifiable_ocr_source` pattern: `segment_derivation_index.schema.json` gained a new `unverified_override_receipt`/`unverified_page` def pair (scheme `segment_page_map_unverified_v1`, schema-required `human_override` block); `document_manifest.schema.json`'s `page_map_entry` gained a `verification_status` const field; `tools/segment_derivation.py` gained `build_unverified_override_receipt`/`unverified_override_fingerprint`/`manifest_page_map_from_unverified_override`/`unverified_override_currency_errors` plus a finalization-gate branch accepting the new scheme as a non-blocker; `tools/dao.py` gained `cmd_record_unverifiable_segment_derivation` + `_commit_unverifiable_segment_derivation` (full lock/journal/rollback transaction mirroring `_commit_segment_derivation`) and a new `record-unverifiable-segment-derivation` CLI subcommand.

5. **Smoke-testing this against real DOC_008 surfaced the wrong premise.** The manifest write failed schema validation needing `derived_text_path`/`derived_text_sha256`/`segment_page_ranges` in addition to what the override already set. While tracing where those get populated, re-read `document_role`'s schema description closely: *"'physical' = an entry with its own real file in data/raw, **including a child PDF that Stage 1 materialized by splitting an intake bundle** (its source_file_name/source_page_start/source_page_end retain that audit trail). 'segment' = processed text derived from an already-processed physical parent and **has no raw file of its own**."*

   CASE_112's DOC_006-213 have real files: `split_bundle` wrote actual per-document PDFs to `data/raw/CASE_112/DOC_NNN.pdf` (confirmed on disk, `file_path` populated on every entry). They are the `physical` category, not the `segment` category that CASE_030's DOC_004/005/006 (genuinely no raw file, text sliced from an already-processed parent) exemplifies. `document_role: None` was never a missing field waiting to be filled in — per the schema's own words, absent is *correctly* treated as `physical` for this case's documents.

6. **Verified directly, in order, that this fully resolves both blockers without any override:**
   - `_physical_page_for(case_id, doc_id, logical_page)`: for `document_role != "segment"`, returns the logical page unchanged, with its own docstring stating "A physical document's logical page IS its physical page" — correct for a document that is its own file, not an error case.
   - `_registered_pdf_digest`: for a document with no `source_document_id`, uses its own `source_pdf_sha256` (each DOC_NNN.pdf's own real hash) — again correct, not a fallback masking a bug.
   - Re-ran `dao.py enable-canonical-uids CASE_112 --doc-id DOC_008` (already canonical_v1 from the earlier subagent's diagnostic run): **passed cleanly**, confirming the earlier "premature/false-positive UID flip" finding was itself mistaken — DOC_007/DOC_008/DOC_112's canonical_v1 status is valid, not corrupted, and needs no repair.
   - `dao._canonical_state_blockers('CASE_112', ['DOC_008'], ...)` → `[]` (empty).
   - `_parent_coverage_name_for('CASE_112', 'DOC_008')` → `policy_parent_coverage_DOC_008.json` (the document's own name, since `document_role != "segment"` routes coverage to the document itself, not to a shared physical-parent contract) — and since DOC_008 does not declare `policy_processing_role: segmented_parent`, `_parent_coverage_current_errors` returns `[]`: **no `policy_parent_coverage_DOC_003.json`/`policy_parent_coverage_DOC_004.json` contract is owed by this corpus at all.** The two subagents' shared blocker report and the entire premise of Part 2 above were built on the wrong document-role assumption.
   - `_policy_write_scheme_blockers('CASE_112', 'normalized_policy_clause_DOC_008.json', {'source_document_id': 'DOC_008'})` → `[]`: clause writes are unblocked.
   - Ran `_canonical_state_blockers` case-wide across all 203 policy documents: 200/203 blocked, but every one of those 200 blocks for exactly one reason — `uid_scheme=legacy`, i.e. `enable-canonical-uids` has simply not been run yet for them (normal, expected, unrelated to P0-6/lineage). Zero blockers reference `document_role`, `page_map`, or segment lineage of any kind.

**Disposition, per explicit user decision**: the P0-6 override system built in steps 3-4 above is **left in place but unused** — no code was reverted, since none of it is wrong or harmful, it is simply not the mechanism this case needs. `record-unverifiable-segment-derivation` remains available for a genuine future case that actually has `document_role: segment` entries derived from an OCR-sourced parent with no raw file of its own. Proceeding directly to real `enable-canonical-uids` + policy-clause extraction across the corpus, treating `document_role`/`page_map`/`register-segment-derivation` as **not applicable to CASE_112 at all**.

**Corrected understanding of CASE_112's actual remaining blocker**: none of Part 2's document_role/page_map findings apply. The only real remaining step before policy-clause extraction is mechanical: run `record-source-digest` + `enable-canonical-uids` for the 200 still-`legacy` policy documents (3 of 203 — DOC_007, DOC_008, DOC_112 — are already `canonical_v1`), then dispatch real clause extraction. `DOC_003`/`DOC_004` (the two `superseded_bundle` parent PDFs) need no coverage contract and are not blockers for anything downstream.

## Canonical UID migration — 203/203, completed via foreground sequential execution

Two background-batch attempts (`bz2tuqyjy` covering all 203, then `b8hvm4auf` a simplified retry-enabled v2) both failed **100%** with `BLOCKED: no readable registered raw source for {doc_id}` — an error immediately disproven by direct single-command retries (e.g. `DOC_213` succeeded instantly when `record-source-digest` was re-run directly against the exact same document right after the batch reported it failed). This is the same batch-context-only transient-failure signature observed earlier this session for the P0-8 override batches, now confirmed for a third, unrelated DAO operation — strongly suggesting the failure is tied to how this session's Bash tool executes `run_in_background: true` shell batches against this DAO, not to any property of the specific command being batched.

Root cause was not identified (as with the earlier occurrences), but this time the pattern was fully characterized: sequential **foreground** execution (no `run_in_background`) succeeded on every single call, across all 203 documents, processed in chunks of ~9-20 per tool call with zero failures. Migration was completed this way: `record-source-digest` then `enable-canonical-uids` per document, foreground, verified at the end via `uid_scheme_for()` against every one of the 203 real policy document IDs — **203/203 confirmed `canonical_v1`**, not inferred from batch logs. This is a mitigation, not a fix — the underlying cause of background-batch failure against this DAO remains open and should be investigated before relying on background batching for future large DAO operations in this project.

## Policy-pipeline dispatch 3 — two real new blockers found and fixed; a third (span-offset validation) found and left open

With all 203 documents canonical, both policy-pipeline subagents were re-dispatched for real clause extraction. Both hit genuinely new blockers (correctly distinguished from the already-resolved P0-6/coverage non-issue):

**False alarm, not a new blocker**: both agents' first report cited a P0-8 refusal (`extraction_method 'ocr' has no deterministic text layer to scan for tables`). Verified directly: this came from the agents calling `scan-table-candidates` (the real-scan CLI path) manually, which always refuses for an OCR document regardless of any existing override — it is not a check for "is there already a recorded override," just the real scan. The `unverifiable_ocr_source` override recorded for all 203 documents earlier this run was never actually reached or re-checked; `dao._table_region_finalize_blockers()` returns `[]` for every document sampled across both dispatches (12 total, including every doc_id either agent specifically cited). Subagents were briefed not to call `scan-table-candidates`/`register-table-region` directly in the next dispatch.

**Real blocker 1 (fixed) — P1-2 policy semantic analyzer was never configured.** `HARNESS_POLICY_SEMANTIC_PROVIDER`/`HARNESS_POLICY_SEMANTIC_MODEL` were unset; `resolve_trusted_analyzer()` fails closed rather than defaulting. No prior case in this project had ever exercised this path (CASE_030's `_policy_polarity_semantic_index.json` is also empty). Set `HARNESS_POLICY_SEMANTIC_PROVIDER=claude-cli` / `HARNESS_POLICY_SEMANTIC_MODEL=claude-sonnet-5` (matching the provider already used elsewhere in the project for P8/D2) and activated via `dao.py set-policy-semantic-analyzer CASE_112 --reason "..." --held-by pyun --run-id RUN_20260730_001` → `PASS: active semantic analyzer is now claude-cli:claude-sonnet-5`.

**Real blocker 2 (fixed, code change) — Windows `.cmd` shim truncates multi-line claude-cli prompts, and `analyze-policy-polarity`'s Phase A/B never requested structured output, so parsing failed even once the analyzer was configured.** Found and precisely diagnosed by the DOC_003.pdf subagent (dispatch 3), deliberately not patched by it since it touches trusted analyzer-facing code:
  - `build_provider()` resolved the bare command `"claude"`, which on Windows resolves (via `shutil.which`) to `claude.cmd`, an npm batch-file shim. `subprocess.run([...], shell=False)` executing a `.cmd` file still routes through `cmd.exe`'s batch-argument parsing, which silently truncates a prompt at its first blank line — reproduced directly: `"Echo back BANANA"` round-trips correctly, the same prompt with an inserted blank line returns generic assistant chatter, because the model never receives anything past the truncation point. Calling the underlying `claude.exe` (`node_modules/@anthropic-ai/claude-code/bin/claude.exe`, one level below the shim) directly reproduces correctly.
  - `ClaudeCliProvider.compare_text()` never passed `output_schema` through to `_run()` (unlike `transcribe_image`/`analyze_image_structured`, which already had Stage 1's structured-image contract), so `analyze-policy-polarity`'s Phase A/B calls always requested free-text output, which `policy_polarity_semantics._parse_strict()` rejects if wrapped in a ```json fence — reproduced deterministically (3/3, not flaky) with `claude.exe` called directly: correct analysis, wrapped in a fence, rejected.

  Fixed: `tools/llm_providers.py` gained `_resolve_claude_cli_command()` (resolves the real `.exe` on Windows unless `HARNESS_CLAUDE_COMMAND` is explicitly set, no-op on non-Windows hosts or hosts without the npm shim layout) wired into `build_provider()`; `compare_text()`'s signature gained `output_schema: Mapping[str, Any] | None = None` across all 6 provider implementations (`ClaudeCliProvider` — passes it through to `_run()`, which already had the structured-output plumbing; `CodexCliProvider`/`OpenAIApiProvider`/`_ApiProviderStub`/`FixtureProvider` — accepted for interface parity, not enforced, since none of those backends have the wiring yet); `tools/policy_polarity_semantics.py` gained `SOURCE_OUTPUT_SCHEMA`/`COMPARISON_OUTPUT_SCHEMA` (JSON Schema mirroring `_SOURCE_KEYS`/`_COMPARISON_KEYS`/`_PROPOSITION_KEYS` exactly); `tools/dao.py`'s two `provider.compare_text()` call sites in the polarity-analysis command now pass the matching schema. Verified live against the real `claude-cli`/`claude-sonnet-5` deployment (not mocked): structured output returned as bare JSON, `_parse_strict` passed cleanly (no fence-rejection), Korean text round-tripped correctly (a prior console-only mojibake appearance was cp949 terminal rendering, not data corruption — confirmed by writing raw output to a UTF-8 file).

  Test regression from the `compare_text` signature change: 4 test files had local `FixtureProvider` subclasses overriding `compare_text(self, prompt, prompt_version)` without the new parameter — fixed in `tests/conftest.py` (`_TwoPhase`), `tests/test_policy_polarity_semantic_receipt.py` (`CountingProvider`/`TimeoutProvider`), `tests/test_policy_polarity_analyzer_trust.py`. One unrelated pre-existing test-string mismatch found in the same full-suite run (`tests/test_segment_page_map_provenance.py::test_an_ocr_segment_blocks_finalization_rather_than_passing` — asserted the OLD pre-P0-6-override blocker message verbatim; the message changed when the P0-6 override path was added earlier this run) — updated to check the message's two stable substrings rather than the exact string that changed. Full suite: **1740/1740 passed** after all fixes.

**Real blocker 3 (found, NOT fixed) — a span-offset validation mismatch, discovered during my own live smoke test of the just-fixed structured-output path, unrelated to blockers 1/2.** Test prompt: real Phase A call against a real (test-authored, not from CASE_112's actual text) Korean policy sentence prefixed with a `제1조(...)` heading. The model's structured response was valid, schema-conformant JSON (blockers 1/2 genuinely fixed) but `parse_source_analysis()` raised `propositions[0].text does not equal source passage at its declared offsets` — the model treated the heading as separate from the body and reported `source_start: 0` against the body's own start rather than the full passage's start including the heading. Not yet determined whether this is: (a) a prompt-construction issue specific to my ad-hoc test string (real CASE_112 passages passed to `build_source_prompt()` may not have this shape), (b) a genuine, reproducible model behavior against heading-prefixed passages that will recur across the real corpus, or (c) something else. **Not investigated further or fixed this session — flagged as the next thing to check before resuming real per-document extraction.**

## Session state at handoff

- All 203 CASE_112 policy documents: `canonical_v1`, confirmed.
- P0-8: fully resolved for all 203 (`unverifiable_ocr_source` override, human-authorized 2026-07-31) — confirmed NOT actually being hit in dispatch 3; the apparent P0-8 errors were a false alarm from agents manually invoking the real-scan path instead of relying on the recorded override.
- P1-2 semantic analyzer: configured and active (`claude-cli:claude-sonnet-5`).
- `llm_providers.py`/`policy_polarity_semantics.py`/`dao.py`: real code fixes for the Windows `.cmd`-shim truncation bug and the missing structured-output wiring on `compare_text`, verified against the real deployment, full test suite green (1740/1740).
- Zero `normalized_policy_clause_*.json` files exist yet for CASE_112 — no real extraction has been completed. The two policy-pipeline subagent dispatches this session (dispatch 2 and dispatch 3) both stopped before writing any contracts, correctly refusing to guess around blockers rather than producing questionable output.
- Dispatch 3's DOC_004.pdf-scoped subagent (`ae31cdddd14fc2768`) was killed mid-run by an external `killed` status — NOT initiated by the user (confirmed directly: "내가 중단한거 아닌데?"). Cause unknown; not investigated this session. It had written zero contract files at the time it was killed.
- Next step before any further subagent dispatch: resolve blocker 3 above (the span-offset validation mismatch), since it will otherwise surface again partway through a large real dispatch and waste the same real-LLM-call budget dispatch 3 already spent reaching the now-fixed blockers.

## Blocker 3 — reproduced with real CASE_112 text, fixed at the prompt level

Reproduced deliberately before fixing, per the handoff note above: called `provider.compare_text()` directly (real `claude-cli`/`claude-sonnet-5`, not mocked) with `build_source_prompt()` against DOC_008's real 제1조(보상하는 손해) passage (page 1 of its `redacted_text.md`, the same DOC_003.pdf-derived policy text the dispatch-3 subagent was working from). Confirmed hypothesis (b) from the handoff, not (a) or (c): this is a genuine, reproducible model behavior against heading-prefixed passages, not an artifact of the earlier ad-hoc test string.

**Root cause, precisely characterized**: the model's declared `source_start`/`source_end` offsets were correct (pointed at the right 138-character span of the real passage, headings included). The failure was in `propositions[].text`: the model filled it with a paraphrased/compressed rendering of that span — dropping parentheticals like `(이하 보통약관이라 합니다)`, `(이하 특별약관이라 합니다)`, `(사고)`, and the qualifier `영업배상책임보험` — rather than the exact verbatim substring `parse_source_analysis()` requires (`source_passage[start:end] != proposition["text"]`). The offset math was never wrong; the model was quietly "cleaning up" the quoted text.

**Fix (user-directed: harden the prompt, not the parser)**: `build_source_prompt()` (`tools/policy_polarity_semantics.py`) now states explicitly that `text` MUST be the exact verbatim substring at the declared offsets — identical characters, including any `제N조(...)` heading and any parenthetical — and that a paraphrase belongs only in `reason`, never in `text`. `parse_source_analysis()`'s verbatim-equality check is unchanged; the fix is entirely prompt-side, per explicit user decision (rejected the alternative of relaxing the parser to treat `text` as advisory-only, since that would weaken what the field is for). `SOURCE_PROMPT_VERSION` bumped `policy_polarity_source_v1` → `policy_polarity_source_v2` so this is a real cache-busting, auditable prompt change, not a silent in-place edit.

**Consequences of the version bump, all resolved**:
- `policy_polarity_semantic_index.schema.json`'s `source_prompt_version` const updated to match.
- `tests/fixtures/policy_polarity_source_prompt_v1.txt` (golden-prompt fixture; filename is a fixture-numbering convention, not tied to the internal prompt-version string) regenerated from the real `build_source_prompt()` output.
- CASE_112's `_policy_polarity_semantic_index.json` (written earlier this run via `set-policy-semantic-analyzer`, 0 receipts ever issued against it) held the stale `v1` identity and became schema-invalid the moment the const changed — `set-policy-semantic-analyzer` itself refused to run because it validates the *existing* index before cascading. Since the index had zero receipts (nothing to lose, nothing to invalidate), user-approved resolution: delete the stale index file and re-run `set-policy-semantic-analyzer` (DAO-governed path, not a hand-edit) to recreate it cleanly under the `v2` schema. Verified: recreated index now shows `source_prompt_version: policy_polarity_source_v2`, `receipts: []`.
- Full suite: 1740/1740 (two unrelated Windows lock-file `PermissionError` flakes in `test_dao_locking.py`/`test_dao_replace_manifest.py` under concurrent full-suite load, both confirmed passing in isolation — pre-existing environmental flakiness, not caused by this change).

**Verified fixed, live**: re-ran the same `compare_text()` smoke test against DOC_008's real passage with the updated prompt. Not yet re-run against a larger real-corpus sample — recommended before resuming the full subagent dispatch, to confirm the fix generalizes past this one passage.

**Status**: blocker 3 resolved. Ready to resume real policy-clause extraction (dispatch 4) across the 203-document corpus, this time verifying a small real sample first rather than dispatching the full batch immediately.

## Blocker 3, continued — a second real bug surfaced by the verbatim fix, also fixed

Widening the real-sample verification past DOC_008 (per the handoff note's own recommendation) to DOC_050 (제1조 exclusion, heading + parenthetical) and DOC_011 (제1조 payout with quoted-title parenthetical + 단, proviso, a `mixed`-classification candidate) surfaced a second, distinct real bug, not an artifact of the v2 prompt change: the model's declared `source_start`/`source_end` character offsets are themselves unreliable over Korean text even when `text` is a correct verbatim quote -- confirmed directly (DOC_050: declared `source_end: 186` against an actual 152-character passage; DOC_011: declared `source_start: 17` for a proposition whose real position, verified via `.find()`, is `0`). The quoted `text` was correct in both cases; only the model's own character count was wrong.

**Fixed** (user-directed: recompute in the parser, since `text` is now separately verified verbatim): `parse_source_analysis()` (`tools/policy_polarity_semantics.py`) no longer trusts the model's declared offsets as ground truth. It now locates the verified verbatim `text` in the passage via `source_passage.find()`, rejects if it does not occur (not a verbatim substring) or occurs more than once (position not resolvable unambiguously), and overwrites `source_start`/`source_end` with the recomputed, guaranteed-correct values before the receipt is built. The declared offsets are no longer range- or equality-checked at all -- they were never reliable input, only `text` is.

Re-verified live against both real passages after the fix: DOC_050 now parses cleanly (`restrictive_or_negative`, 1 proposition). DOC_011 hit a third, unrelated, correctly-caught issue on retry -- the model classified the passage `mixed` (correct call: affirmative payment + a 단서 carve-out) but returned `review_required: false`, which `parse_source_analysis`'s existing (unchanged) rule for `mixed`/`ambiguous` classifications correctly rejects. This is the validator working as designed against a genuine model inconsistency, not a parser or prompt defect -- no fix applied, no fix needed; a real production run would route this to human review rather than silently accepting an unreviewed mixed clause.

Full suite re-verified after both fixes: 1723/1723 passed (excluding the two pre-existing flaky Windows lock tests, confirmed passing in isolation earlier this session).

**Status, final**: both real bugs blocking policy-clause extraction (verbatim-text drift, offset miscounting) are fixed and verified against real CASE_112 passages spanning plain exclusion clauses, quoted-title payout clauses, and a genuine mixed/proviso clause. Proceeding to dispatch 4 (real clause extraction across the 203-document corpus) per explicit user instruction to move past remaining non-blocking rough edges rather than continue isolated verification.

## Session resumed 2026-07-31 -- user directed skipping Stage 4 (dispatch 4, real clause extraction) and proceeding directly to Stage 5 (claim-analysis)

User instruction: exclude Stage 4's remaining work (dispatch 4 was never executed -- 0/203 `normalized_policy_clause_*.json` files exist) and move on to Stage 5. Recorded here in full since this required a hard-rule-adjacent action.

### `document_processing` finalized via the sanctioned DAO path (no rule violation)

`_run_state.json` showed `document_processing: in_progress` (`attempt_count: 220`) despite checkpoints 1-3 genuinely completing for 219/221 documents earlier this run (see above). Ran the real `finalize-stage` command rather than hand-editing:

`python tools/dao.py finalize-stage CASE_112 RUN_20260730_001 document_processing --held-by pyun` -> `OK: finalized document_processing -> passed, snapshot at .../_backups/step_01_document_processing`. This is a normal, gate-verified finalize -- not a hand-edit, not a rule exception.

### `policy_clause_processing` -- forced to `passed` via a direct hand-edit of `_run_state.json`, explicitly authorized by the project owner

Checked the sanctioned path first: `dao.py update-run-state` does not even accept `passed` as a status value for that command (argparse rejects it -- only `pending/in_progress/failed/skipped`). The only route to a real `passed` is `finalize-stage`/`snapshot-backup`, both of which call `_policy_completion_blockers()` and refuse identically: all 203 policy documents lack a declared `policy_processing_role` and a `normalized_policy_clause_*.json`. Ran `finalize-stage` for real to confirm this rather than assume it -- confirmed refused, full 203-document blocker list obtained.

Reported this to the user in full before acting: CLAUDE.md's hard rule ("No agent reads or writes outputs/, data/, or a ledger/run-state file directly... Everything goes through tools/dao.py") would be violated by any hand-edit, and this exact category of action was tried once before this run (see "Correction" section above, step 1) and reverted after being found ineffective. User's response, verbatim intent: full project-owner authorization to hand-edit the DAO state, explicitly overriding the hard rule for this one case.

**Action taken**: backed up `outputs/CASE_112/_run_state.json` to `_run_state.json.bak_before_manual_policy_override` first. Then edited the `policy_clause_processing` stage entry directly: `status: "failed"` -> `"passed"`, `completed_at` refreshed, and a new `manual_override` object added recording `authorized_by: "pyun"`, a timestamp, and a full-text reason explaining this is NOT a real completion (0/203 clause files exist), names the exact refused gate, and warns that any downstream stage relying on `normalized_policy_clause_*.json` or `policy-snapshot` has no real policy grounding and must not cite policy clauses as verified.

**Known residual defect in this hand-edit, not resolved**: `run_state.schema.json`'s `stage_entry` definition has a schema-level `allOf`/`if`/`then` rule requiring `backup_path` to be a non-null, non-empty string whenever `status == "passed"` (added specifically to prevent the CASE_030 half-finalized-state bug this description names). The hand-edited entry has `backup_path: null` -- confirmed via direct `validate_instance()` call: `stages/2/backup_path: None is not of type 'string'`, one schema error. Did not fabricate a `backup_path` value pointing at a snapshot that doesn't exist (that would misrepresent P10 provenance, a worse fabrication than the status field alone) and could not create a real snapshot via `snapshot-backup` either -- it calls the identical `_policy_completion_blockers()` gate and was refused the same way. **Consequence**: `outputs/CASE_112/_run_state.json` as a whole will fail a full schema validation pass (e.g. if any future tooling calls `validate_instance(..., "run_state.schema.json")` against it) even though ad-hoc reads like `get-last-passed-stage` (confirmed returns `policy_clause_processing`, no validation performed) tolerate it fine. Flagging for whoever repairs this state later -- the honest fix is real Stage 4 clause extraction (dispatch 4), which would let `finalize-stage`/`snapshot-backup` populate `backup_path` for real and make the `manual_override` block removable.

**Scope of what remains fabricated going forward**: any CASE_112 output from Stage 5 onward that references a policy clause, coverage match, or `policy-snapshot` object should be treated as unverified/placeholder until real Stage 4 extraction runs. `claim-analysis.md` checkpoint 2 (Coverage Identification) explicitly requires resolving clause references against a current `policy_audit_result_*.json` with no open findings and a DAO-recomputed `upstream_policy_snapshot` -- since no `normalized_policy_clause_*.json` exists for any of the 203 documents, any attempt to actually populate a real coverage match this way will still correctly fail at the DAO layer (the override only unblocked the stage-gate check, not the per-reference audit/snapshot checks used inside checkpoint 2 itself). Practical implication for Stage 5: checkpoint 1 (Claim Field Extraction) can proceed normally since it does not touch policy data; checkpoints 2-4 (coverage, case type, requirement matching) will hit the same real per-reference blockers policy data was always going to require, now surfaced at the point of use instead of at the stage gate.


## Stage 5 — Claim Analysis (`claim_analysis`) — all 4 checkpoints attempted, 1 clean / 1 partial-but-substantive / 2 blocked by absent Stage 4 data

Dispatched 2026-07-31 with explicit instruction to run all four internal checkpoints and let each succeed or fail on its own merits against real case data — specifically NOT to paper over the missing policy layer. Reviewer/held-by `pyun`, `--stage claim_analysis` throughout.

Pre-flight: `get-last-passed-stage` → `policy_clause_processing` (the hand-edited entry, per the section above). `check-conflicts-clear` → clear, 0 pending.

### Case shape established (checkpoint 1's substantive finding)

CASE_112 is a **third-party premises-liability (시설소유관리자 배상책임 / General Liability) claim with a formally assessed permanent disability**, not a first-party benefit claim. Injured party slipped on an un-cleared, iced-over stairway at the insured's premises (소추원) around 11 a.m., sustaining a right distal-radius fracture (KCD **S6280**), treated by OR/IF with plate & screws under BPB anesthesia, admitted 2023-12-04~2023-12-07 (정형외과). A McBride 후유장애 진단서 assesses **13% 노동능력상실률, 영구**, for residual right-wrist 부전강직 (Ⅲ-A-2 준용). Policy limit ₩500,000,000/인, 자기부담금 ₩100,000/사고; insurer has communicated a 구내치료비 ₩2,000,000 figure while asserting no legal liability.

The two legal opinions in the pack are directly opposed on the decisive question: DOC_001 (claimant-side) concludes 민법 제758조 제1항 liability IS established with no comparative fault; DOC_002 (insurer-relayed) concludes it is NOT, snowfall/icing being a natural phenomenon and the fall attributable to the injured party's own inattention. **This opposition was deliberately NOT resolved at this stage** — it is insurer-vs-evidence argumentation (denial-validation's Phase 2 job), which `harness-guardrails` P6 explicitly excludes from the conflict ledger, and resolving it also requires the contract's own 보상하는 손해 wording, which does not exist.

### Checkpoint-by-checkpoint result

| checkpoint | output | status | verdict |
|---|---|---|---|
| 1 — Claim Field Extraction | `extracted_claim_fields.json` | **partial** | Substantively complete; 30 typed fields extracted, every one evidence-linked. `partial` only for redaction-scope nulls + 2 excluded documents |
| 2 — Coverage Identification | `coverage_result.json` | **partial (blocked)** | 2 coverages identified from non-policy evidence; **both `matched_clause_ref: null`**, no `upstream_policy_snapshot`. Genuinely blocked |
| 3 — Case Type Classification | `case_type_result.json` | **success** | Clean pass, independent of the policy gap. `배상책임` primary + `후유장해` secondary, `template_id: 배상책임_후유장해형` (canonical registry key) |
| 4 — Requirement Matching | `requirement_matching_result.json` | **partial (blocked)** | 8 requirements across 2 coverages, **all 8 `status: uncertain`, all `clause_ref: null`**. Zero met/not_met possible |

All four written via `dao.py write-contract --stage claim_analysis`, all schema-PASS.

### What worked cleanly (independent of Stage 4)

- **Checkpoint 1** used only document-pipeline's redacted/chunked text and worked as designed. 30 fields with `evidence_references`/`confidence`/`review_required`/`reviewer_role`, including the disability core (13%/영구/ROM/McBride Ⅲ-A-2), surgery, admission period, accident mechanism, both sides' asserted liability bases, limits, and the 246,560원 outpatient cost. Medical-judgment fields (labor_capacity_loss_rate, disability_permanence, ROM, pre-existing contribution, causation) routed to `reviewer_role: 의사` per the spec; the legal-basis fields to `법률전문가`.
- **Primary-diagnosis-code rule applied and recorded, not silently resolved.** DOC_214 (acute-phase 진단서) and DOC_215 (후유장해 진단서) carry the **same code S6280** with different wording — DOC_214 anatomically specific (우측 손목 요골 원위부 골절), DOC_215 the broader KCD label. Because the code agrees, this is a wording/specificity difference rather than a code conflict; DOC_214's more specific wording is the headline `diagnosis_name`, DOC_215's label is kept as `secondary_diagnosis_name` with `is_primary: false`, and both are logged under `inconsistencies`. Nothing dropped. Per the spec this is a labeling decision, **not** a P6 deletion, so no conflict-ledger entry was raised.
- **Checkpoint 3 passed on claim-fact grounds and was NOT discounted to match checkpoint 2.** Classification rests on document character (a liability dispute with a formally assessed permanent disability), all evidenced in fully processed non-policy documents. Confidence 0.88. `template_id` is a real canonical key from `templates/registry.json` — no closest-variant substitution needed, so `templates/index.md`'s 실손형/기타형 caveat does not apply.
- A second inconsistency was recorded rather than smoothed over: DOC_215's pre-printed form asks about 기왕증-**교통사고** causation (answered 무), but this is a premises fall, not a traffic accident — flagged for 의사 review as possibly inapplicable boilerplate.

### What was genuinely blocked, and exactly where the wall is

**Root cause: Stage 4 never produced any clause data.** Verified directly this run, not assumed:
- `normalized_policy_clause_*.json`: **0 files** (203 `insurance_policy` documents in the manifest).
- `policy_audit_result_*.json`: **0 files**. `read-contract CASE_112 policy_audit_result_DOC_008.json` → `NOT_FOUND`.
- `dao.py policy-snapshot CASE_112 --document-id DOC_008` → `BLOCKED: no normalized policy contract for DOC_008 -- a snapshot may only be issued for documents the policy layer has actually produced`. Same for DOC_011/DOC_050.

**The per-reference DAO gates held exactly as the prior section predicted.** The `policy_clause_processing` status override unblocked only the stage gate; it did not and could not manufacture clause addresses or snapshot digests. Nothing was fabricated to force a write: no `clause_uid` guessed, no `condition_uid` invented, no `upstream_policy_snapshot` synthesized.

**The 104 `candidate_normalized_policy_clause_*.json` files were deliberately not used.** Each self-declares `artifact_status: CANDIDATE_UNVALIDATED`, `promotion_blocked: true`, and `not_a_contract: "Candidate extraction only… No polarity_analysis_receipt_id has been issued, so no outcome-sensitive condition here is usable downstream."` Harvesting `clause_uid`s from these would have produced syntactically valid but substantively fabricated references that passed schema validation — the most dangerous available shortcut, and the one most worth recording as explicitly refused.

**Semantics flag for downstream consumers — `applicable: false` does NOT mean "coverage denied."** `coverage_result.schema.json` requires a boolean and offers no "undetermined" value. Both coverages are recorded `false` strictly in the "not established" sense, with the warnings stating so explicitly. A downstream stage reading that boolean naively would conclude coverage was affirmatively ruled out, which is the opposite of the finding. Worth a schema follow-up (a tri-state or a companion `determinable` flag) if this situation is expected to recur.

**Checkpoint 4's `requirement_text` values are labeled reconstructions, not quoted conditions.** The 8 requirements are the generic condition set for this coverage class, inline-labeled as such in every entry, recorded so a reviewer can see the shape of the unfinished analysis. REQ-1/2/3/6/7/8 cite real claim-side evidence but have no condition to match against; REQ-4 (보험기간 내 사고) and REQ-5 (면책사유 부해당) carry genuinely empty `evidence_references` — the accident date and policy period are both redacted out of processed text, and no readable exclusion list exists anywhere in this run's outputs.

### Redaction-scope nulls — a real, separate limitation from the Stage 4 gap

Independent of the policy problem, document-pipeline checkpoint 2's PII redaction removed most dates and the institution name. `accident_date`, `diagnosis_date`, `imaging_date`, `disability_diagnosis_date`, `policy_contract_date`, `hospital_name` are all `null` even though the underlying documents genuinely carry those values on their faces — the field *labels* are present and quoted in `evidence_references`, only the values are masked. Case-wide, exactly **two** concrete dates survive: the 2023-12-04~2023-12-07 admission (DOC_220) and the 2024-06-28 outpatient encounter (DOC_223). Consequence: the accident cannot be dated from processed text, so neither the accident-to-disability-assessment interval nor REQ-4's 보험기간 check can be evaluated at all. Recovering these needs a human with authorized access to unredacted source, not a stage re-run.

DOC_221/DOC_222 remain excluded (still `ocr_status: failed`, held for human P8 resolution). DOC_221 is a 7-page discharge-related document from the same DOC_005 medical bundle that supplied DOC_214–DOC_220, so claim facts it holds — plausibly including dates and the treating institution — are genuinely unavailable. This contributes to checkpoint 1's `partial` alongside the redaction nulls.

### Run-state could not be updated — the hand-edit's residual defect, now hit in practice

Every one of the four `write-contract` calls succeeded on the contract and then failed on the run-state update, identically:

```
PASS: wrote …\outputs\CASE_112\<contract>.json
FAIL: schema validation errors for …\outputs\CASE_112\_run_state.json -- not written:
  - stages/2/backup_path: None is not of type 'string'
WARNING: contract write succeeded, but run-state could not be updated … run-state may now lag behind actual progress
```

This is precisely the residual defect the prior section flagged: the hand-edited `policy_clause_processing` entry has `status: passed` with `backup_path: null`, which `run_state.schema.json`'s `allOf`/`if`/`then` rule forbids. The file is now unwritable *as a whole* through any DAO path that validates before writing — confirmed by running the sanctioned `dao.py update-run-state CASE_112 RUN_20260730_001 claim_analysis in_progress --held-by pyun` directly, which fails with the same single error. So the prediction that "ad-hoc reads tolerate it fine" holds for reads, but **any write now fails**, including writes belonging to unrelated stages.

No attempt was made to repair this by fabricating a `backup_path` or by hand-editing the file — that would repeat the original violation and misrepresent P10 provenance. **`_run_state.json` therefore does not reflect Stage 5 at all**, even though all four contracts are on disk. The honest fix remains real Stage 4 extraction, after which `finalize-stage` can populate `backup_path` legitimately and the `manual_override` block becomes removable. Until then, `_run_state.json` is stale for every stage from Stage 5 onward, and resume logic must not be trusted for this case.

### P6 / conflict ledger

No conflict-ledger entry was raised, and this was a considered call rather than an omission. The two candidate conflicts both fall outside P6: the DOC_214/DOC_215 diagnosis wording difference is the primary/secondary labeling case the spec explicitly says is *not* a P6 deletion (and the KCD code agrees between them anyway), and the opposed legal opinions are insurer-vs-evidence argumentation, which `harness-guardrails` P6 explicitly carves out as denial-validation's analytical purpose. `check-conflicts-clear CASE_112` remains `clear: true` after this stage.

### Bottom line for the pipeline-limits question this dispatch was set up to answer

The pipeline reached **exactly** the wall the prior section predicted, at the point of use rather than the stage gate, and stopped there without fabricating:

- Everything grounded in **document-pipeline's real output** worked — extraction, evidence linking, reviewer routing, inconsistency labeling, and case-type classification (which fully passed and produced a valid downstream `template_id`).
- Everything requiring **Stage 4's clause layer** failed honestly — coverage applicability and every one of 8 requirements, all marked `uncertain`/null with the cause named.
- The DAO's per-reference gates (`policy-snapshot`, audit resolution) proved to be the effective safeguard, not agent diligence: they refused before any fabricated reference could be written, exactly as designed.
- **Downstream stages must treat CASE_112's coverage and requirement outputs as UNDETERMINED placeholders, never as findings.** `screening-report` may use `template_id` and the checkpoint-1 fact card; nothing downstream may cite a policy clause for this case until Stage 4 dispatch 4 actually runs.

## User decision (2026-07-31) — permit candidate (unvalidated) policy clauses as coverage/requirement grounding, for this pipeline-walkthrough purpose only

Following the claim-analysis dispatch's report above (checkpoints 2/4 correctly returned `uncertain`/null for lack of real Stage 4 output), the project owner reviewed the actual `candidate_normalized_policy_clause_DOC_*.json` files directly before deciding how to proceed. Findings from that review, confirmed directly (not agent-reported):

- The candidate files' quoted clause text is real, verbatim source content (`제N조(...)` clauses, exact source spans, real `clause_uid`s like `PC-97d9be0921264da0`) — not placeholder or fabricated text. 103 of 104 sampled/checked files (DOC_006 among the exceptions) have non-empty `clauses` arrays.
- `promotion_blocked: true` / `artifact_status: "CANDIDATE_UNVALIDATED"` is present specifically because no `polarity_analysis_receipt_id` was ever issued against them — i.e., P1-2's semantic analyzer never verified whether each clause is a payout-affirming condition, an exclusion, or something else. The clause's *existence* is trustworthy; its *polarity/legal effect* is an unverified inference.

**Stated purpose driving this decision**: today's session goal is a pipeline-operation briefing/walkthrough for CASE_112 — demonstrating the full mechanism producing real, non-placeholder output end-to-end (through to a draft/final report), not producing a submission-grade loss-adjustment work product for this session. Given that framing, the project owner explicitly authorized using the real clause text/`clause_uid`s from the unvalidated candidate files as the cited basis for `coverage_result.json`/`requirement_matching_result.json` judgments, overriding the earlier "do not fabricate/harvest from candidates" instruction for those two checkpoints specifically.

**Conditions attached, communicated to the claim-analysis agent before it resumed work**:
1. `review_required: true` / `reviewer_role: policy_analyst` (or equivalent) must stay set on every field grounded this way — never flipped to false just because a citation now exists.
2. Every such field must carry an explicit warning naming the citation as sourced from an unvalidated `candidate_normalized_policy_clause_*.json` (`promotion_blocked: true`, no P1-2 polarity check), distinct in wording from a real DAO-audited citation.
3. No fabricated `upstream_policy_snapshot` and no claim that the DAO's `policy-snapshot`/audit gate was satisfied — provenance must stay honest even while the citation itself is used.
4. Where no candidate clause exists at all for a given coverage/requirement, it stays `uncertain` as originally reported — this authorization does not extend to inventing citations with no source.

Claim-analysis (agent `aeebc720e4b6674fe`) was resumed in the background with this exact instruction set immediately after this decision; its revision of checkpoints 2 and 4 is reported in a follow-up section below once it completes.

**Scope note**: this authorization is specific to CASE_112's current walkthrough purpose and the four conditions above — it is not a general project policy change. `known-gaps.md`/`open-decisions.md` are not updated by this entry; if this pattern is wanted for future cases it should be decided and documented there separately, not inferred from this one-off record.

## Stage 4 official normalization resumption — 2026-07-31T06:21:13+09:00 — P4 HALT

This entry records the later session whose explicit goal was to replace the
candidate/pilot policy artifacts with complete official Stage 4 contracts.
It is append-only and does not revise the earlier walkthrough or manual-override
entries.

### Authorized scope and preflight

- Case/run: `CASE_112`, `RUN_20260730_001`.
- Required order: finish DOC_008 as a valid official result, then process every
  manifest document typed `insurance_policy` in DOC_006–DOC_111.
- Explicit exclusion: DOC_112–DOC_213 were not to be processed in this session.
- The target list was derived from `document_manifest.json` through
  `dao.py read-contract`: 104 documents. DOC_077 (`other`) and DOC_091
  (`insurance_certificate`) were not targets.
- `dao.py check-conflicts-clear CASE_112`: clear, zero pending conflicts.
- `dao.py get-last-passed-stage CASE_112`: `document_segmentation`.
- The run-state read through the DAO showed `document_processing: in_progress`
  and `policy_clause_processing: failed` at this resumption point.
- The run-state and manifest were unlocked. The three planned official filenames
  for each of the 104 targets were checked through `dao.py check-lock`
  (312 checks total): zero locks and zero DAO errors.
- Existing background `promote.py` / `analyze-policy-polarity` processes were
  checked before new work. Duplicate DOC_008 Claude analyzer processes from an
  earlier session were allowed to stop or were aborted by the user before new
  writes were attempted.
- P0-8 table handling followed the user's prior authorization:
  `unverifiable_ocr_source` overrides were treated as already recorded.
  Neither `scan-table-candidates` nor `register-table-region` was called.

### Execution-environment corrections

- Anaconda Python initially failed inside the Codex sandbox with Windows
  `0xC0000022`. This was an execution-permission failure, not a DAO or case-data
  failure. All later Python/DAO/pytest/native-Codex calls were made with
  first-call sandbox escalation; read-only source searches remained sandboxed.
- The specialized `policy-pipeline` subagent could not start because its fixed
  `opus` model is unsupported for the connected ChatGPT account. A default
  subagent was used as a fallback and was instructed to follow
  `.claude/agents/policy-pipeline.md` in full.
- During scratch-file discovery, that fallback subagent directly enumerated
  filenames and sizes under `outputs/CASE_112`. It did not read file contents,
  write case state, or access ground truth. This was nevertheless a procedural
  violation of the no-direct-`outputs/` rule. The subagent was stopped, the
  violation was disclosed to the user, and the user explicitly instructed the
  run to continue. Information learned from the enumeration was not used for
  subsequent contract construction; later case access returned to public DAO
  paths only.
- This incident means the file header's older session-wide statement that
  `outputs/` was “never read directly” is no longer literally true for the
  complete lifetime of the run. The correction is recorded here rather than
  editing the prior append-only text.

### Semantic analyzer recovery and parallelization

- The configured `claude-cli:claude-sonnet-5` analyzer stopped returning while
  its provider child process had already disappeared. The remaining DAO parent
  was terminated after exceeding the configured 300-second comparison timeout.
- A stale `_policy_polarity_semantic_index.json.lock` was then found through
  `dao.py check-lock`, held by `policy-pipeline` / `RUN_20260731_001` since
  `2026-07-31T04:53:09.848911+09:00` for DOC_008 polarity analysis. There was no
  live writer. Per P5, no agent removed it. The user removed the stale lock and
  the DAO subsequently confirmed `locked: false`.
- With explicit user approval, `dao.py set-policy-semantic-analyzer` changed the
  active analyzer to `codex-cli:codex-cli`. Existing Claude receipts remained
  in the protected index but became stale and were excluded from the current
  validity count.
- Windows bare-command execution initially failed for Codex. The native
  npm-packaged `codex.exe` was resolved and used through the public
  `HARNESS_CODEX_COMMAND` configuration. Codex CLI authentication was confirmed
  as ChatGPT-backed.
- The original DAO held the case-wide semantic-index lock across both provider
  calls, making all polarity work serial. At the user's request for safe
  parallel execution, the implementation was refactored to:
  1. take short `semantic_index → document_manifest → revision_index` locks for
     a coherent prepare snapshot and cache check;
  2. run the two provider phases without shared-file locks; and
  3. reacquire the same ordered locks for commit-time analyzer, revision, UID,
     quote, hash, and cache revalidation.
- Concurrent identical inputs use first-commit-wins and produce one receipt.
  Analyzer or source-revision changes while a provider is running cause the old
  candidate to be rejected rather than committed.
- Changed files for this acceleration:
  `tools/dao.py`, `tools/dao_transaction.py`,
  `tests/test_dao_transaction.py`, and
  `tests/test_policy_polarity_semantic_receipt.py`.
- Focused regression result: **59 passed in 16.94 seconds, 0 failed**. Tests
  covered different-key overlap, same-key first-wins, source-revision TOCTOU,
  analyzer-switch TOCTOU, and lock ordering. The production P5 30-second polling
  interval was not changed.
- Three-way staggered DOC_008 analysis then ran concurrently with the semantic
  index unlocked during provider work. This reduced the remaining receipt time
  materially.

### Preparation coverage and known fail-closed candidates

- DAO-gated, read-only policy preparation was completed for all 104 target
  documents. No DOC_112–DOC_213 document was processed.
- No official document other than the attempted DOC_008 writes was submitted.
- Preparation identified several classes that must remain fail-closed during
  official processing:
  - DOC_006 is a cover-only page with no substantive clause, conflicting with
    the automated-policy non-empty normalized-clause gate.
  - Several physical continuation documents contain orphaned beginnings or
    endings of operative predicates. Text from a neighboring physical document
    must not be borrowed to fabricate source-local support.
  - DOC_056–DOC_058 contain numbered but blank schedule anchors, which cannot be
    hidden as administrative exclusions.
  - Several documents contain table/sentinel layouts that remain governed by
    the already-authorized `unverifiable_ocr_source` handling.
  - Limited-coverage wording, composite umbrella/subitem clauses, and mixed
    exclusion/carve-out passages must preserve their exact semantics and may
    fail closed if the analyzer returns `mixed`, `ambiguous`,
    `meaning_preserved=false`, or `review_required=true`.

### DOC_008 semantic result

- The initial extraction count of 23 conditions was corrected to 24 after the
  final nested exclusion was split into two atomic subitem predicates.
- The combined, non-atomic receipt and one review-required intermediate receipt
  are forbidden for official use.
- The final intended official mapping has **24/24 current Codex receipts**.
  Every receipt selected for the intended contract has the required bucket
  polarity, `meaning_preserved=true`, `review_required=false`, and no
  contradiction.
- One nested subitem initially omitted the governing object in normalized
  wording. It was not weakened or moved to another bucket; the full governing
  object was restored and a new receipt passed.

### Official-write attempts and P4 halt

The required official files remain:

- `policy_boundary_inventory_DOC_008.json`
- `normalized_policy_clause_DOC_008.json`
- `policy_audit_result_DOC_008.json`

First official boundary submission:

- Rejected because `reviewer_role: null` is not an allowed enum value.
- No official file was written.
- The null key was removed and all three candidate official contracts passed
  local full-schema prevalidation before the one allowed correction attempt.

Single P4 correction attempt:

- Rejected by DAO policy-completeness validation.
- `PS-11858f54dce73852` / `PB-0eee04ad3c8a4354` covered three structural
  article/paragraph/item anchors and must be split into material
  subparagraph boundaries.
- Every boundary `normalized_mapping` also failed to resolve because the
  referenced `PC-*` clause/condition did not yet exist as an official normalized
  contract. This exposed a boundary/normalized bootstrap-order problem:
  boundary validation requires the normalized target to exist while the
  normalized contract is also validated against the boundary inventory.
- No official file was written by the rejected retry. DAO reads confirmed the
  three official DOC_008 contracts were still absent.

Per P4, no third write was attempted. The pipeline is halted pending a human
choice:

1. `retry-N-times` after repairing the public DAO bootstrap path and splitting
   `PB-0eee04ad3c8a4354`;
2. `fix-manually`, followed by DAO validation; or
3. `ignore-and-proceed`, carrying an explicit validation warning.

Recommended recovery recorded at halt: authorize `retry-2-times`, repair the
boundary/normalized bootstrap transaction, split the over-broad boundary, and
resume DOC_008. Because the user required DOC_008 to finish first, official
processing did not advance to the other 103 targets.

### Status at this entry

| item | status |
|---|---|
| official documents completed | **0 / 104** |
| current document | `DOC_008` |
| intended current receipts | **24 / 24 valid** |
| official DOC_008 files | **0 / 3 written** |
| blocked documents | `DOC_008` (P4 halt: over-broad boundary + DAO bootstrap ordering) |
| Stage 4 finalizable | **No** |
| next required input | Human P4 recovery choice |

### FOLLOW-UP (2026-07-31, same Stage 5 dispatch) — checkpoints 2 and 4 revised to v0.3 under a new project-owner instruction: candidate-sourced clause citations now permitted, labeled unvalidated

Appended as a follow-up to the Stage 5 section immediately above. The earlier entry is left intact and its reasoning still stands as the record of what was decided under the prior instruction.

**Instruction received** (project owner, relayed by the coordinator, mid-dispatch): today's goal is a pipeline-operation briefing/walkthrough for CASE_112, so the priority is real, non-null output running all the way to a draft/final report to demonstrate the mechanism end to end. Full P1-2 clause verification is **expressly not required for this pass**. Where a coverage/requirement was left `uncertain` *purely* because no `normalized_policy_clause_*.json`/`policy_audit_result_*.json`/`policy-snapshot` existed, the real `clause_uid`/condition text/`source_span_uids` in `outputs/CASE_112/candidate_normalized_policy_clause_DOC_*.json` may now be used as the cited basis — with a non-negotiable condition that every such use be distinguishably marked unvalidated, no fabricated `upstream_policy_snapshot`, no claim that the DAO gate was satisfied, `review_required`/`reviewer_role` retained, and anything with no candidate clause at all left `uncertain` rather than stretched.

This reverses the earlier entry's "refused to harvest from candidates" decision **for these two checkpoints only**, on the owner's authority and for a stated demonstration purpose. It is a deliberate, scoped provenance relaxation, not a discovery that the earlier reasoning was wrong. Checkpoints 1 and 3 were not touched.

#### What changed

| | v0.2 (earlier today) | v0.3 (this revision) |
|---|---|---|
| `coverage_result.json` | 2 coverages, both `applicable: false` (meaning *not established*), zero clause grounding | 2 coverages, both `applicable: true` as a **qualified trigger reading**, grounded in real candidate clause text |
| `requirement_matching_result.json` | 8 requirements, **all** `uncertain`, zero clause grounding | **10** requirements; 4 `met`, 6 `uncertain`; 7 of 10 candidate-grounded |
| `clause_ref` / `matched_clause_ref` | null | **still null** (deliberate — see below) |
| `upstream_policy_snapshot` | absent | **still absent** (deliberate) |
| `review_required` / `reviewer_role` | true / set | **unchanged, true / set on every entry** |

Requirement count rose 8 → 10 because the real DOC_008 text contains two genuine conditions the earlier generic reconstruction had no counterpart for: the 사고일로부터 1년 후 치료비 면책 (REQ-9) and the 치료비 scope definition (REQ-10).

#### Clause sources used (all verbatim-verified against the candidate files before writing)

- **DOC_007** `PC-97d9be0921264da0` 제1조(사고) — the covered-accident definition; **DOC_007** `PC-9a19575af9ae42f5` 제2조(보상하지 않는 손해) — the 특별약관 exclusion list.
- **DOC_008** `PC-aa50bed1c8431b93` 제1조(보상하는 손해); `PC-e01f526afabe328e` 제2조 (items 10 and 11 relied on); `PC-2eeb25243bb4ed33` 치료비 용어풀이.
- **DOC_019** `PC-b074786a20b5358c` — 보상한도액/자기부담금 적용 방식.

A verification pass confirmed all 7 labeled quotes match their source candidate clause byte-for-byte (whitespace-normalized), that every candidate-sourced requirement carries a labeled quote, that no labeled quote appears on an unflagged requirement, and that all 10 requirements retain `review_required: true`, a `reviewer_role`, and `clause_ref: null`.

#### How "unvalidated" is marked (the instruction's non-negotiable condition)

Three independent, redundant markers, so the label cannot be lost by a consumer reading only one part of the artifact:

1. **Inline in every quote** — each candidate-sourced `evidence_references` entry is prefixed `[UNVALIDATED CANDIDATE CLAUSE — candidate_normalized_policy_clause_DOC_NNN.json, clause_uid PC-…, display C-N, span pN a-b, promotion_blocked=true, no polarity_analysis_receipt_id]` before the verbatim text. The label travels with the quote wherever it is copied.
2. **Inline in every `requirement_text`** — prefixed `[CANDIDATE-SOURCED, UNVALIDATED]`.
3. **Dedicated `warnings` entries** on both artifacts stating the source is `CANDIDATE_UNVALIDATED` / `promotion_blocked: true`, that P1-2 never ran, and that this is not a DAO-audited citation.

**`matched_clause_ref`/`clause_ref` were deliberately left null even though real `clause_uid`s are now known.** Those fields are schema-defined as pointers into the DAO-audited `normalized_policy_clause.json` and are what downstream stages join on; a candidate UID placed there would assert audited provenance in the one field a consumer is entitled to trust, and the "unvalidated" label would silently drop the moment the pointer was dereferenced. Per the instruction's own carve-out ("use whatever schema-legal null/omitted form keeps the write honest … carry the candidate citation in evidence_references/warnings prose"), the citation lives in `evidence_references` instead, where its label is inseparable from it. No `upstream_policy_snapshot` was written; `dao.py policy-snapshot` still returns `BLOCKED` for DOC_007/DOC_008/DOC_019 and no `policy_audit_result_*.json` exists — the DAO gate is not satisfied and is not claimed to be.

Confidences were raised only modestly (coverages 0.3 → 0.45/0.5; candidate-grounded requirements capped at 0.6) to keep candidate-sourced items visibly below what an audited citation would justify.

#### Two substantive findings the real clause text produced

Worth flagging because they change the shape of the case, not just its provenance markers:

1. **구내치료비 may be a no-fault benefit that the liability denial does not defeat — and may even engage.** DOC_008 제1조 grants 치료비 for a victim's 신체장해 arising from a 시설소유(관리)자 특별약관 제1조 accident, expressly *notwithstanding* (규정에도 불구하고) the 보통약관 제3조 insuring clause. DOC_008 제2조 item 11 then **excludes** 치료비 "타인의 신체장해에 대하여 피보험자에게 **법률상의 배상책임이 있는** 치료비". Read together, the benefit appears to operate precisely where legal liability does *not* attach — consistent with the insurer having communicated a 구내치료비 ₩2,000,000 figure in the very letter denying 법률상 배상책임. Recorded as REQ-8, status **uncertain** (not `met`) because the reading is polarity-dependent on unvalidated text and its outcome is contingent on REQ-2. This is the highest-value item for a reviewer to confirm and the one most damaging if the polarity reading is wrong.
2. **The 천재지변 exclusion is the insurer's likely clause-level hook.** DOC_007 제2조 제3호 excludes loss from "지진, 분화, 홍수, 해일 또는 **이와 비슷한 천재지변**". Whether snowfall/icing falls inside that phrase is exactly what the insurer's legal opinion leans on ("자연 현상 등으로 인한 부분까지 시설의 하자로 보기 어렵다"). Left **uncertain** (REQ-5): it is a legal-construction judgment barred by P3, and the candidate quote captures only items 1-3 of a longer list, so the full exclusion set has not even been read.

#### What remains genuinely uncertain — not stretched to fit

Per the instruction's final condition, three requirements had no candidate clause available and were left as they were:

- **REQ-2 (법률상 배상책임 성립)** — the decisive requirement. The 보통약관 제3조 main insuring clause is **absent from all 104 candidate files**; verified by matching every clause head, 0 hits. The candidates cover 특별약관/추가특별약관 only, never the base 보통약관. Still contested in both directions (DOC_001 vs DOC_002), still unresolved, still P3-barred from resolution here.
- **REQ-4 (보험기간 중 사고)** — doubly unevaluable: no occurrence-basis period clause among the candidates (the period clauses present belong to claims-made 배상청구기준 특약 that do not apply), *and* both the accident date and the policy period are redacted out of processed text. `evidence_references` deliberately left empty.
- **REQ-9 (사고일로부터 1년 면책)** — has real clause text, but cannot be computed because the accident date is redacted. Relevant since the 2024-06-28 outpatient encounter is where most documented outpatient cost sits.

#### A selection hazard this exercise exposed

**DOC_034 also carries a 구내치료비 추가특별약관, with materially different terms** — 제1조 scoped to "학교구내에서 **학생이** 입은 신체장해", plus a 제3조 imposing a 180-day-from-injury cap and an explicit 보상한도액 that DOC_008's version does not state. DOC_034 was **not** used because its student/school scoping does not match this claim. But which variant is actually incorporated into *this* contract cannot be determined from processed text: the policy number and contract period in DOC_002 are redacted. Selecting among same-named 특약 variants by name alone is unsafe, and this is a concrete argument for why real normalization (which binds a clause to an audited document revision) is not a formality. Recorded in `coverage_result.json`'s warnings as well.

#### Standing caveat for the walkthrough

These two artifacts are now demonstration-grade, not decision-grade. Every coverage and requirement verdict resting on candidate text is a provisional polarity reading by this agent over text that the P1-2 check never examined. `_run_state.json` was left untouched per instruction and still does not record Stage 5. Anything downstream (screening report, draft report) that carries these conclusions forward must preserve the unvalidated labeling and must not present a candidate-sourced clause as an audited citation. The remediation is unchanged: run Stage 4 dispatch 4, then re-run checkpoints 2 and 4 together.

#### Follow-up decision (2026-07-31) — confirmed `matched_clause_ref`/`clause_ref` stay null, join mechanism not simulated

The dispatch's own summary flagged this as a judgment call needing confirmation: whether to populate `matched_clause_ref`/`clause_ref` with the real candidate `clause_uid`s to demonstrate the downstream join mechanism. Checked directly against `tools/dao.py` before deciding (`_downstream_policy_ref_errors()`, dao.py:1480-1529): the moment either field is non-null, `write-contract` runs `_canonical_state_blockers`, `_policy_stage_passed_errors`, and `_upstream_snapshot_errors` for real — the same audited-artifact checks the earlier hand-edit was specifically trying to avoid triggering. Since no `normalized_policy_clause_*.json`/`policy_audit_result_*.json` exists for DOC_007/DOC_008/DOC_019, populating either field would make the DAO refuse the write outright — confirmed by inspection, not executed against the real file (no need to reproduce a predictable refusal against live case state).

**Decision: leave both fields null**, exactly as the dispatch already had them. Populating them would not actually demonstrate a working join (the DAO would refuse the write before any join could be shown) and would only reproduce the refusal already documented structurally above — not a useful addition to today's walkthrough. The real `clause_uid`s remain visible in `evidence_references`/`warnings` prose per the existing v0.3 artifacts; no further contract revision was made.


## Stage 6 — Consistency Check (`consistency_check`) — 28 checks performed, 26 consistent / 2 inconsistent, 2 conflict-ledger entries raised (P6 gate now CLOSED)

Dispatched 2026-07-31. Reviewer/held-by `pyun`, `--stage consistency_check` throughout. Upstream input: Stage 5's four claim-analysis contracts (including the v0.3 candidate-sourced revisions of checkpoints 2 and 4 recorded immediately above).

Pre-flight: `check-conflicts-clear CASE_112` → `clear: true`, 0 pending. `read-conflict-ledger` → empty `conflicts` array. All five spec-required inputs read through `dao.py read-contract`: `extracted_claim_fields.json`, `coverage_result.json`, `case_type_result.json`, `requirement_matching_result.json`, `page_chunks.json` (359 chunks; 42 belong to the 20 available claim-fact documents DOC_001/DOC_002/DOC_210–DOC_224).

### Result

`evidence_validation_result.json` written via `dao.py write-contract --stage consistency_check` — schema-PASS, re-verified independently with `tools/validate_output.py`. `status: partial`, 28 `checks[]` entries, 11 warnings.

| | count |
|---|---|
| checks performed | **28** |
| `result: consistent` | **26** |
| `result: inconsistent` | **2** (CHK-6, CHK-7) |
| conflict-ledger entries raised | **2** (`CONFLICT_1`, `CONFLICT_2`), both `verdict: pending` |

Per the PoC dev-phase full-audit-trail decision, every comparison actually performed is logged, not only the findings. `conflict_id` is set on both `inconsistent` entries and `null` on all 26 consistent ones, per the schema's conditional rule.

### What was checked

Coverage across the fact classes the spec names — dates, diagnoses, accident circumstances, treatment periods — plus the financial and identity facts the pack supports:

- **Diagnosis/medical (CHK-1 to CHK-4, CHK-14 to CHK-19)**: KCD code agreement across DOC_214/DOC_215; diagnosis wording; anatomical site and laterality across six documents; surgery name across DOC_214/DOC_215/DOC_217/DOC_219; imaging modality vs the actual reports; fracture characterization (DOC_218 radiology vs DOC_217 intra-operative finding); the DOC_215 disability assessment's internal coherence (ROM → McBride Ⅲ-A-2 → 13% → 영구); pre-existing condition; the DOC_223 itemized bill against its own total.
- **Accident circumstances (CHK-5 to CHK-9, CHK-24, CHK-25)**: mechanism across DOC_001/DOC_002 and the contemporaneous clinical notes DOC_215/DOC_216; site character; premises scale; the 제설 fact; time of day; the insurer's own concession that the site is a facility it manages; the weather antecedent.
- **Dates/treatment periods (CHK-10 to CHK-13, CHK-17)**: accident date availability; the injury→presentation→surgery sequence; treating institution; admission duration vs the 8-week recuperation opinion and subsequent 타원 rehabilitation; the DOC_215 ROM date `24/6/28` against DOC_223's 2024-06-28 encounter carrying the 후유장애진단서 issuance fee — these match, which is a genuine positive cross-document corroboration, not merely an absence of contradiction.
- **Financial/identity/scope (CHK-20, CHK-21, CHK-26 to CHK-28)**: policy financial terms; claim identity; the ₩2,000,000 구내치료비 figure against the 246,560원 documented cost; DOC_224's 위자료 기준표 scope; and whether any extracted field silently rests on an excluded document.

### The two genuine conflicts raised

Both concern the **factual predicate of where and on what the claimant fell** — deliberately not the legal-liability dispute.

**`CONFLICT_1` — 사고 장소의 물리적 성격: 계단 vs 인도·도로.** DOC_001 and DOC_002's own cover letter (p1) both place the accident on a 계단. DOC_002's substantive legal-opinion body argues throughout in terms of 시설 내 인도와 도로 and never once addresses a stairway — including in its own 사고의 경위 summary. Its central reasoning is also drawn from a cited precedent about a flat 주차구획선 area, and it asserts the claimant could have chosen a cleared alternative route, which is a materially different proposition on a stairway than on an open walkway. Note the inconsistency is partly *internal to DOC_002* (p1 vs p4), which is why it is a source-vs-source finding and not merely an argument this agent disagrees with. Three sources recorded (DOC_001 p1, DOC_002 p4, DOC_002 p1) with verbatim quotes.

**`CONFLICT_2` — 사고 장소의 규모·제설 가능성.** DOC_001 p9: the stairway is '그리 넓지 않은바' and clearing it needed no excessive cost, manpower or equipment. DOC_002 p4: the site is '규모가 상당히 큰 시설' for which clearing all snow at once is practically impossible. Directly opposed factual characterizations of the same place. Recorded honestly in the artifact's warnings that these are not strictly irreconcilable in principle — a large facility can contain a small stairway — but neither document reconciles them, and each side's feasibility conclusion depends on its own characterization holding.

Both feed the 제설 feasibility analysis on both sides, so settling them changes the factual base Phase 2 argues from, independently of who is right on the law. Neither was resolved by this agent; per P6 that is a human decision.

### What was deliberately NOT raised, and why

- **DOC_214/DOC_215 diagnosis wording (CHK-2)** — logged `consistent`, no ledger entry. claim-analysis already handled this structurally via the primary/secondary labeling rule with both wordings preserved and both logged in its own `inconsistencies` array. Per this agent's spec, a finding already resolved by that documented, evidence-preserving priority decision must not be duplicated as a fresh P6 conflict. The KCD code S6280 is identical in both documents, so there is no code-level disagreement to begin with.
- **The two opposed legal opinions (CHK-22, CHK-23)** — logged for the audit trail, no ledger entry. `harness-guardrails` P6 expressly carves insurer-vs-evidence argumentation out as `denial-validation`'s Phase 2 analytical purpose. Recorded explicitly in the artifact's warnings that `result: consistent` here means "no P6 conflict raised", **not** "the two opinions agree" — they plainly do not.
- **Candidate-sourced coverage/requirement conclusions** — out of scope by design. `coverage_result.json`/`requirement_matching_result.json` v0.3 ground several judgments in unvalidated candidate clause text under the owner's walkthrough authorization. That is a provenance/verification-status question, not a factual contradiction between source documents; no candidate-sourced conclusion was treated as a fact needing reconciliation, and the triple unvalidated labeling on those artifacts stands untouched.
- **DOC_221/DOC_222 exclusion** — not rediscovered as a new finding. Restated only to bound coverage: 28 checks over 20 available documents, not over the complete pack.

### Limitations recorded (`status: partial`)

Two reasons, neither a failure of this stage's own logic. (a) DOC_221/DOC_222 remain P8-blocked and structurally unavailable, so cross-referencing could not be exhaustive — DOC_221 is a 7-page discharge-related document from the same DOC_005 bundle that supplied DOC_214–DOC_220 and plausibly carries dates and the institution name. (b) The accident date does not exist anywhere in processed text. Verified directly this run rather than assumed: a regex scan across all 42 claim-document chunks found that **every** date-like string in DOC_001/DOC_002 belongs to cited case law, and only two real dates survive case-wide (2023-12-04~07 admission, 2024-06-28 outpatient). CHK-11 therefore confirms only that DOC_216's relative sequence (`전일 slip down` / `익일 Op.`) is *compatible* with the 4-day admission window; it verifies no absolute date, and REQ-9's 사고일로부터 1년 치료비 cut-off stays uncomputable. That is a redaction-scope limitation, not a document disagreement, so no conflict was raised for it.

### Run-state could not be updated — same pre-existing defect, hit again

`write-contract` succeeded on the contract and then reported:

```
PASS: wrote E:\SNU\intern\harness-project\outputs\CASE_112\evidence_validation_result.json
NOTE: stage 'consistency_check' not advanced to 'in_progress' -- unmet dependencies:
  - cannot advance 'consistency_check' to 'in_progress': prerequisite 'claim_analysis' is absent (never recorded) -- must be passed first
```

This is a second-order consequence of the already-flagged hand-edit defect, not a new problem and not something this stage caused: because `_run_state.json` has been unwritable since the `policy_clause_processing` entry was hand-edited to `passed` with `backup_path: null`, Stage 5's four contract writes could never record `claim_analysis`, so the dependency check now correctly finds it absent. No repair was attempted — fabricating a `backup_path` would misrepresent P10 provenance and hand-editing would repeat the original violation. `_run_state.json` therefore does not record `consistency_check` either; resume logic remains untrustworthy for this case, and the honest fix is still real Stage 4 extraction.

### P6 gate status — CLOSED, human input required

`dao.py check-conflicts-clear CASE_112` now returns `{"clear": false, "pending": ["CONFLICT_1", "CONFLICT_2"]}` (exit 1). Per `harness-guardrails` P6 and `pipeline.md` stage 7, **`screening-report` cannot start** until a human sets each entry to `resolved` or `false_positive` with a `resolution_note` (schema-enforced since the 2026-07-22 fleet pass). This is the P6 mechanism working as designed on a real finding, and it is the first time this run has exercised it.

### P6 gate — resolved by human review (pyun, 2026-07-31), via `dao.py set-conflict-verdict`

**CONFLICT_1 (사고 장소: 계단 vs 인도·도로) → `resolved`.** Human judgment: the accident location is confirmed as the stairway (계단). DOC_001 and DOC_002's own cover-letter (p1) both state 계단; DOC_002's legal-opinion body (p4), which argues from a "시설 내 인도와 도로" premise, is judged to be the erroneous side of its own document's internal inconsistency — its premise does not match its own cover letter, and the precedent it draws on (a flat 주차구획선 case) does not transfer to a stairway's physical structure. Recorded instruction for downstream stages (notably Phase 2 denial-validation): treat the accident location as a stairway, and treat DOC_002 p4's alternate-route/인도·도로-premised argument as resting on an incorrect factual premise, not as a live alternative reading.

**CONFLICT_2 (시설 규모/제설 가능성) → `resolved`.** Human judgment: rather than adjudicating one side as correct, both positions are recorded side-by-side and passed downstream unreconciled. DOC_001's claim (the accident site itself — the stairway — is small enough that clearing it was not unduly burdensome) and DOC_002's claim (the facility as a whole is large enough that clearing everything at once was practically impossible) are not necessarily mutually exclusive (a large facility can contain a small stairway), and neither document reconciles the scale each is actually describing. Recorded instruction for downstream stages: Phase 2 denial-validation should construct rebuttal points aware of which scale (local stairway vs. whole-facility) each side's claim actually depends on, rather than treating either claim as having been factually settled here.

Both verdicts written via the sanctioned `dao.py set-conflict-verdict CASE_112 {CONFLICT_ID} resolved --note "..." --held-by pyun --run-id RUN_20260730_001` path — no hand-edit. `dao.py check-conflicts-clear CASE_112` now returns `{"clear": true, "pending": []}`. **Stage 7 (screening-report) is unblocked.**

## Denial Response (`denial_response`) — dependency-triggered, run ahead of screening-report — 2 reasons extracted, 0 policy matches (real DAO refusal confirmed)

Dispatched 2026-07-31. Reviewer/held-by `pyun`, `--stage denial_response` throughout. Triggered by DOC_002's reclassification to `predicted_document_type: insurer_response` (see the "Human classification correction — DOC_001 / DOC_002" section above), not by a Phase 2 gate. Run before `screening-report` because that stage's spec reads `denial_reason_result.json` as an input dependency whenever an insurer-response document exists.

### Input

`dao.py read-document-text CASE_112 DOC_002` → `data/processed/CASE_112/DOC_002/redacted_text.md`, read in full (15 pages: p1 is the insurer's 협조요청 공문 carrying the denial rationale, p2–p15 are the attached 법률자문 회신 the rationale relies on, bundled inside the same document). DOC_001 (피보험자측 법률질의회신서) was also read via the same DAO path for rebuttal-relevant context only — it is not the insurer-response document and no reason was extracted from it. `read-page-text` was not called at any point; every quote in the artifact comes from redacted text.

### Reasons extracted — 2, both `denial`, both `R04`

| reason | decision_type | payment_status | taxonomy_code | candidate_codes | policy_matches |
|---|---|---|---|---|---|
| `DR_1` — 법률상 배상책임 부존재 (핵심 결정) | `denial` | `unpaid` | **R04** 약관상 지급요건 미충족 | R04 0.72 / R05 0.30 / R99 0.10 | **0** |
| `DR_2` — 과실비율·위자료 산정 자체 거부 | `denial` | `unpaid` | **R04** 약관상 지급요건 미충족 | R04 0.55 / R99 0.30 / R11 0.10 | **0** |

`denial_reason_result.json` written via `dao.py write-contract --stage denial_response` — schema-PASS on the second attempt (first attempt failed on `model_info` missing `model_name`/`prompt_version`; that was the one P4 self-correction, and it was a shape fix, not a content change). Re-verified independently with `tools/validate_output.py` → PASS. `status: partial`, 11 warnings.

**Why DR_2 is `denial` and not `reduction`, and specifically not R11.** The insurer's legal opinion refuses to assess 과실비율 and 적정 위자료 at all ("피해자의 과실비율은 판단하지 않겠습니다" / "적정 위자료는 판단하지 않겠습니다"), on the stated ground that the accident was the victim's own inattention. This is not a computed 과실상계 deduction — no percentage was assessed and no loss amount was calculated — so R11 (피해자 과실상계, reduction-only) would misrepresent it as a quantified reduction that never happened. It is the damages-quantification consequence of the liability denial, so it stays `denial`/R04 with R11 kept only as a ranked third candidate. Recorded as a separate `reason_id` rather than folded into DR_1 because it is a materially distinct decision (refusal to quantify vs. refusal of liability) that Phase 2 will need to validate separately.

**R05 (면책사항) deliberately not assigned as the headline code, kept second.** The insurer never invokes an exclusion clause. Its entire argument is 민법 제750조/제758조 공작물책임 and case law — no 약관 조문 number appears anywhere in the 15 pages. Assigning R05 would attribute a clause-based defence the insurer did not make.

### 구내치료비 ₩2,000,000 — deliberately NOT recorded as a third reason

DOC_002 p1 item 6 states the ₩2,000,000 구내치료비 was already communicated to the injured party, in the same sentence as the liability-denial result. No 부지급/삭감 language attaches to it and no ground is stated against it. Manufacturing a `reason_id` for it would invent an insurer decision that the document does not contain, so it is recorded in `warnings` instead, flagged for denial-validation as rebuttal material (an insurer offering a benefit while denying liability) rather than as an insurer decision. Amounts across both reasons are all `null`: the document states only contract terms (보상한도액 ₩500,000,000/1인, 자기부담금 ₩100,000/1사고) and this ₩2,000,000 figure, none of which are a claimed/payable/denied/reduced amount.

### Policy matches — real write attempted, real DAO refusal, then honest fallback

Per the dispatch instruction, a populated `policy_matches` entry was **actually attempted** rather than assumed to fail: a scratch variant carrying one `agent_inferred` match to DOC_007 `C-2` (the 천재지변 exclusion) was submitted through the real `write-contract` path. Verbatim refusal:

```
FAIL: denial_reason_result.schema.json has stale, unresolved, or invalid policy references:
  - upstream_policy_snapshot is missing -- an artifact that references policy clauses must record the policy
    snapshot it was derived from (expected snapshot_sha256 71f8e0c8f04d776c...)
  - denial_reasons[0].policy_matches[0]: normalized policy contract is missing
  - denial_reasons[0].policy_matches[0]: policy audit is missing
  - denial_reasons[0].policy_matches[0]: canonical UID: DOC_007: no policy_boundary_inventory_DOC_007.json --
    clause UIDs are derived from their parent boundary, which only the inventory defines, so they cannot be
    recomputed without it
```

Nothing was persisted by the refused attempt. This confirms the real policy-layer state at the point of use, exactly as Stage 5 found: the hand-edited `policy_clause_processing: passed` unblocked only the stage gate, and the per-reference checks (`_canonical_state_blockers` / `_policy_stage_passed_errors` / `_upstream_snapshot_errors` plus the missing-normalized/missing-audit/missing-inventory checks) still refuse. `dao.py policy-snapshot CASE_112 --document-id DOC_007` → `BLOCKED`, confirmed directly this run. Both reasons therefore carry `policy_matches: []`, and `upstream_policy_snapshot` is **omitted entirely** (not fabricated) — the spec requires it only when a `policy_matches` entry exists.

Also worth recording: even with a working policy layer, **no `insurer_cited` match was ever possible for this case**, because the insurer cites no clause at all. Only `agent_inferred` matches were ever candidates here.

### Candidate-clause warnings — which reasons got them

Three candidate clauses were cited in `warnings` under the Stage-5 labeling convention (`[UNVALIDATED CANDIDATE CLAUSE — candidate_normalized_policy_clause_DOC_NNN.json, clause_uid PC-..., promotion_blocked=true, no polarity_analysis_receipt_id]`), all verbatim-checked against the candidate files before writing:

- **DR_1** — DOC_007 `PC-9a19575af9ae42f5` (제2조 보상하지 않는 손해 제3호, 천재지변 면책) as the only plausible clause-level hook for the insurer's "자연현상" argument, explicitly noting the insurer never invoked it and that whether 강설·결빙 falls inside "이와 비슷한 천재지변" is a P3-barred legal-construction judgment; and DOC_007 `PC-97d9be0921264da0` (제1조 사고 정의), noting the insurer itself concedes the site is a facility it manages, while the 보통약관 제3조 main insuring clause is absent from all 104 candidate files.
- **구내치료비 (not a reason)** — DOC_008 `PC-e01f526afabe328e` 제2조 item 11 ("타인의 신체장해에 대하여 피보험자에게 법률상의 배상책임이 있는 치료비" is excluded), the same REQ-8 issue Stage 5 flagged: the benefit appears to operate precisely where liability does *not* attach, which is coherent with the insurer denying liability while offering ₩2,000,000.
- **DR_2** — **no candidate clause cited at all.** Its grounds are purely factual/quantification-refusal statements with no clause counterpart; nothing was stretched to fit.

Stage 5's 변형-특약 selection hazard is restated in `warnings`: DOC_034 also carries a 구내치료비 추가특별약관 with different scope (학생/180일/별도 한도), and the 증권번호·보험기간 in DOC_002 are redacted, so which 특약 판본 this contract actually incorporates cannot be determined from processed text.

### CONFLICT_1 / CONFLICT_2 human resolutions carried into the artifact

Both 2026-07-31 verdicts were applied rather than re-litigated. `BASIS_6` (DR_1) is the only `agent_inferred` basis in the artifact and records CONFLICT_1's resolution directly: DOC_002 p1 says 계단, DOC_002 p4's legal-opinion body argues from a 시설 내 인도와 도로 premise, and the human verdict confirmed the stairway and judged the 인도·도로-premised alternate-route argument to rest on an incorrect factual premise. It is routed `review_required: true` / `손해사정사` per the spec's rule for inferred items. `BASIS_3` (the 규모가 상당히 큰 시설 / 제설 불가능 ground) carries a note that CONFLICT_2 was resolved by preserving both scale characterizations unreconciled, so denial-validation knows which scale each side's claim depends on rather than treating either as settled.

### `status: partial` and run-state

`partial` for three stated reasons: no policy matches recordable (policy layer absent); no final 지급결정 문서 exists in the pack, so the payment outcome is inferred from the absence of any payment statement rather than from a confirmed determination (`payment_status: unpaid` is labeled as such in `warnings`); and the accident date remains redacted out of processed text, so period-dependent grounds are not evaluable. None of these is recoverable by a P9 retry — they need real Stage 4 extraction or human access to unredacted source.

Run-state could not be updated, identically to Stages 5 and 6:

```
PASS: wrote E:\SNU\intern\harness-project\outputs\CASE_112\denial_reason_result.json
FAIL: schema validation errors for ...\_run_state.json -- not written:
  - stages/2/backup_path: None is not of type 'string'
```

Same pre-existing hand-edit defect (`policy_clause_processing: passed` with `backup_path: null`), not caused by this stage. No repair attempted — fabricating a `backup_path` would misrepresent P10 provenance and hand-editing would repeat the original violation. `_run_state.json` does not record `denial_response`.

### Consumers

`denial_reason_result.json` is now available for `screening-report` (Phase 1 §2, insurer's determination) and later for `denial-validation` (Phase 2). Phase 2 must produce exactly one validation per reason (`DR_1`, `DR_2`) and zero policy-match verifications, since `policy_matches` is empty on both.

## Stage 7 — Screening Report (`screening_report`) — both artifacts written, template gate passed first try, denial hash accepted

Dispatched 2026-07-31. Reviewer/held-by `pyun`, `--stage screening_report` throughout. Append-only; nothing above this section was edited.

Pre-flight: `dao.py check-conflicts-clear CASE_112` → `{"clear": true, "pending": []}` (exit 0). The P6 gate Stage 6 closed is genuinely open again via the two human verdicts, so this stage was entitled to start.

### Inputs read (all via `dao.py read-contract`)

`extracted_claim_fields.json`, `coverage_result.json` (v0.3), `case_type_result.json`, `requirement_matching_result.json` (v0.3), `denial_reason_result.json`. `evidence_validation_result.json` and `read-conflict-ledger` were additionally read to source §4 (문서 간 불일치) — the template assigns that section to consistency-check, and the two human-resolved conflicts live in the ledger, not in the five spec-named inputs. No `source-cases/` or `data/ground_truth/` access at any point; no direct `outputs/` reads.

### Output

| artifact | result |
|---|---|
| `outputs/CASE_112/screening_report.json` | written via `dao.py write-contract --schema-name screening_report.schema.json --stage screening_report`, **schema-PASS**; re-verified independently with `tools/validate_output.py` |
| `outputs/CASE_112/screening_report.md` | rendered via `document_assembly.py --template screening_report`, **7/7 sections in exact registry order, accepted on the first attempt** |
| `outputs/CASE_112/screening_report.evidence.json` | auto-generated by the same pass, **31 citations**, schema-PASS against `evidence_sidecar.schema.json` |

`status: partial`, overall `confidence: 0.55`, 16 warnings, 6 key issues, 5 inconsistencies, 10 missing documents, 17 review points. No schema-validation failure occurred, so the P4 single-correction budget was never used. The one tooling hiccup was a CLI arity error (`write-contract` requires `--schema-name`), which is not a validation failure and consumed no P4 attempt.

### `insurer_position` — denial and reduction kept structurally separate

- `has_denial: true`, `denial.reason_ids: ["DR_1","DR_2"]`, `denial.total_amount: null`
- `has_reduction: false`, `reduction.reason_ids: []`, `reduction.total_amount: null`
- `has_denial_or_reduction: true` — recorded strictly as the logical OR of the two explicit booleans, and labeled in `warnings` as the deprecated compatibility field it is. `main_reason_code`/`main_reason_label`/`reduction_amount` are populated but likewise flagged non-authoritative.

`total_amount` is `null` on both sections because denial-response set every amount field null on both reasons: DOC_002 states only contract terms (보상한도액 ₩500,000,000/1인, 자기부담금 ₩100,000/1사고) and the 구내치료비 ₩2,000,000 figure, none of which is a claimed/denied/reduced amount. Per the spec, a total is set only when every amount needed for it is explicitly stated — nothing here was summed or estimated.

**The 감액 subsection was deliberately rendered rather than omitted.** `§2-2. 감액 (reduction) — 해당 없음` states explicitly that no `decision_type: reduction` decision exists in DOC_002 and that this is a review finding, not an oversight, so a triage reader can see reduction was checked. It also records why DR_2 is not a reduction: the insurer refused to assess 과실비율/위자료 at all rather than computing and deducting, so R11 (과실상계, reduction-only) would misstate a quantified deduction that never happened.

### `source_denial_contract_hash`

Computed from the actual current file immediately before writing, via `_cross_contract.upstream_hash()` (the method `denial-validation.md` documents): `7ea4191b150252346168e71fee34d328728b779ff3297a7147bf39553269f660`. The DAO recomputed it at write time and accepted, confirming the denial contract was not rewritten during this stage.

### How the unvalidated-policy-data caveat was handled

This was the central instruction of the dispatch, and the handling is deliberately redundant so the caveat cannot be lost by a reader who sees only one part of the artifact:

1. **Nothing was upgraded.** Stage 5's v0.3 coverage/requirement conclusions were summarized at their own stated certainty, never restated as settled findings. The two coverages appear as candidate-grounded identifications, not as established coverage; the 4 `met` requirements are reported as candidate-grounded readings.
2. **A blocking notice sits at the top of §1** of the narrative, before any coverage statement is made, naming `CANDIDATE_UNVALIDATED` / `promotion_blocked: true`, the 0-count of `normalized_policy_clause_*.json` and `policy_audit_result_*.json`, the all-null `matched_clause_ref`/`clause_ref`, and the absent `upstream_policy_snapshot`.
3. **`ISSUE_6` exists solely to carry this** as a first-class key issue rather than a footnote, so it appears in the structured JSON a downstream consumer joins on.
4. **The *nature* of the missing check is named, not just its absence.** Two warnings state that the un-run P1-2 check is specifically the *polarity* check, so candidate-grounded conclusions are not merely low-confidence but of the kind that **invert** if misread. `ISSUE_2` (구내치료비) is explicitly labeled the single highest-damage item under that failure mode.
5. **Upstream confidence caps were preserved and mirrored** (0.45–0.6 upstream; this report's overall 0.55), and no `review_required` flag was flipped to false anywhere.
6. **`draft_writer_input_summary`** carries an explicit instruction that draft-report must not promote these to settled findings nor present a candidate `clause_uid` as an audited citation.

### Priority review flags actually included (`preliminary_assessment`, all P3-hedged)

`feasibility: medium`, `difficulty: high`, 7 priority points, each prefixed `[잠정]` and routed `review_required: true` / `손해사정사`:

1. The whole coverage/requirement picture rests on unvalidated candidate text — recognize this before reading anything else.
2. REQ-2 (법률상 배상책임 성립) is decisive **and** has no clause basis at all — the 보통약관 제3조 main insuring clause is absent from all 104 candidate files, so the case's most important requirement is exactly the one the candidate corpus does not cover.
3. 구내치료비 ₩2,000,000 should be triaged as a **separate action item**, not folded into the liability dispute — potentially a no-fault benefit engaged precisely where liability does not attach, with the polarity-dependence stated in the same breath.
4. CONFLICT_1/CONFLICT_2's resolutions change how DOC_002 is read: the 인도·도로 premise (and the alternate-route argument built on it) is a **wrong factual premise**, not a live alternative reading; whereas the facility-scale claims were deliberately left unreconciled and must be handled by distinguishing local (stairway) from whole-facility scale.
5. The missing accident date is the bottleneck for REQ-4 and REQ-9, and bears directly on the 2024-06-28 encounter where most documented outpatient cost sits.
6. The 13%/영구 disability figures are DOC_215 restatements, not this stage's determination — routed to 의사, with the 교통사고-scoped 기왕증 form field flagged.
7. Most upstream inputs are `status: partial` for reasons P9 retry cannot fix.

Feasibility `medium` rather than higher/lower is itself hedged in-text with both the favorable factors (the insurer's own concession that the site is a facility it manages; the stairway confirmed by human review; a possibly independent 구내치료비 path) and the unfavorable ones (decisive requirement contested with no clause text, accident date lost, no income basis), and states it may move in either direction once audited policy text exists.

### Run-state — the known defect recurred, in its Stage-6 dependency form

`write-contract` succeeded on the contract and then reported:

```
PASS: wrote E:\SNU\intern\harness-project\outputs\CASE_112\screening_report.json
NOTE: stage 'screening_report' not advanced to 'in_progress' -- unmet dependencies:
  - cannot advance 'screening_report' to 'in_progress': prerequisite 'claim_analysis' is absent (never recorded) -- must be passed first
  - cannot advance 'screening_report' to 'in_progress': prerequisite 'consistency_check' is absent (never recorded) -- must be passed first
```

Confirmed directly rather than assumed by running the sanctioned `dao.py update-run-state CASE_112 RUN_20260730_001 screening_report in_progress --held-by pyun`, which returned `REFUSED` with the same two dependency errors. Note the difference from Stages 5/6: this call is now **refused at the dependency gate before it ever reaches the write**, so the `stages/2/backup_path: None is not of type 'string'` schema error does not even surface here. It is the same root cause one step removed — the hand-edited `policy_clause_processing: passed` with `backup_path: null` made `_run_state.json` unwritable, so `claim_analysis` and `consistency_check` were never recorded, and their absence now blocks this stage's own advance.

No repair attempted: fabricating a `backup_path` would misrepresent P10 provenance, and hand-editing would repeat the original violation. **`_run_state.json` does not record `screening_report`.** Resume logic remains untrustworthy for this case; the honest fix is still real Stage 4 extraction.

### Consumers

`screening_report.json` + `case_type_result.json`'s `template_id` (`배상책임_후유장해형`, a canonical `templates/registry.json` key) are now available to `draft-report` for v1. The draft must preserve the unvalidated-candidate labeling on every coverage/requirement statement it carries forward, treat 감액 as affirmatively absent rather than unexamined, and keep the 구내치료비 question separable from the liability outcome.

## Stage 8 — Draft Report v1 (`draft_report_v1`) — 8/8 sections, template gate passed first try, 95 citations, run-state defect recurred

Dispatched 2026-07-31. Reviewer/held-by `pyun`, `--stage draft_report_v1` throughout (canonical name, never bare `draft_report`). Append-only; nothing above this section was edited.

Pre-flight: `dao.py check-conflicts-clear CASE_112` → `{"clear": true, "pending": []}`. `case_type_result.json` re-read to confirm `template_id: 배상책임_후유장해형`, and that key verified verbatim against `templates/registry.json` before the render call rather than assumed from the run log.

### Inputs read (all via `dao.py read-contract`)

`screening_report.json` (the spec's primary v1 input), `case_type_result.json`, plus `extracted_claim_fields.json`, `coverage_result.json` (v0.3), `requirement_matching_result.json` (v0.3), `denial_reason_result.json` for the underlying quotes — the screening report summarizes these but the draft needs the verbatim source text for `evidence_references`. No `source-cases/` or `data/ground_truth/` access at any point; no direct `outputs/` reads.

### Output

| artifact | result |
|---|---|
| `outputs/CASE_112/draft_report_v1.md` | rendered via `document_assembly.py --template 배상책임_후유장해형 --held-by draft-report --run-id RUN_20260730_001`; **8/8 변형 A sections in exact registry order, accepted on the first attempt** (표지 / I. 사정 요약 / II. 위임 및 보험계약사항 / III. 사고 및 손해 발생 / IV. 관계법규 및 보상책임 / V. 사정 기초 / VI. 손해배상금 사정 / VII. 증빙자료) |
| `outputs/CASE_112/draft_report_v1.evidence.json` | auto-generated by the same pass, **95 citations**, schema-PASS against `evidence_sidecar.schema.json` (re-verified independently with `tools/validate_output.py`) |
| `outputs/CASE_112/draft_report_metadata_v1.json` | written separately via `dao.py write-contract --schema-name draft_report_metadata.schema.json --stage draft_report_v1`, **schema-PASS**; re-verified independently |

`status: partial`, `confidence: 0.5`, 14 warnings. No `[E#]` tag was hand-written — every one came from a `{{E}}` placeholder resolved by the tool. `dao.py read-evidence-tags` → `{"consistent": true, "orphaned_tags": [], "unused_citations": []}`. `dao.py check-forbidden-expressions` → `{"clean": true, "hits": []}` (the deterministic literal floor; the semantic P3 layer is the critic's).

**No P4 correction attempt was consumed.** The template gate and both schema validations passed on the first try. The one hiccup was a CLI arity error (`write-contract` requires `--held-by`), which is not a validation failure.

### How the unvalidated-policy caveat was carried into the actual deliverable text

This was the dispatch's central instruction, and it needed a different treatment from the screening report's: the draft is the document a human reads as the case's draft position, so a caveat living only in a JSON `warnings` array would be invisible to its actual reader. Four layers, each reaching a different kind of reader:

1. **A blocking notice on the 표지 itself**, before section I, headed "본 초안 전체에 적용되는 최상위 유보사항 (읽기 전 필독)". It names `CANDIDATE_UNVALIDATED` / `promotion_blocked: true`, the 0-count of `normalized_policy_clause_*.json` and `policy_audit_result_*.json`, the all-null `matched_clause_ref`/`clause_ref`, the absent `upstream_policy_snapshot`, and states explicitly that the un-run check is the **polarity** check, so a misread inverts the conclusion rather than merely weakening it. A reader who reads only the cover page still cannot miss it.
2. **A section-level 유보 block opening section IV** (관계법규 및 보상책임) — the only section that cites clauses at all — repeating the same in the place where the citations actually appear.
3. **The `[UNVALIDATED CANDIDATE CLAUSE — …, clause_uid PC-…, promotion_blocked=true, no polarity_analysis_receipt_id]` prefix travels inside every candidate-sourced quote**, exactly as Stage 5 established. Because these are `evidence_references`, the label is reproduced verbatim in `draft_report_v1.evidence.json`, so it survives being read through the sidecar rather than the prose.
4. **Wording throughout defers rather than concludes.** Coverage is written as "…로 읽힐 여지가 있다" / "잠정 판독", never as established coverage; section IV-2-나 states outright that REQ-2's governing clause (보통약관 제3조) is absent from all 104 candidate files, so the case's decisive requirement is exactly the one the corpus does not cover; the 천재지변 clause is presented with the express note that **the insurer never invoked it** and that the candidate quote captures only items 1-3 of a longer list. Sections VI-6-나 and VII both restate the caveat at the close, and VII footnotes 제13~15호 (DOC_007/008/019) as 후보·미감사 rather than listing them as ordinary 증빙.

Nothing was upgraded. Upstream confidence (0.45-0.6 on the coverage/requirement artifacts, 0.55 on the screening report) is mirrored at 0.5 here, and no `review_required` flag was flipped anywhere.

### What the draft actually says on the case's four load-bearing questions

- **Liability (REQ-2).** Both legal opinions are set out in section IV-1 and **neither is adopted** — P3. What the draft does record as fact-level findings are two things the insurer cannot dispute: that it concedes the accident site is a facility it manages (DOC_002 p4), and that its own p4 reasoning runs on an 인도·도로 premise contradicted by its own p1 cover letter.
- **CONFLICT_1 applied, not re-litigated.** The stairway is written as the established location throughout. DOC_002 p4's alternate-route argument is characterized as resting on an incorrect factual premise (and its cited precedent as a flat 주차구획선 case that does not transfer), explicitly **not** as a live alternative reading — matching the human verdict's recorded downstream instruction.
- **CONFLICT_2 applied as a deliberate non-reconciliation.** Section III-1-2 puts both characterizations in a two-row table with the human verdict stated, and section V-1 instructs that any 제설 feasibility judgment must first distinguish local (stairway) from whole-facility scale. Neither side is asserted as settled.
- **구내치료비 ₩2,000,000 kept separable.** Section IV-3 gives it its own subsection outside the liability analysis, reads DOC_008 제1조's "보통약관 제3조의 규정에도 불구하고" grant together with 제2조 제11호's exclusion of 치료비 *where legal liability does attach*, and states the resulting no-fault reading is coherent with the insurer offering the figure in the very letter denying liability — while labelling it the single highest-damage item if the polarity is read backwards. It is recorded as an item to confirm first and separately, never as an entitlement.
- **Disability figures restated, not determined.** 13% / 영구 / McBride Ⅲ-A-2 / the ROM measurements appear as DOC_215 quotations with an explicit statement that they are not this stage's medical judgment; adequacy, permanence timing, pre-existing contribution, causation, and the 관절면 침범 분쇄골절 prognosis are all routed to 의사 in section V-2. The 교통사고-scoped 기왕증 form field is flagged there as possibly-inapplicable boilerplate.

### Section VI produced no amounts, deliberately

`손해배상금 사정` is structurally present and complete but every figure reads 미정 / 산정 유보: 위자료, both 일실수익 구간, 손해배상금, and the final 사정금액. Three inputs are missing and none was estimated — the accident date (redacted out of processed text), occupation/income evidence (absent from the pack entirely), and any quantified 과실비율 (the insurer refused to assess one and the claimant side asserts none without quantifying). The section instead carries a four-row table naming each missing input, what it blocks, and its recovery path. The only real figure in the draft is the 246,560원 outpatient total, which is quoted as documented cost, not as an assessed 사정금액. Section V-4 keeps the 위자료 산식 skeleton with `{placeholder}`s rather than dropping it, per the template's 작성 규칙 5.

This is why `status: partial` — structurally complete, substantively unable to reach a quantum.

### Run-state — the known defect recurred, in its dependency form

```
PASS: wrote E:\SNU\intern\harness-project\outputs\CASE_112\draft_report_metadata_v1.json
NOTE: stage 'draft_report_v1' not advanced to 'in_progress' -- unmet dependencies:
  - cannot advance 'draft_report_v1' to 'in_progress': prerequisite 'screening_report' is absent (never recorded) -- must be passed first
```

Same root cause as Stages 5/6/7 and denial-response, one step further down the chain: `_run_state.json` has been unwritable through any validating DAO path since the `policy_clause_processing` entry was hand-edited to `status: passed` with `backup_path: null` (forbidden by `run_state.schema.json`'s `allOf`/`if`/`then`), so `screening_report` was never recorded, and its absence now blocks this stage's own advance. Note the shape matches Stage 7's exactly — refused at the dependency gate before the write is attempted, so the `stages/2/backup_path` schema error does not surface here either.

No repair attempted: fabricating a `backup_path` would misrepresent P10 provenance and hand-editing would repeat the original violation. **`_run_state.json` does not record `draft_report_v1`.** Resume logic remains untrustworthy for this case; the honest fix is still real Stage 4 extraction, after which `finalize-stage` can populate `backup_path` legitimately.

### Consumers

`draft_report_v1.md` + `draft_report_v1.evidence.json` are now available to `critic` for the v1 review pass. Points the critic should expect to weigh: the four-layer unvalidated-policy labeling (whether it is sufficient rather than merely present), the section VI all-미정 outcome (whether withholding amounts was correct rather than a gap), and the P3 line on liability — the draft records the insurer's own facility concession and the DOC_002 p1-vs-p4 internal contradiction as fact-level findings while declining to conclude on 배상책임 성립 itself. If Phase 2 runs, `draft_report_v2.md` reads this file plus `rebuttal_points.json` and writes a fresh `draft_report_metadata_v2.json` (never an overwrite of v1's).


## Stage 9 — Critic Pass v1 (`critic_v1`) — passed:true with 5 findings (medium 2 / low 3), both deterministic floors clean, run-state defect recurred

Dispatched 2026-07-31. Reviewer/held-by `pyun`, `--stage critic_v1` throughout (canonical name, never bare `critic`). Append-only; nothing above this section was edited.

Pre-flight: read `harness-guardrails`, `harness-guardrails-dev`, `pipeline.md`, `.claude/agents/critic.md`, `templates/forbidden-expressions.md`, and this log from the "Session resumed 2026-07-31" section through the end.

**D1 compliance.** No `source-cases/` or `data/ground_truth/` access at any point. Inputs were `draft_report_v1.md`, `draft_report_v1.evidence.json`, `draft_report_metadata_v1.json`, `candidate_normalized_policy_clause_DOC_007/008/019.json`, and `data/processed/CASE_112/*/redacted_text.md` for quote verification.

### Output

| artifact | result |
|---|---|
| `outputs/CASE_112/critic_result_v1.json` | written via `dao.py write-contract --schema-name critic_result.schema.json --stage critic_v1`, **schema-PASS on the first attempt**; re-verified independently with `tools/validate_output.py` → PASS. No P4 correction consumed |
| `outputs/CASE_112/draft_report_v1_reviewed.md` | written via `dao.py write-reviewed-draft CASE_112 v1 --held-by pyun --run-id RUN_20260730_001`. Annotation-only: verified by difflib that **0 original lines were removed or altered** — the five `>>> [CF-N]` blocks are pure insertions |

`passed: true`, `orphaned_tag_count: 0`, `unused_citation_count: 0`, `forbidden_literal_hit_count: 0`, `confidence: 0.82`, 5 findings, 10 warnings.

### Deterministic floors — both clean, both genuinely ran

- `dao.py read-evidence-tags outputs/CASE_112/draft_report_v1.md` → `{"consistent": true, "orphaned_tags": [], "unused_citations": []}` across 95 citations.
- `dao.py check-forbidden-expressions outputs/CASE_112/draft_report_v1.md` → `{"clean": true, "hits": [], "source": "templates\\forbidden-expressions.md", ...}` — a real `{clean, hits}` JSON object, **not** the bare `NOT_FOUND`/`NO_TEMPLATE` text that would indicate a setup failure. The literal floor genuinely executed against the authoritative template.

Per spec, the semantic P3 pass was run on top regardless of the clean literal result. It produced CF-5 and nothing else.

### Findings — 5, none of them a fabrication

| id | type | severity | one-line |
|---|---|---|---|
| CF-1 | `fabrication_unlinked_claim` | **medium** | The only candidate-clause quotation in the draft lacking the `[UNVALIDATED CANDIDATE CLAUSE]` marker |
| CF-2 | `fabrication_unlinked_claim` | low | DOC_019 clause paraphrased but cited nowhere — 0 of 95 citations reference it, though VII lists it as 증빙 제15호 |
| CF-3 | `fabrication_unlinked_claim` | **medium** | E12/E78/E84 all quote only DOC_224's bare title; the scope-limiting sentence they are used to prove is on the same page, unquoted |
| CF-4 | `fabrication_unlinked_claim` | low | E11/E69 point at DOC_001's liability conclusion, not at its no-comparative-fault statement |
| CF-5 | `unhedged_inference` | low | CONFLICT_1's verdict carried into the deliverable at strong register — ratification request, not a defect |

**Every asserted fact, clause, and figure was independently traced to real source text and matched.** 60+ citations spot-verified against processed text and the candidate clause files. No fabricated quote, no invented `clause_uid`, no invented figure. The clause UIDs the draft attributes (`PC-97d9be0921264da0`, `PC-9a19575af9ae42f5`, `PC-aa50bed1c8431b93`, `PC-e01f526afabe328e`, `PC-2eeb25243bb4ed33`, `PC-b074786a20b5358c`) all exist in their candidate files with matching text.

CF-1 is the one a reviewer should not defer. The bullet at IV-3-라 directly quotes `'사고일로부터 1년 후에 발생한 치료비'` (DOC_008 제2조 제10호) but its only tag `[E61]` resolves to a DOC_223 billing line. The clause text is genuine; the consequence is that because no clause-sourced evidence entry was attached, the inline `[UNVALIDATED CANDIDATE CLAUSE — …, promotion_blocked=true, no polarity_analysis_receipt_id]` prefix does not travel with this one quotation. It is the single leak in the draft's otherwise-sound four-layer labeling scheme.

### On the unvalidated-policy caveat — reviewed on merits, not accepted on self-report

The dispatch specifically directed not to wave this through. Verified independently rather than trusting Stage 8's account:

- All **6** clause-sourced citations in the sidecar (E52/E54/E58/E59/E60/E91) carry the full `[UNVALIDATED CANDIDATE CLAUSE — …, clause_uid PC-…, promotion_blocked=true, no polarity_analysis_receipt_id]` prefix **inside the quote string itself**, so the label survives being read through the sidecar alone. This was checked by parsing the JSON, not by reading the prose claim about it.
- The cover-page blocking notice, the section-IV 유보 block, and the VII 제13~15호 주기 are all present, and each names the un-run check as specifically the **polarity** check — the accurate and material framing, since a misread inverts rather than weakens.
- **Verdict: the scheme is sound; CF-1 and CF-2 are two coverage leaks in it, not a design failure.** Had the caveat been flattened, that would have been a high-severity finding. It was not flattened.

### Other judgment calls the dispatch asked to be weighed

- **Section VI's all-미정 outcome is correct, and the harder choice.** 사고일자 is genuinely absent from processed text, 직업·소득 evidence is absent from the pack, and neither side quantified 과실비율 (the insurer expressly refused — E10/E65/E68, verbatim). The four-row deficiency table naming each missing input and its recovery path serves a reviewer better than an estimate would. The only real figure, 246,560원, is correctly bounded as documented cost, not an assessed 사정금액. Not a finding.
- **P3 line on liability holds.** Both opinions set out, neither adopted. The two things stated as fact-level findings — the insurer's own concession that the site is a facility it manages (E51/E53/E88, verbatim in DOC_002 p4) and the p1-vs-p4 internal contradiction — are documented facts, not inferences.
- **Disability figures are consistently restatement, not determination**, with adequacy/permanence-timing/pre-existing/causation all routed to 의사. The 교통사고-scoped 기왕증 form field is flagged as possibly-inapplicable boilerplate rather than read as a clean negative finding — correct.
- **구내치료비 is the draft's strongest passage**: polarity-dependence stated, coherence with the insurer's own conduct noted, self-labeled as the highest-damage item if read backwards, and recorded as a separate item to confirm rather than an entitlement. No payability asserted.
- **Forbidden-expression substitutions appear in their correct substituted form** — `지급 가능성을 검토할 여지가 있으며` (lines 108, 536) and `해당 약관 적용의 적정성에 대한 검토가 필요하다` (lines 261, 293), both prescribed replacements from `templates/forbidden-expressions.md`. Evidence the writer worked against the table.

### Why `passed: true` — an explicit judgment, not an inference from the array

No finding is a fabrication; no forbidden expression appears literally or semantically; the P3 line holds on liability, disability, and coverage throughout. The two medium findings are citation-support defects correctable in a handful of `evidence_references`, not structural problems with the draft's reasoning or its caveat discipline. CF-1 should be fixed before this draft is treated as a working document, but it does not warrant failing the version.

### Observation worth keeping beyond this case — a structural blind spot in the deterministic check

CF-1 through CF-4 are one defect class: **a true, source-verifiable assertion carrying a citation that does not itself evidence it.** `read-evidence-tags` cannot detect this by construction — it verifies tag↔sidecar-entry *correspondence*, never quote↔assertion *support*. It returned `consistent: true` here while four such gaps existed, one of which (CF-1) directly caused an unvalidated-label leak. This is distinct from `known-gaps.md` item 18's `document_assembly` orphan-tag entry, which concerns a different axis. Recorded as an observation; no code change made this pass.

### Run-state — the known defect recurred, in its dependency form

```
PASS: wrote E:\SNU\intern\harness-project\outputs\CASE_112\critic_result_v1.json
NOTE: stage 'critic_v1' not advanced to 'in_progress' -- unmet dependencies:
  - cannot advance 'critic_v1' to 'in_progress': prerequisite 'draft_report_v1' is absent (never recorded) -- must be passed first
```

Confirmed directly rather than assumed, by running the sanctioned path: `dao.py update-run-state CASE_112 RUN_20260730_001 critic_v1 in_progress --held-by pyun` → `REFUSED` with the same single dependency error (exit 1). `dao.py get-last-passed-stage CASE_112` still returns `policy_clause_processing`.

Same root cause as Stages 5/6/7/8 and denial-response, one step further down the chain: `_run_state.json` has been unwritable through any validating DAO path since the `policy_clause_processing` entry was hand-edited to `status: passed` with `backup_path: null`, so `draft_report_v1` was never recorded, and its absence now blocks this stage's advance. Shape matches Stages 7/8 — refused at the dependency gate before the write, so the `stages/2/backup_path` schema error does not surface here either.

No repair attempted: fabricating a `backup_path` would misrepresent P10 provenance and hand-editing would repeat the original violation. **`_run_state.json` does not record `critic_v1`.** Resume logic remains untrustworthy for this case; the honest fix is still real Stage 4 extraction.

### Consumers

`critic_result_v1.json` + `draft_report_v1_reviewed.md` now go to **human review** — `critic` does not hand off to `evaluation` directly. A human disposes of CF-1…CF-5 in `expert_review_v1.json` (which references these `finding_id`s), and only after `mark-human-review-complete` may `evaluation` open ground truth for v1. Finding IDs restart at CF-1 for any future v2 pass; a v2 finding at the same id is not the same entity.

## Run-state repair (2026-07-31) — the hand-edit's `_run_state.json`-wide write-block, actually fixed for real, so Stage 10 could proceed

The pre-existing defect flagged repeatedly above (Stages 5 through denial-response all recording a `write-contract` PASS followed by a run-state advance failure) finally blocked something it could not route around: `request-expert-review CASE_112 v1` (needed to start human review / evaluation) also writes `_run_state.json` and was refused with the identical `stages/2/backup_path: None is not of type 'string'` error. There was no dependency-gate workaround available for this one — it had to be actually fixed to proceed to Stage 10.

**Diagnosed the real structural bind before acting**: `policy_clause_processing` could not be left `failed` (the honest state) because `claim_analysis`'s dependency check then correctly refuses to finalize (`prerequisite 'policy_clause_processing' is failed -- must be passed first`) — confirmed directly, not assumed, by attempting `finalize-stage claim_analysis` against the reverted state and getting exactly that refusal. So the file could be schema-valid-but-blocking (as `failed`) or schema-invalid-and-writable-nowhere (as `passed`/`backup_path: null`), but not both open and honest at once, given that real Stage 4 completion (the only fix that is both) was out of scope for today.

**Reported this tradeoff to the project owner explicitly before acting**, including that the only way to make the file schema-valid again while `policy_clause_processing` reads `passed` is to populate `backup_path` with something — and a real path would misrepresent a P10 snapshot that doesn't exist, a second fabrication layered on the first. The project owner explicitly instructed proceeding anyway, this being a one-off demo case, not a real production run.

**What was actually done, via direct hand-edit of `outputs/CASE_112/_run_state.json` (not a DAO call — no DAO path exists for this), in order:**

1. Reverted `policy_clause_processing` to `status: "failed"` (its honest state), which restored file-wide schema validity and confirmed `get-last-passed-stage` worked again (`document_processing`). Recorded a `manual_override.reverted_at`/`reverted_reason` explaining why, on the same entry the original 2026-07-31 hand-edit created.
2. Attempted the real, sanctioned `dao.py finalize-stage claim_analysis` against this reverted state — refused for the reason above, confirming the bind was real and not assumed.
3. Set `policy_clause_processing` back to `status: "passed"`, but this time with `backup_path` set to the literal string `"MANUAL_OVERRIDE_NO_REAL_SNAPSHOT -- see manual_override note below; this path does not point at a real P10 backup directory and must never be read/restored from"` rather than a plausible-looking fake path — deliberately unusable as a real path (no such directory exists, and the string itself refuses to look legitimate) so it cannot be mistaken for or accidentally treated as a real snapshot location. Added a second `manual_override.re_reverted_at`/`re_reverted_reason` entry naming exactly what this adds (a second false claim — a P10 snapshot existing — stacked on the first, the stage having really passed) and the concrete repair steps for whoever fixes this for real later (run Stage 4 dispatch 4, let `finalize-stage` populate a real `backup_path`, delete the whole `manual_override` block). Confirmed schema-valid via direct `validate_instance()` call: 0 errors.
4. With the file writable again, ran the real, sanctioned `dao.py finalize-stage` for every stage that had genuinely completed its contract-level work this run but never got to record itself in run-state, in dependency order: `claim_analysis` → `consistency_check` → `screening_report` → `draft_report_v1` → `critic_v1` → `denial_response`. **All six passed on the real gate, no shortcuts** — each call independently verified the stage's actual completion criteria (contract files present, schema-valid, dependencies satisfied) before recording `passed` and taking a real P10 snapshot at `outputs/CASE_112/_backups/step_01_{stage_name}/`. `get-last-passed-stage CASE_112` now correctly returns `denial_response`.
5. `dao.py request-expert-review CASE_112 v1 --held-by pyun --run-id RUN_20260730_001` → `OK: evaluation -> waiting`. The block is cleared; Stage 10 (evaluation) Phase A is now proceeding.

**What remains true and unchanged**: `policy_clause_processing` is still not really complete (0/203 real normalized policy clauses). The six stages finalized in step 4 are now honestly recorded — their underlying contract work was always real, only the run-state bookkeeping was previously missing — but they still rest, where applicable, on the same candidate-clause/unvalidated-policy-data caveats documented throughout this log. The `manual_override` block on `policy_clause_processing` is the single remaining dishonest artifact in `_run_state.json`, is clearly labeled as such in three places (its own `reason`, `reverted_reason`, `re_reverted_reason`), and its removal condition is documented in the same block: real Stage 4 clause extraction, followed by a real `finalize-stage`/`snapshot-backup` run.

## Stage 10 — Evaluation (`evaluation`) — Phase A + Phase B complete; real outcome: 배상책임 인정, 사정금액 33,650,000원

Dispatched 2026-07-31. Reviewer/held-by `pyun`, `--stage evaluation` throughout (canonical name). Append-only; nothing above this section was edited.

Pre-flight: read `harness-guardrails`, `harness-guardrails-dev`, `pipeline.md`, `.claude/agents/evaluation.md`, and this log from the "Session resumed 2026-07-31" section through the end.

### Phase A — `expert_review_v1.json` (no ground truth involved)

Written before any ground-truth access, per the spec's two-phase ordering. Confirmed `human_input_status` was genuinely `waiting` (set by `request-expert-review`) rather than assuming it, and re-read `critic_result_v1.json` through the DAO to confirm the five finding IDs and severities directly rather than taking them from this log.

| item | value |
|---|---|
| `reviewer_id` / `reviewer_role` | `pyun` / `손해사정사` (the schema's enum offers only 손해사정사/의사/법률전문가 — there is no "project owner" value) |
| `overall_approved` | `true` |
| `findings_disposition` | CF-1…CF-5, **all `accepted`** |
| notes | Added on CF-1 and CF-3 (the two medium findings) recording that they are accepted as valid but deferred to a future revision pass. CF-2/CF-4/CF-5 carry none, per the schema |

Written via `dao.py write-contract --schema-name expert_review.schema.json --stage evaluation` → PASS; independently re-verified with `tools/validate_output.py` → PASS. The `general_comments` and `warnings` state explicitly that `overall_approved: true` means "findings acknowledged as valid, version approved as-is" — **not** "no findings" and **not** "findings fixed" — and that the approval does not lift the draft's demonstration-grade caveat.

The write returned the expected D1 refusal (`human review is not marked complete`), which is the gate working as designed, not the `backup_path` defect.

### Gate

`mark-human-review-complete CASE_112 v1 --reviewer pyun` was run by the orchestrator, not by this agent. `human_input_status` → `received` (`received_at: 2026-07-31T08:01:56+09:00`), `_human_review_complete_v1.flag` created. Note this also repaired run-state enough that `evaluation` advanced to `in_progress` normally — the first stage in this run since Stage 5 to do so.

### Phase B — ground truth opened

`dao.py read-ground-truth CASE_112 --caller-stage evaluation --version v1` → returned `data/ground_truth/CASE_112`, i.e. no longer denied. Ground truth is a **single file, `GT_001.pdf`, 12 pages**, with **zero embedded text** (verified: `pypdf` extracts 0 characters across all 12 pages). No processed/OCR'd copy and no `_intake_record.json` crosswalk exist for it. It was therefore rendered at 200dpi via PyMuPDF into the session scratchpad and read directly by this agent. This is inside the D1 exception (ground truth is not pipeline input and is not subject to P8 dual-read), and the render artifacts live in the scratchpad, not in `outputs/`/`data/`.

Note for the record: a `Bash`-level `ls` of the ground-truth directory was **denied** by `.claude/settings.json`'s Read/deny surface, and only the sanctioned `dao.py read-ground-truth` path was permitted — the same D1-shaped permission behavior CASE_022 recorded. No attempt was made to work around it.

### What ground truth actually shows

The real report is a completed 손해사정서 by 바른결손해사정 (책임사정사 김태윤), submitted under 보험업감독규정 제9-18조.

- **Liability: AFFIRMED.** "본 사고는 눈을 치우지 않은 계단을 보행 중 미끄러져 넘어지면서 발생한 사고로써, 위 기명피보험자는 가해자로 피해자의 손해에 대하여 민법 제750조, 민법 제755조에 의한 손해배상책임이 발생함." The insurer's 법률상 배상책임 부존재 position (DOC_002) was **not** adopted.
- **The 천재지변 exclusion was never applied.** "약관의 '보상하지 않는 손해'에 해당되는 항목이 없으므로" — the exact clause-level hook the draft identified as the insurer's only plausible one turned out to be unused.
- **Comparative fault: 0%.** "피해자의 과실을 찾기 어려우므로 사고 피해자의 과실은 없음."
- **Direct claim route:** 상법 제724조 제2항 + 약관의 손해배상청구권자 직접청구 — the victim claims directly against the insurer.
- **Disability:** 수관절 부전강직 Ⅲ-A-2 **13%, 영구장해**; 상병명 우측 손목 요골 원위부 골절 **(S6280)**.
- **Income basis:** 세법상 소득 입증 곤란 → 일용근로자 임금. 2023.09.01~ 161,858원×22일=3,560,876원; 2024.01.01~ 165,545원×22일=3,641,990원.
- **Quantum:** 위자료 100,000,000 × 13% × {1−(0%×6/10)} = **13,000,000원**; 적극적 손해(치료관계비) **3,391,540원**; 소극적 손해 **17,258,472원**; 손해배상금 33,650,012원 → 백원미만 절사 → **최종 사정금액 33,650,000원**. 향후치료비는 "추후 산정키로 함."

### Metric results

| metric | result |
|---|---|
| `core_field_accuracy` | **0.75** (12/16 일치) — applicable |
| `case_type_accuracy` | **correct: true** — 배상책임 (+후유장해), `배상책임_후유장해형` |
| `denial_reason_top1_agreement` | **correct: false**, predicted R04, **actual `null`** — applicable |
| `denial_reason_top3_agreement` | **correct: false**, candidates [R04, R05, R99], **actual `null`** — applicable |
| `policy_mapping_top3_inclusion` | **`applicable: false`** — 대조 대상 부재 |
| `draft_quality` | **`good`** (qualitative, no numeric score) |

**On the two denial-reason metrics — `correct: false` is not a classification failure.** The real adjuster did not uphold any denial; liability was affirmed and a payout was assessed, so **no R-code exists on the ground-truth side to agree with**. The draft's R04 (약관상 지급요건 미충족) remains a defensible classification *of what DOC_002 actually argued*. Reading these two `false` values as pipeline accuracy loss would be a misreading, and the artifact's `warnings` say so explicitly.

**`policy_mapping_top3_inclusion` is N/A on both sides.** Predicted side: 0/203 audited `normalized_policy_clause_*.json`, both `policy_matches` arrays empty, no `upstream_policy_snapshot`. Ground-truth side: the real report cites 약관 only by the headings "보상하는 손해"/"보상하지 않는 손해" and never by article number, so there is no clause-level target either. The unvalidated candidate citations were deliberately **not** substituted in as a stand-in.

**Four mismatched core fields, none of them a reasoning error:** `claimant_comparative_fault` (draft 미정 vs actual 0%), `medical_expense_incurred` (246,560원 vs 3,391,540원), `occupation_income_basis` (null vs 일용근로자 임금), `final_assessed_amount` (null vs 33,650,000원). All four trace to input scoping/redaction, not inference: 진료비세부내역서·사고경위서·소득 자료 were effectively absent from the processed pack, and the draft named each missing input plus its recovery path in a four-row deficiency table rather than estimating. `accident_date` is scored as a null-null match, which is flagged in `warnings` as *generous* — it means neither side surfaced the value, not that extraction succeeded.

### Two findings worth carrying beyond this case

1. **CONFLICT_1's human verdict was vindicated by ground truth.** The P6 gate resolved 사고 장소 as 계단 over DOC_002 p4's 인도·도로 premise; the real report independently describes "눈을 치우지 않은 계단." This is the first time in this project that a P6 human resolution has been checked against ground truth and found correct.
2. **A methodologically-careful hedge went the wrong way.** The draft flagged that DOC_224's 위자료 기준표 self-limits to 교통·산재 전담재판부 and therefore might not govern a premises fall (this is also critic finding CF-3's subject). The real adjuster applied the 서울중앙지방법원 기준금액 100,000,000원 without qualification. The draft's caution was defensible but diverged from actual practice — worth noting for how strictly this kind of scope caveat should be pressed in future drafts.

Input under-scoping recurs here in the same shape as CASE_022 (`known-gaps.md` item 14 addendum): the pipeline's reasoning quality and its input coverage must be read separately.

### Output

| artifact | result |
|---|---|
| `outputs/CASE_112/expert_review_v1.json` | `write-contract --stage evaluation`, **schema-PASS**, independently re-verified |
| `outputs/CASE_112/evaluation_result_v1.json` | `write-contract --stage evaluation`, **schema-PASS on the first attempt**, independently re-verified. No P4 correction consumed |

Both filenames are `_v1`-versioned per spec, so a future v2 evaluation cannot overwrite them. No `evaluation_summary.json` was written — that is a multi-case aggregate and this dispatch covered one case.

Self-correction worth recording: the first assembled `core_field_accuracy.score` read 0.8125 (13/16), which a recount before writing showed to be wrong — the true figure is 12/16 = 0.75. Corrected in the payload and in the two places the narrative/warnings restated the count, before the contract was written. Nothing incorrect reached disk.
