# Harness Changelog

Moved out of `CLAUDE.md` on 2026-08-16: the root instruction file is injected
into every subagent dispatch, and ~90KB of history was fixed context cost on
every stage agent that needed none of it. Same content, same order, append new
entries here (newest at the bottom of its date cluster, matching the old file).


| Date | Change | Scope | Reason |
|---|---|---|---|
| 2026-07-07 | Initial setup (7 agents + 2 skills + 2 tools) | project-wide | -- |
| 2026-07-08 | Notes-first, incremental-write discipline added | skills/component-output-contract | Observation 1: claim-analysis exited without notes (context lost on interruption) |
| 2026-07-08 | Resume-from-interruption + notes-existence check | skills/loss-adjustment-pipeline | Observation 2: promoted file-based-stitching resilience to an intended feature |
| 2026-07-08 | Primary-diagnosis-code selection rule (document-character-based priority) | agents/claim-analysis + wiki/field-extraction | Observation 3: headline KCD misselected in a permanent-disability case (failure type F1) |
| 2026-07-10 | Full restructure: English throughout, root taxonomy cleanup (`source-cases/`, `archive/`), old `component-output-contract`/`loss-adjustment-pipeline` skills replaced by `harness-guardrails` (10 universal rules P1-P10) + `harness-guardrails-dev` (4 PoC-only rules), entire pipeline redesigned (18+13-step draft → 10+2 stages) with a fresh 10-agent landscape (split from the old 7 to fix 3 identities that bundled unrelated jobs, most notably separating the ground-truth-blind critic pass from the ground-truth-sighted evaluation pass), and a DAO (`tools/dao.py`) making the guardrails structurally enforced rather than prompted. `outputs/` wiped -- CASE_003's old run predates all of this and isn't a valid baseline. | project-wide | Coworker brought a rough pipeline reference; review surfaced that the old skills leaned on dead `wiki/` cross-references (wiki now lives in a separate repo) and several rules were duplicated across files and already drifting between the Claude/Codex/generic copies |
| 2026-07-12 | Pipeline/tooling completeness review (findings in `known-gaps.md`); wrote the 12 output schemas the review found missing (`coverage_result`, `case_type_result`, `requirement_matching_result`, `normalized_policy_clause`, `evidence_validation_result`, `denial_validation_result`, `rebuttal_points`, `draft_report_metadata`, `critic_result`, `expert_review`, `evaluation_result`, `evaluation_summary`) plus a `policy_matches` patch to the pre-existing `denial_reason_result` schema; synced all 8 affected `.claude/agents/*.md` specs to match (via `tools/sync_agents.py`) | schemas/, agents/ | Half the pipeline's stages couldn't complete a DAO `write-contract` checkpoint -- no schema existed for their output, discovered via real test runs (CASE_002/CASE_009) that got further than prior smoke tests and hit this wall directly |
| 2026-07-13 | CASE_002 D1 near-miss resolved (both files rejected in `_source_ledger.json`, reviewer `Dev`, real evidence verified -- DOC_002 read in full, DOC_003 actually OCR'd for the first time rather than trusted secondhand); `tools/intake_case.py` gained a content pre-check (D2) -- one vision call over a `raw`-proposed PDF's first 5 pages, flags (doesn't auto-reject) answer-key-class content via `content_warning` in the ledger, since the case's PDFs turned out to have zero embedded text so a cheap keyword-scan wasn't possible; `source_ledger.schema.json` v0.1 -> v0.2 | tools/intake_case.py, schemas/source_ledger, harness-guardrails-dev D2 | Filename-pattern classification alone missed two files that were actually completed third-party loss-adjustment reports with stated payout figures -- caught only because a subagent happened to read the OCR'd content, not by any structural check; see `known-gaps.md` item 2 |
| 2026-07-13 | End-to-end pipeline audit found 4 real structural blockers nothing had ever exercised (every prior run stopped by stage 2-6, never reaching stages 9-10) -- all fixed: `dao.py` gained `write-text`/`write-reviewed-draft` (critic's annotated draft had no write path at all), `set-human-input-status`/`request-expert-review`/`mark-human-review-complete` (P7's wait-tracking and D1's evaluation gate had no write path either -- evaluation could never have been legitimately unblocked, for any case, ever), and `read-ground-truth` now takes `--version` (the gate flag is versioned per v1/v2 so a stale flag can't look valid during the other version's review); `normalized_policy_clause.json` renamed to one-file-per-policy-document (a multi-insurer case would have silently overwritten prior policy clauses -- worse than a halt, silent data loss) and `_validation.py` fixed to resolve the new suffix; `tools/chunk_text.py` added (checkpoint 3 had no chunking tool at all -- relied on an agent re-typing "verbatim" text with no structural guarantee), which required checkpoint 2 to start embedding `<<<PAGE page=N>>>` markers so page boundaries are recoverable. `evaluation.md` rewritten to describe the real two-phase flow (`expert_review.json` needs no ground truth; only the actual comparison does). 25 new tests (96 total) | tools/dao.py, tools/chunk_text.py (new), tools/_validation.py, agents/evaluation+critic+policy-pipeline+claim-analysis+denial-response, document-pipeline checkpoint 2/3 | See `known-gaps.md` item 8 for the full audit |
| 2026-07-13 | Found `ocr_result.json`/`classification_result.json`/`redaction_result.json` had the identical silent-overwrite bug just fixed for `normalized_policy_clause.json` (a shared `documents: []` array, written by a per-document stage, no merge logic) -- but with a much wider blast radius: it hits every case with 2+ documents of any kind, not just multi-policy-document cases. All three renamed to one-file-per-document and flattened (`document_id` now top-level, the array wrapper removed entirely, not just kept at length-1); schema versions bumped. 3 new tests (99 total) | schemas/ocr_result+classification_result+redaction_result, agents/document-pipeline | See `known-gaps.md` item 9 |
| 2026-07-13 | Added `tools/fork_case.py` -- forks a case's `outputs/`+`data/processed/` into a fresh `case_id` (schema-locked to `CASE_NNN`, auto-assigned; `data/raw`/`data/ground_truth` opt-in) so downstream branches can reuse already-completed OCR/redaction work instead of re-running it; `data/` was never covered by P10's per-step backups. Verified against real repo data (forked `CASE_009`), which surfaced that `schema_name_for()` never resolved `_source_ledger.json`/`_run_state.json`/`_conflict_ledger.json` (leading-underscore mismatch against their schema filenames) -- fixed; also surfaced, not yet fixed, that none of those three files' DAO write paths ever call `validate_instance()` at all, so they have zero schema enforcement anywhere. 19 new tests (118 total) | tools/fork_case.py (new), tools/_validation.py | See `known-gaps.md` item 10 |
| 2026-07-13 | Closed the gap the fork test surfaced: `cmd_set_ledger_status`, `_update_run_state`, `cmd_add_conflict_entry`, `cmd_set_conflict_verdict` now schema-validate before writing (matching `write-contract`'s own fail/don't-persist contract) -- `_source_ledger.json`/`_run_state.json`/`_conflict_ledger.json` had no enforcement anywhere before this. 3 adversarial tests added (feed genuinely invalid state through each function, confirm it's rejected and nothing gets written) since all 118 pre-existing tests passing unchanged only proved nothing broke, not that the new checks do anything. 121 tests total | tools/dao.py | See `known-gaps.md` item 10 |
| 2026-07-13 | Ran real checkpoint 1-3 end-to-end on a real document (`CASE_012`/DOC_001, via a fresh re-intake of `CASE_009`'s source after its 4 other files got flagged answer-key-class by the real content pre-check) -- confirmed real, non-mocked LLM calls throughout, resolved a real P8 disagreement (I67 vs I67.8, verified against the raw page image), and found a real P8 methodology gap along the way: `compare()` missed a hallucinated appendix one OCR read fabricated (English meta-commentary that doesn't exist on the source page at all) because it only checks for conflicting facts, not extraneous content. Corrected the affected processed file, added a `resolution` field to `ocr_result.schema.json` (also needed for the I67.8 resolution itself -- there was no way to record "resolved," only "agreed" or "disagreed_pending_review," before this) and a matching `disagreed_resolved` state mirrored into `document_manifest.schema.json`. The methodology gap itself (compare()'s blind spot) is NOT fixed -- flagged for every prior "agreed" page in the project, none of which have been re-checked | schemas/ocr_result, schemas/document_manifest | See `known-gaps.md` item 11 |
| 2026-07-13 | Added `tools/run_checkpoint1.py` (automates checkpoint 1's OCR->write->classify->manifest sequence in one call, stopping cold at a P8 disagreement) and `tools/run_scenario_matrix.py` (forks a real disagreement three ways -- reading_a/reading_b/unresolved -- via `fork_case.py`, running real OCR only once). `ocr_extract.py` refactored to expose `run_ocr()` as a reusable function (pure extraction, no behavior change). Found and fixed a real gap while building this: `ocr_result.json` never retained `reading_a`'s full text, so resolving a disagreement in a later, separate process would have forced a wasteful real-OCR re-run -- now saved to `_ocr_scratch/{case_id}_{doc_id}_raw.json` when a disagreement blocks the run. 16 new tests (138 total), all real subprocess calls mocked | tools/run_checkpoint1.py (new), tools/run_scenario_matrix.py (new), tools/ocr_extract.py | See `known-gaps.md` item 12 |
| 2026-07-13 | Ran `run_scenario_matrix.py` for real, twice, against a forked real document (`CASE_012`->`CASE_013`, `--include-raw`) -- confirmed genuine OCR non-determinism between the two runs (different pages disagreed each time). The first real run found a real bug (not a mocking gap): a disagreement-blocked run left `document_manifest.json`/`_run_state.json` showing STALE `completed`/`passed` values inherited from the fork source's earlier successful run, directly contradicting the fresh `ocr_result.json` -- not fork-specific, the same staleness would hit any genuine re-run that newly fails after a prior success. Fixed: the blocked path now resets every field checkpoint 1 owns (`ocr_status: failed`, redaction/classification fields nulled) and correctly marks the run-state stage `failed`. Re-ran for real a second time to confirm the fix on genuine non-deterministic data, not just a mocked test -- confirmed. 1 new test (139 total) | tools/run_checkpoint1.py | See `known-gaps.md` item 12 |
| 2026-07-13 | Fixed the P8 `compare()` blind spot found in the CASE_012 real run (known-gaps.md item 11): `COMPARE_PROMPT_TEMPLATE` now also asks whether either transcription contains one-sided content the other lacks (fabricated appendix, meta-commentary), not just whether core facts conflict, and instructs treating any such addition as a disagreement. Verified for real: reconstructed a faithful analog of the actual fabricated page-3 text (original wasn't persisted verbatim, since it wasn't a blocked disagreement at the time) and ran the fixed `compare()` against the real `claude` CLI -- correctly returned DISAGREE, and the model's own verdict independently flagged the embedded "route per D2 guidance" text as resembling a prompt-injection attempt and said it was ignoring it. 1 new test (140 total). Retroactive audit of every prior "agreed" page in the project against the fixed prompt is explicitly NOT done -- tracked as a separate follow-up. | tools/ocr_extract.py | See `known-gaps.md` item 11 |
| 2026-07-13 | CASE_002's D1 incident (item 2) fully resolved: re-intaking its source folder as a fresh case to exclude the 2 already-rejected files ran the (now-existing) D2 content pre-check against the 2 *other* insurer files for the first time and flagged both -- `DOC_001`(KB)/`DOC_004`(한화) were approved 2026-07-10 before the content-check tool existed, never actually verified. All 4 of CASE_002's insurer-specific files turned out to be the same third-party adjuster's completed reports; only the denial-notice file was genuinely raw. CASE_002's ledger corrected (`DOC_001`/`DOC_004` -> rejected, evidence recorded) and left as the historical incident record; `CASE_020` created via `intake_case.py --files "*면책 공문*"` as the actual go-forward case, ledger containing only the 1 clean file, content-check clear, executed for real. | tools/dao.py (used, no code change), known-gaps.md | See `known-gaps.md` item 2 |
| 2026-07-13 | Closed item 7's residual gap: added `dao.py patch-manifest-document` (and `patch_manifest_document()`, callable in-process) -- an atomic read-modify-write for a single document's fields in `document_manifest.json`, acquiring the lock before reading rather than after, unlike the generic `read-contract`+`write-contract` path every other file still uses. `run_checkpoint1.py`'s two manifest-mutation sites switched to it; `document-pipeline.md` updated to call it for both checkpoint 1 and checkpoint 2 (checkpoint 2's `redacted_text_path` update was never actually instructed before this pass -- a smaller gap found while fixing this one), synced to Codex/generic copies. 8 new tests (148 total), including one that reproduces the exact race this closes: a concurrent write lands while the caller is blocked on the lock, and the eventual write preserves it instead of clobbering it with pre-wait data. Verified for real too -- ran the new CLI subcommand via subprocess against a scratch copy of `CASE_020`'s actual manifest. | tools/dao.py, tools/run_checkpoint1.py, .claude/agents/document-pipeline.md | See `known-gaps.md` item 7 |
| 2026-07-13 | Closed item 4's last residual: 39 new tests for the two previously-untested tools, `intake_case.py` (25 -- pure helpers, `write_manifest`'s DAO write, and the `--execute` path end to end: DOC_XXX/GT_XXX renaming, ground truth never leaking into `document_manifest.json`, the `_intake_record.json` crosswalk, D2's block-on-any-unresolved-entry rule) and `sync_agents.py` (14 -- frontmatter parsing, TOML escaping including the adversarial embedded-triple-quote case, both sync functions). 187 tests total. Pure coverage debt -- neither tool had a proven-bug history and none was found writing these. | tests/test_intake_case.py (new), tests/test_sync_agents.py (new) | See `known-gaps.md` item 4 |
| 2026-07-13 | Frontend reviewed for the first time (item 5) -- found and fixed a real bug: `main.py`'s two human-review endpoints (`set_ledger_status`/`set_conflict_verdict`) shelled out to `dao.py` without `--held-by`/`--run-id`, which became required arguments when P5's locking was closed for those subcommands (item 7, earlier this session) -- the frontend was never updated to match, so approving/rejecting a ledger entry or resolving a conflict through the UI has been silently impossible since that change landed. Fixed with a `_frontend_run_id()` helper and updated call sites; verified for real against the actual `dao.py` subprocess path (first verification attempt accidentally mutated the real committed `CASE_020` ledger since a Python monkeypatch doesn't reach a subprocess -- caught via `git diff`, reverted, redone safely with a throwaway case). Rest of the frontend reviewed clean: backend already had real path-traversal guards and a scoped tool allowlist going in; two lint warnings traced and ruled out as false positives. One design-level gap noted but not fixed: `denial-response` (dependency-triggered) has no representation anywhere in the pipeline viewer. | frontend/backend/main.py | See `known-gaps.md` item 5 |
| 2026-07-13 | Automated security review of the frontend commit found a real argument-injection bug (item 13): `set_ledger_status`/`set_conflict_verdict` placed unvalidated `file_name`/`conflict_id` as bare positionals in a subprocess argv to `dao.py` -- a value exactly matching a real `dao.py` flag (e.g. `--held-by`) would be consumed by argparse as that option instead of the intended positional, desyncing the rest of the parse. Verified the exact attack before fixing, confirmed blocked after: added `_valid_conflict_id` (pattern match against the schema's own `CONFLICT_[0-9]+` format) and `_known_ledger_file_name` (must already be a real entry in the case's ledger). 15 new tests (202 total), including full-endpoint attack simulations. Also ran `tools/run_checkpoint1.py` for real against `CASE_020`'s one document -- found a real P8 disagreement (pages 1/3/4 of 4, same non-determinism this document has shown before) and correctly halted, no classification/manifest-completion. | frontend/backend/main.py | See `known-gaps.md` item 13 |
| 2026-07-13 | Open-decisions #2 (document-assembly template rules) turned out to already be answered -- `wiki/templates/draft-report.md` (a separate, wiki-owned git repo) had extracted the real section structure from the 4 ground-truth reports back on 2026-07-08, but that never made it into this repo, so `pipeline.md`/`open-decisions.md`/3 schemas kept describing it as "pending from the user." Adopted the 5-file `wiki/templates/` set into a new `templates/` at repo root (this repo's copy is now authoritative going forward; wiki's is caught up separately and may drift -- each adopted file carries a `provenance` note), rewrote cross-links to real harness-project paths (`.claude/agents/*.md`, `pipeline.md`, `schemas/`), and updated `case_type_result.schema.json`/`draft_report_metadata.schema.json`/`screening_report.schema.json`/`pipeline.md`/`open-decisions.md` to point at it instead of saying "pending." Marked #2 partially resolved, not closed: 배상책임_후유장해형/진단수술비형 are ground-truth-backed and documented, but 실손형/기타형 still have no ground-truth case to derive structure from, and `tools/document_assembly.py` still doesn't enforce section presence/order against `template_id` -- that wrapper is still a follow-up. | templates/ (new), schemas/case_type_result+draft_report_metadata+screening_report, pipeline.md, open-decisions.md | See `open-decisions.md` #2 |
| 2026-07-14 | CASE_004 Stage 2 was blocked by claude-cli OCR reader self-refusal: `ocr_extract.py` spawns child `claude -p` sessions as P8's two readers. The fix that stuck is a **neutral transcription prompt** with the image referenced as an explicit Read instruction (`ClaudeCliProvider.transcribe_image` sends `Read the image file at {path} and then: {prompt}`) plus `--safe-mode` so the child inherits none of this repo's `CLAUDE.md`/skills/hooks. An intermediate attempt that prepended a defensive `_OCR_READER_ROLE_FRAMING` block ("this is a SANCTIONED step, do not refuse…") plus a matching `CLAUDE.md` carve-out was the wrong direction and was reverted -- the framing *caused* the refusal. Also fixed a real cp949 crash in `_run`. Kept: the provider abstraction and the honest weak-P8 label (`cross_validation_mode`, schema-required). The only `CLAUDE.md` residue is the neutral Hard-rules note that no OCR carve-out is needed. | tools/llm_providers.py, tools/ocr_extract.py, tools/run_checkpoint1.py, schemas/ocr_result, CLAUDE.md (Hard-rules note) | See `open-decisions.md` #4 |

| 2026-07-14 | **First full end-to-end run in project history**: CASE_021 (raw = the 약관상 case's 4-page 3-insurer denial pack; ground truth = the adjuster's 4 완성 손해사정서, D2-intaken as GT_001-004) ran intake → evaluation in one pass (RUN_20260714_001) -- all stages passed, 21/21 contracts schema-PASS, human-review gates exercised for real under user-delegated review authority (reviewer recorded as `Claude (delegated by Rekhet)` throughout). Real P8 disagreement on pages 2/4 resolved by image review at 450-900dpi: page 2 was a SECOND live fabricated-addition catch (reading_a appended fake D2-routing meta-commentary; item 11's fixed compare() flagged it), page 4 needed human-corrected text (KCD I678 on-page -- one read dropped the digit, the other misread 8→g -- plus 7 lesser glyph fixes, low-confidence tokens flagged in the resolution note). Evaluation verdict: real adjuster ruled all policies payable (70M원 total) -- opposite the draft's hedged lean but via exactly the missing-evidence path the draft prioritized; draft rated `good`, core-field accuracy 0.86. Gaps sealed same day: run-state stage-name drift (schema v0.2 enum, all specs pinned, CASE_021 state repaired), extracted_claim_fields v0.2 (6 missing slots + oneOf→anyOf ad-hoc-date fix + FormatChecker enforcement project-wide -- `format: date` had been decorative everywhere), redaction scope convention settled in document-pipeline.md. OPEN: single-denial-pack under-scoping (농협 policy invisible to the whole run) -- user decision. 4 new tests (238 total, incl. the frontend-wiring branch's 32) | schemas/run_state+conflict_ledger+extracted_claim_fields, tools/_validation.py, all 10 agent specs, skills/loss-adjustment-pipeline, outputs/CASE_021 (new case) | See `known-gaps.md` item 14 |

| 2026-07-14 | **Phase 2 executed end-to-end on CASE_021** (RUN_20260714_002, delegation extended by the user; 농협 kept out of scope per user decision): `denial-validation` ran for the first time ever (all 3 R04 denial reasons → partially_supported; 3 hedged rebuttal points built strictly from insurer self-concessions, cross-notice tension, and the evidence-acquisition/재검토 paths — first live use of the adopted `templates/rebuttal-points.md`), draft v2 restructured to `templates/draft-report.md` 변형 B with v1's CF-1 fixed and 사정금액 correctly withheld (미정/산정 유보 — no amounts exist in-case), critic v2 passed (1 low finding: 한화-only 소견 over-attributed to "각 안내문", fix binding for v3), both v2 human gates exercised for real (expert_review_v2 + versioned v2 flag — item 8's per-version gate working as designed). Evaluation v2 headline: the real adjuster overturned all denials via a 신경외과 의료자문 concluding exactly the "소혈관질환 초기 소견" reading v2 made load-bearing across RB-1/2/3, resolved via exactly the 제3자 의료자문 path v2 named — rebuttal grounds vindicated by ground truth; core_field_accuracy 0.86→0.90, draft_quality `good` both versions (v2 materially stronger; held from `excellent` only by the P3-mandated refusal to assert the payable outcome plus the one low slip). All 13 canonical stages now `passed` across the two runs; every CASE_021 output schema-PASS. | outputs/CASE_021 (Phase 2 files), no code changes | -- |

| 2026-07-14 | Built open-decisions #2's enforcement wrapper: `templates/registry.json` (machine-readable section contracts derived from the template .md files -- changes with them in the same commit, D4) + `document_assembly.py --template <key>`, which validates section presence AND order before anything touches disk and hard-exits on mismatch (same fail/don't-persist contract as its sidecar validation). Enforced: 배상책임_후유장해형, 진단수술비형, screening_report; rebuttal_points deliberately unenforced (per-reason repeating structure). draft-report/screening-report specs now pass the flag; claim-analysis.md requires canonical registry keys as `template_id` (CASE_021's pre-template `template_진단수술비_v1` would now break the draft stage -- intentionally). 6 new tests (244 total) | tools/document_assembly.py, templates/registry.json (new), schemas/case_type_result, agents/draft-report+screening-report+claim-analysis, open-decisions.md, pipeline.md | See `open-decisions.md` #2 |

| 2026-07-15 | **Second case end-to-end: CASE_022 (기왕증·퇴행성 감액)**, RUN_20260714_003, first run under template enforcement (screening + 변형 A draft both passed the registry gate first try). Firsts: real P6 conflict raised and adjudicated (사고기전 계단 낙상 vs KB coded "무리한 동작" -- resolved 계단 낙상, KB coding preserved), D2 content pre-check flag reviewed-for-real and approved with documented reasoning (교보 third-party 사정결과 as 타사 지급내역 precedent evidence), P9 partial audited and accepted (KB states amounts not rationale -- unrecoverable by retry). **Live catch: OCR subprocess context contamination** -- both blind reads editorialized D2 meta-commentary onto transcribed pages; two-sided similarity defeated item 11's one-sided check and contaminated 3 processed pages before any downstream stage ran. Fixed structurally (`ocr_extract.py` --safe-mode on both claude calls), pages scrubbed under DAO with per-page resolution records (known-gaps item 15). **Evaluation twist:** draft vindicated on everything its pack could see (GT_001.txt confirms 50%→기왕증 10%p 공제→40%, R01, mechanism -- and the 계단낙상-vs-무리한동작 verdict), but GT_002 turned out to be a third-party 배상책임 quantum (89,952,400원, 노동능력상실률 32%) whose claim documents were never in the pack -- case_type scored correct=false through input scoping, not reasoning (item 14 addendum; second instance of the open under-scoping decision). core_field_accuracy 0.875, draft_quality good. Evaluation stage also hit two auto-mode classifier denials of `read-ground-truth` (the D1-shaped permission surface working as designed against an orchestrator relay); resolved by user adding the permission allow rule + explicit instruction. | outputs/CASE_022 (new case), .claude/settings.json (read-ground-truth allow rule), known-gaps.md item 14 addendum | See `known-gaps.md` items 14/15 |

| 2026-07-17 | Forbidden-expression table consolidated to one source. `critic` -- the only agent that checks forbidden expressions -- read `pipeline.md`'s copy of the table, while `draft-report`/`templates/draft-report.md` (the writer side) wrote against `templates/forbidden-expressions.md`: writer and checker consulted **different documents**, with no reference, test, or registry entry linking them, and `open-decisions.md` #2's "templates/ is authoritative" never reached the one consumer that disagreed. The copies happened to agree (only divergence: `POC guide.md` uses curly quotes, the others straight), so nothing broke -- but nothing would have caught it if they hadn't, since no code string-matches these at all (the check is a model reading a table, and `registry.json` contracts section *structure* only, never expression *content*). Fixed: `critic.md` + `draft-report.md` both now cite `templates/forbidden-expressions.md` explicitly, and `pipeline.md`'s table reduced to a reference (it had zero consumers left after the critic fix -- an orphan duplicate is exactly the surface that drifts unnoticed). Contrast worth keeping: the R01-R99 taxonomy has 4 copies and never drifted, because the schema enum is machine-enforced and every other file defers by reference rather than restating -- that's the pattern this change adopts. Found via a knowledge-graph pass over the harness docs (`graphify`), which surfaced the same table in three files with no link between them. No code change; 245 tests pass unchanged. | pipeline.md, agents/critic+draft-report | -- |

| 2026-07-18 | Built the forbidden-expression deterministic floor (spec: `docs/superpowers/specs/2026-07-18-forbidden-expression-enforcement-design.md`). New read-only `dao.py check-forbidden-expressions DOC_PATH` scans a rendered draft for the *listed literal* phrases in `templates/forbidden-expressions.md` and prints JSON hits (exit 0 clean / 1 hit-or-missing-draft / 2 missing-or-empty-template) -- mirroring `read-evidence-tags`. `critic` now calls it, records hits as `forbidden_expression` findings + a new optional `forbidden_literal_hit_count` (critic_result v0.2, additive -- pre-2026-07-18 outputs still validate), and still runs its semantic P3 pass for the implied/paraphrase cases the literal floor can't see. Record-only (a hit is a finding, never an auto-`passed:false`), no write-time gate (findings-not-halt preserved), no new agent (the critic already owns the semantic layer). Quote/whitespace normalization handles the straight-vs-curly drift found on 2026-07-17. New `tests/test_dao_forbidden_expr.py`. | tools/dao.py, schemas/critic_result, agents/critic, tests/ | See the design spec |
| 2026-07-21 | **PR #8 (`fix_codex`) review pass, integrated onto the branch's later work.** (1) **Purged the offline local model stack** -- `local-ocr`/`local-vlm`/`local-llm` providers, `tools/local_runtime.py`, both PowerShell setup scripts, `tests/test_local_runtime.py`. It never transcribed real Korean pages (`known-gaps.md` item 16) and was single-machine (Windows/E:) only. `dual_technology` stays a defined-but-unreachable `cross_validation_mode`, reserved for a future real OCR engine; every current provider is LLM-vision-backed so any reader pair is honestly `single_technology_weak_p8_poc`. Kept the branch's embedded-text passthrough (`extraction_method: embedded_text`, `deferred_poc`) and non-text-image document path (`non_text_image`), which are independent of the local stack. (2) **Hardened the provider surface**: claude-cli passes `--model` (was silently dropped); `scan_intake_content` takes `image_paths` and attaches them per provider (openai-api previously scanned zero images); openai-api raises on a truncated response; CLI children run with a secret-scrubbed env; API stubs raise at build when no model resolves. Structural gaps + PoC-tolerable limits deferred to issues #10/#11. (3) **Redaction abstraction** (`tools/redaction.py`): a `Redactor` seam (today `LlmRedactor`, dev default codex-cli, verified live producing correct spans; OpenMed NER can drop in). Redesigned to **span substitution, not page rewriting** -- the LLM only IDENTIFIES PII values and the redacted text is built deterministically from the source, so non-PII omission/fabrication is structurally impossible; a detected possible leak (structured PII surviving `scan_residual_pii`, or a model-named value not present verbatim) HARD-FAILS the document, over-redaction risk sets `review_required`. (4) **Backfilled** `cross_validation_mode` onto pre-field `ocr_result` files (`tools/backfill_cross_validation_mode.py`). (5) `sync_agents.py` robustness deferred to issue #12. Docs reconciled to the post-purge reality; `docs/CASE_004_stage2_dev_bypass_progress.md` dropped. **Review-pass hardening (R1-R8, mostly verified against the real claude/codex CLIs):** CLI providers fail closed on empty/errored output; `_child_safe_env` gained a `HARNESS_CHILD_ENV_KEEP_PREFIXES` override for cloud-backed CLIs; OpenAI status handling requires `completed`; embedded-text decode is utf-8-sig-first (BOM strip + no mojibake) and fails closed on empty; the D2 vision scan fails closed on missing/absent images. 382 tests pass. | tools/llm_providers+ocr_extract+run_checkpoint1+intake_case+redaction(new)+redact_document+backfill_cross_validation_mode(new), schemas/ocr_result+redaction_result, guardrails/document-pipeline specs, README/pipeline/open-decisions/known-gaps | See issues #10/#11/#12; `open-decisions.md` #1/#3/#4 |

| 2026-07-21 | Expanded the denial/reduction taxonomy from R01-R09/R99 to R01-R21/R99 using the loss adjuster's reviewed frequency workbook. Added frequency as non-evidentiary `x-codebook` metadata, synchronized the human-readable table and schema with regression tests, updated `denial-response`, and removed stale draft-template code ranges. R02's existing correct label `장해율 과다` was retained instead of copying the workbook typo `징해율 과다`. | denial-response, pipeline taxonomy, common schema, draft template, tests | Loss-adjuster review supplied twelve additional operational reason codes and high/medium/low frequency tiers |
| 2026-07-22 | Added the loss adjuster's denial/reduction classification to each R-code as `applicable_decision_types` metadata. R15/R99 support both; R12/R14 remain explicitly unclassified because the reviewed source left those cells blank. Synchronized the pipeline table and regression tests. | pipeline taxonomy, common schema, tests | Loss-adjuster supplied the operational denial-vs-reduction mapping |
| 2026-07-22 | Separated per-reason `decision_type` from `payment_status`, removed `partial_payment` as a decision type, split insurer grounds into contractual/medical-factual/calculation categories, made policy-match source locations mandatory, added downstream policy-link verification, and split screening denial/reduction summaries while retaining the combined boolean for one deprecated compatibility version. | denial-response, denial-validation, screening-report, schemas, templates, tests | User approved the reviewed denial/reduction semantics and prioritized fail-safe missing links over incorrect policy matches |
| 2026-07-22 | **Full review-fleet pass over `main`** (8 independent adversarial reviewers: DAO/concurrency, provider/P8, redaction/intake, assembly/schemas, frontend-security, guardrails-vs-code, tests, adversarial-GT). Systemic finding: the DAO enforces guardrails along the sanctioned path but is not a *sealed* one, and D1 ground-truth isolation was breakable multiple ways. **Fixed (clear, verified):** DAO path traversal (case_id/doc_id/filename choke-point -- was arbitrary file read/write incl. `data/ground_truth` and `source-cases`); frontend `/source-file` served ground-truth (now class-`raw`-only); a tracked `.claude/settings.json` Read-tool deny-glob on `data/ground_truth`/`source-cases`/`data/raw`/`archive` (D1/P2 boundary for the Read tool -- Bash reads still need OS perms, documented); OCR child `Read` confined to the image dir; the lock primitive was TOCTOU-racy (now atomic `O_EXCL`); `_set_human_input_status` skipped schema validation; intake `--execute` could copy a ground-truth file into `data/raw` on arg drift (now cross-checks the reviewed ledger); `scan_residual_pii` missed multi-separator/line-wrapped/region-plate/contiguous-landline PII (widened, 0-FP re-verified); `chunk_text` in-band `<<<PAGE>>>` marker corrupted boundaries (now line-anchored + monotonic + fail-loud on pre-marker content); `document_assembly` output_path traversal (contained to `outputs/`); `conflict_ledger` now schema-enforces `resolution_note` when resolved. Deferred (documented, `known-gaps.md` item 18): D1 Bash-read OS-level seal, per-field manifest ownership, DAO-level P8 write guard, unstructured-PII/ordinary-word over-redaction, `document_assembly` orphan-tag detection, D2 `.pdf`/5-page scope, `fork_case` embedded case-id paths, frontend auth (accepted -- localhost-only dev tool). ~35 new tests. | tools/dao+llm_providers+redaction+chunk_text+intake_case+document_assembly, frontend/backend/main.py, .claude/settings.json (new), schemas/conflict_ledger, guardrails spec | See `known-gaps.md` item 18 |
| 2026-07-21 | **Stage 1 redesigned into Case Intake & Document Segmentation (`feature/stage1`).** A raw source may be a bundle concatenating several logical documents; treating one bundle as one document puts every later stage on wrong boundaries. `tools/segment_case.py` splits an approved bundle before OCR: 4×4 top-third contact sheets → boundary proposal + targeted full-page fallback → optional long-segment refine → human approval → logical PDFs, retaining the bundle as `superseded_bundle`. 4×4 measured F1 0.88 vs 3×4's 0.64; refine threshold 4 raised recall 0.81→0.96 and F1→0.94. `document_manifest` v0.6 adds provenance, superseded-bundle disposition, and the human `segmentation_status` gate. Every intake PDF starts pending; `run_checkpoint1.py` now blocks Stage 2 case-wide before any provider/PDF work until every PDF is human-cleared or split. | segmentation schemas/tools/tests, pipeline/CLAUDE, document-pipeline agent, loss-adjustment-pipeline skill | See `docs/stage1-document-segmentation-plan.md` |
| 2026-07-29 | **Stage 1 structured-output hardening.** Contact-sheet reasoning had become systematic prose-without-JSON under the full production prompt even when the image and boundary reasoning were correct. Stage 1 now calls a provider-neutral structured-image contract; claude-cli implements it with native `--output-format json --json-schema`, while future local providers can implement the same seam without Stage 1 changes. A failed sheet receives exactly one caller-owned correction, then remains unassigned and makes `propose` return `status: partial`/non-zero. Resume cache reuse is now bound to geometry + prompt + output-schema + provider + model, and failed cache entries are diagnostic-only. 170 focused tests pass. | tools/llm_providers.py, tools/segment_case.py, tests/test_llm_providers.py, tests/test_segment_case.py, Stage 1 plan | -- |
| 2026-07-30 | **Contact-sheet cache-versioning gap closed + full-page v0.3 remeasured on CASE_025.** The short contact-sheet prompt adopted 2026-07-29 had left `SEGMENT_PROMPT_VERSION` at its old `v0.2` value -- a stale-cache risk if any older sheet response had ever been saved under that version. Bumped to `v0.3` and added `_segment_prompt_fingerprint()` (a SHA256 of `SEGMENT_PROMPT`'s literal text) into the cache contract, so a future in-place prompt edit invalidates cached sheets even if the version constant is forgotten again. Real remeasurement against CASE_025's 110-page bundle (real Claude CLI vision calls, no mocks): cap-ratio 1.0/no-refine scored P 0.7527/R 1.0000/F1 0.8589 (26/26 fallback pages resolved, 0 unresolved, 0 structured-output failures, p53-p73 boundary-exact); adding `--refine --refine-threshold 4` improved to P 0.8434/R 1.0000/F1 0.9150 (17 genuinely new refine calls, 29 reused from the shared fallback cache) -- the best F1 measured for this bundle to date, beating the pre-structured-output baseline (P 0.9500/R 0.8143/F1 0.8769). Refine's split-biased policy introduced 2 new false positives at p106/p108 (photo pair and 산정기준표+continuation, both flagged by the baseline doc as judgment-call risks) -- noted as a follow-up, not fixed this pass. 172 focused / 588 total tests pass. | tools/segment_case.py, docs/stage1-document-segmentation-plan.md, tests/test_segment_case.py | Prompt text and its version constant had drifted out of sync once already; caught before it caused a real stale-cache reuse |

| 2026-08-03 | **Stage 1 gains a deterministic text-anchor path; vision is now the fallback, not the default.** Once `ocr_extract.py` began using embedded PDF text (same-day fix), segmentation's original cost argument -- cut the bundle so OCR runs on right-sized documents -- no longer applied to born-digital bundles, and the vision proposal was measurably the weaker reader of a page whose text we already have. `text_anchor_boundaries()` derives boundaries from the publisher's own title lines (`...보통약관`/`...특별약관`/`...특약`, allowing a scope parenthetical and an ordinal suffix), suppressing a table-of-contents block by line density -- without which CASE_112 DOC_004's 6-page contents alone would emit ~60 spurious cuts. Measured against the human-approved CASE_112 baseline (DOC_003 145p + DOC_004 178p): **precision 1.0000** (0 false positives in 173 predicted boundaries), recall 0.8317 -- and 27 of the 35 "misses" are pages the HUMAN baseline cut mid-clause, i.e. over-splits this method correctly declines. On the metrics that actually gate Stage 4 it beats the human baseline outright: segments with no `제N조` anchor 11→6 / 13→8, segments starting mid-clause **19→0 and 8→0**. Article headings (`제N조`) are deliberately excluded as boundary signals -- admitting them drops precision 1.00→0.91 (21 FPs, mostly `제N조(준용규정)`), because 조 boundaries are not *document* boundaries. Cost: a 145p bundle proposes in **0.75s with 0 model calls and 0 sheets rendered** (was ~10 vision calls). Scans and partially-digital bundles return None and take the unchanged vision path; `--no-text-anchor` forces vision for comparison. Found while writing the fixtures: the TOC-continuation check chained off the *raw* flag, so a run of short single-title pages could bootstrap itself into a fake contents block and swallow real boundaries -- now chained off the resolved neighbour. `segmentation_proposal` schema: `mode` gains `text_anchor`, `render_dpi`/`sheet_pixel_budget` become nullable (nothing was rendered). 13 new tests (148 in test_segment_case.py, 2010 in tests/ pass). | tools/segment_case.py, schemas/segmentation_proposal, tests/test_segment_case.py, CLAUDE.md | Over-segmentation was breaking Stage 4 clause normalization; vision was guessing at boundaries the text layer states outright |

| 2026-08-04 | **Case type becomes an adjuster-supplied input on two axes (phase 1: additive, CASE_907-compatible).** A survey of the raw corpus (`docs/case-type-inventory-2026-08-04.md`) found the v0.1 `case_type` enum could not express what the material actually contains: **~37 of 45 source PDFs had never been intaken**, and they form two coherent series -- 개인보험1-20 (첫-party 상해후유장해, verified by reading each `I. 사정 요약`) and **TA1-16 (자동차보험 대인배상, computed on 자동차보험약관 지급기준 with 소득/과실상계) -- a coverage basis with NO value anywhere in the taxonomy, and the corpus's largest series**. Root cause of the mismatch: the enum conflated two orthogonal questions, which is why every multi-type case on record straddled in the same direction (배상책임 × 후유장해) and why `templates/registry.json`'s key `배상책임_후유장해형` was already a compound of both. Split into **`coverage_basis`** (배상책임/개인보험/자동차보험) × **`loss_type`** (후유장해/진단·수술비/실손), both adjuster-supplied at intake -- case type is not written on any document, so the adjuster's engagement-time knowledge is an input, not something to reverse-infer (the CASE_022 `correct=false` was input scoping, not reasoning). P1 is satisfied by recorded provenance (`supplied_by`), not a quote. `intake_case.py` gains `--coverage-basis/--loss-type/--non-claim-case/--supplied-by/--case-type-note` (choices derived FROM the schema, not copied -- the 2026-07-17 drift shape); `document_manifest` gains optional `adjuster_case_type`; `case_type_result` v0.2 gains the axes + `case_type_source` + `axis_cross_check` + `is_claim_case`. checkpoint 3 no longer infers when input exists but still forms an independent view and routes contradictions to review, never overriding. `기타`'s double duty split: non-claim material (CASE_030) sets `is_claim_case: false` with null axes. **Deliberately additive** -- axes optional, `case_type` still required -- so in-flight CASE_907 (at Stage 2/4, `claim_analysis` not yet run) completes unchanged; all 3 existing `case_type_result.json` and every manifest still PASS untouched. 실손 is accepted but warns at intake (no registry template -- fails at render, surfaced early by design). 52 new tests (1849 total). **Phase 2 (after CASE_907 completes): make the axes required, deprecate `case_type`, migrate the 4 existing outputs, and derive `template_id` from the axis pair** -- and unlike 실손, both new series have real completed reports on disk to derive templates from. | schemas/case_type_result+document_manifest, tools/intake_case.py, agents/claim-analysis, tests/test_case_type_axes.py (new)+test_intake_case.py, open-decisions.md, docs/case-type-inventory-2026-08-04.md (new) | See `open-decisions.md` #8/#8a |

| 2026-08-05 | **Segmentation moved after OCR and merged into Stage 2 (`feature/stage-inversion`; `open-decisions.md` #7 resolved as option (b)).** Boundaries were decided from the raw PDF -- a downscaled 4×4 contact-sheet crop for a scan -- while the text that states them outright was produced one stage later. The corpus is overwhelmingly scans (CASE_025's 110 pages and CASE_026's 59 carry **zero** embedded characters), so the deterministic path reached almost none of it, and CASE_907's DOC_005 was classified as one `diagnosis_certificate` across 19 pages holding a 진단서, two English REPORTs, an 입퇴원확인서 and two 진료비 명세서. Now: bundle OCR (`run_checkpoint1.py --bundle-ocr`, OCR only -- classification is refused because `document_type` is per-document and one label cannot fit a bundle) → bundle redaction → segmentation over the **redacted** text → split → per-child classification/redaction → chunking. `split_bundle` hands each child the bundle's pages renumbered from 1 with P8 verdicts intact, so children are never re-OCR'd; the split is strictly cheaper than before. **Option (c) (text-anchor pre-OCR for born-digital, post-OCR only for scans) was rejected on measurement, not preference:** boundaries from the embedded layer, from raw OCR text, and from redacted OCR text are byte-identical on CASE_112's 323 policy pages -- all 173, precision 1.0000 -- so two paths would have bought two sets of failure modes for no accuracy difference. Segmentation reads redacted text specifically: titles survive redaction in all 217 of CASE_112's split children, so pre-redaction page text stays behind its capability gate. Medical bundles gained their own title rule (form names through a 5-line header window, because a 별지 서식 header or field labels precede the title; letter-spaced `진 단 서`; consecutive reprints of one title are one document; a generic `REPORT` is a form KIND, not a name, and never merges) plus an **LLM tier** for pages the rule cannot settle, which splits and flags on an unusable verdict. Measured on CASE_908 (fork of CASE_907, no ground truth) DOC_005, a 19-page scan: title rule alone P 1.0000/R 0.9091; with the real claude-cli tier over the 5 pages (26%) it could not settle, **P 0.9167/R 1.0000** -- the one miss a page whose body text is absent from the supplied material, flagged by the model at the run's only low confidence (0.62). Running it found four real defects CASE_112's bundle did not contain (a 원본대조필 stamp breaking the end-of-line anchor, a 40-char limit tripped by layout spacing, `내역` vs `내역서` across hospitals, and a 7-page reprint run scoring P 0.6250). `document_segmentation` is deprecated in `run_state.schema.json` -- retained only so CASE_112's pre-merge record still validates, deliberately not rewritten, since that run really did execute split-then-OCR. 41 new tests (1973 total). | tools/segment_case.py+run_checkpoint1.py+stage_dependencies.py, schemas/run_state, pipeline.md, CLAUDE.md, agents/document-pipeline, skills/loss-adjustment-pipeline, open-decisions.md #7, docs/run-notes/CASE_908_RUN_20260805_001.md (new) | Vision was guessing at boundaries the page text states outright, and only a born-digital bundle ever had that text in time |

| 2026-08-05 | **The `raw_page_text` classification flag wired to a gate, and a latent bug in the human-review hash found by doing it.** The Stage 1/2 merge left classification able to read PRE-redaction `page_NNN.md` when no `redacted_text.md` existed yet -- deliberate (an unredacted document must stay classifiable) and recorded as `classification_text_source: raw_page_text` + `review_required`, with the schema promising "a reviewer sees that unredacted text reached an analysis stage". **Nothing kept that promise**: no finalize check read the flag, `record-human-review` had no artifact kind that could clear it, and a census of CASE_909 found 4 of 16 classifications (DOC_001-004, every top-level PDF; all 12 split children were clean, having inherited their bundle's redaction) sitting flagged with no path to resolution. A flag nobody reads and nobody can clear is not a notification. Fixed by extending the existing hash-bound ledger rather than inventing a second mechanism: new `classification_review` artifact kind (target key pinned to the one condition it clears, refusing a document that never read raw text), a nullable `human_review_uid` on `classification_result` (v0.3 -> v0.4, additive), and a `document_processing` finalize gate that refuses while any flagged classification lacks a valid, hash-current `verified` record. The reviewer's question is narrow and answerable -- does the classification's evidence quote survive into the redacted text? -- because if it does, the same label is reachable from the redacted layer and no PII was load-bearing. **Building it surfaced a real bug in the shared mechanism**: `_strip_disposition` NULLED disposition fields instead of removing them, so an artifact went from `{...}` to `{..., "human_review_uid": null}` when the UID was written -- different canonical bytes, invalidating the record the moment it was used. The two original kinds never exposed it (their artifacts always carry the keys); `classification_review` adds the key on write. Now removed rather than nulled, with `legacy_null_artifact_sha256` retained as a verification-only fallback so pre-existing reviews still verify. CASE_909's 4 reviews were re-recorded under the corrected rule after the first attempt failed exactly this way. Verified by reintroducing each defect: the nulling bug fails 6 tests, and unwiring the gate fails the one test that drives `cmd_finalize_stage` itself rather than the helper -- added specifically because every other test called the helper directly and would have passed with the gate computed but never consulted, which is the exact "implemented but not wired" shape this change exists to fix. 16 new tests (2028 total). | tools/human_review.py, tools/dao.py, schemas/classification_result+human_review_ledger, agents/document-pipeline, tests/test_classification_review_gate.py (new) | The Stage 1/2 merge created the condition; the flag recording it had no consumer and no resolution path |

| 2026-08-06 | **CASE_909 run to the D1 gate: 12/12 stages, and running it found six defects reading could not.** First full Phase 1 + Phase 2 under the merged Stage 1/2 order (RUN_20260805_002; 계단 결빙 낙상 배상책임 × 후유장해, McBride 13%, insurer denied 배상책임 while notifying ₩2,000,000 구내치료비). Stops at `evaluation`, correctly: `mark-human-review-complete` needs a genuine human action and the D1 gate refuses ground truth without it. **Every defect below was found by executing a stage, not by reading code**, and each is the same shape -- a fact the pipeline depends on, carried somewhere nothing enforces. (1) *`raw_page_text` classification flag* -- set by checkpoint 1, read by nobody, clearable by nobody; 4 of CASE_909's 16 classifications sat flagged with no resolution path. Wired to a `document_processing` finalize gate via a new `classification_review` kind on the existing hash-bound ledger; building it exposed a real bug in that shared mechanism (`_strip_disposition` NULLED rather than removed, so writing the returned UID changed the very bytes the record bound to -- invisible to the two original kinds, whose artifacts always carry the keys). (2) *superseded-bundle text* served to analysis stages though the manifest says `redacted_text_path: null` -- worse than a leak, since every page of it is also a child's page, so one page counts as two agreeing sources; consistency-check excluded DOC_005 by hand and reported it. (3) *P6's conflict gate was prose only* -- `check-conflicts-clear` was a CLI command an agent had to remember; `screening_report` finalized with `CONFLICT_1` still pending, and nothing went wrong only because it happened to be adjudicated first. Now enforced in `finalize-stage` over `CONFLICT_GATED_STAGES` (`consistency_check` deliberately excluded -- it RAISES conflicts, so gating it would deadlock). (4) *`check-untagged-claims` returned `clean` from scanning zero sections* when a known template's headings appear nowhere in the document -- indistinguishable from a real pass; denial-validation noticed its own clean result was meaningless. (5) *`verification_status: verified` was unreadable* -- it means the clause exists where claimed, NOT that the insurer cited it, and those came apart completely here (all 3 matches verified byte-exact; the insurer cited no policy clause anywhere, 0 hits for 10 spellings across 15 pages). `match_source` now required in the validation contract and checked against its upstream source, since a copied field can drift. (6) *`document_assembly.py` never verified a quote it rendered* -- 29 of the v2 draft's 190 citations were wrong (recalled rather than copied; bills attributed to the wrong document), caught only because that agent wrote its own verifier. Now checked against the processed text before anything reaches disk; all 432 shipped citations pass. **Analytical high point:** denial-validation established that the insurer transplanted 청주지법 2017가합10124 with four referents swapped while dropping the precedent's three load-bearing facts -- that site was `경사가 없는 평지`, its defendant bought AND deployed 염화칼슘·모래 including 07:43-08:26 on the accident morning, and the victim deviated from the expected route -- importing a holding without its ratio, where DOC_001 ⑦ alleges the opposite of the second. critic_v1's CF-1 (v1 dropped 3 of the claimant's 9 grounds) turned out to understate itself: it named ⑦⑧ and missed ①, which sits on the preceding page. P1 also gains a clause on stale system claims, after an agent reported a defect as live an hour after it was committed fixed. 41 new tests (2057 total). | tools/dao.py+human_review.py+_cross_contract.py+document_assembly.py, schemas/classification_result+human_review_ledger+denial_validation_result, guardrails P1/P6, agents/denial-validation+draft-report+screening-report+document-pipeline, pipeline.md, outputs/CASE_909 | Six defects, one shape: enforced in prose, unenforced in code |

| 2026-08-06 | **`screening_report.review_points` given the structure P3's aggregation point needs -- after a redundant tool was built and reverted.** The session began by building `dao.py collect-review-flags`, on the premise that nothing read `review_required`. That premise was wrong: `review_point`'s schema `$def` is literally *"A point flagged for expert review"*, carries the same `reviewer_role` enum, and has been populated on **all 6 cases that reached the stage** (CASE_021 6 / CASE_024 6 / CASE_112 17 / CASE_907 10 / CASE_908 10 / CASE_909 10), routed across all three roles. P3's aggregation half was built; it was the *raw contract field* that had no reader. Building a second list beside a working one only adds a way for the two to disagree -- the 2026-07-17 forbidden-expression drift shape -- so both commits were reverted to a byte-identical tree and the real array was improved instead. **Three gaps, each measured on the shipped corpus.** (1) *No id.* Critic findings have carried `finding_id` from the start and `expert_review.json` disposes of each by `finding_ref`; a review point had none, so a reviewer's answer had nothing to bind to and nothing could later say which points were addressed -- added `point_id` (`^RP-[0-9]+$`, deliberately not the critic's `CF-` namespace). (2) *No source link.* On CASE_909, 5 of 10 points named no document at all in prose and **not one** named its source machine-resolvably, so nothing could check whether a flagged upstream contract had been surfaced -- added `source_refs` (`{contract, element_id}`, where `element_id` addresses `REQ-3`/`DR_1`/`PM_2`/`CONFLICT_1`; empty stays legitimate for a point from the agent's own reading, since forcing one would make it invent a source). (3) *No way to say answering changes nothing* -- added `answerable`, whose description names the preferred alternative rather than permitting a drop: raise the DOWNSTREAM CONSEQUENCE, as CASE_909 did by routing a redaction-blanked 사고일자 as the premise of 보험기간 내 사고 여부·소멸시효 rather than as "a field is null". **The one finding worth keeping from the reverted work is a routing rule, now in the schema and pinned by a test**: a contract's own `reviewer_role` is a coarse default agents rarely vary -- every one of CASE_909's 14 contract-level flags read `손해사정사` -- while the questions behind them split 법률전문가 4 / 손해사정사 5 / 의사 1, because whether a McBride rating is medically sound is a 의사 question no matter which contract raised it. Per-question routing wins, and the agent spec now says to sweep upstream flags, **merge rather than transcribe one point per flag**, and set priority itself. All three fields optional, `v0.2 -> v0.3`: the 4 valid shipped reports still PASS untouched (CASE_021/CASE_024 fail only on the 2026-07-22 `insurer_position` split, confirmed pre-existing on a clean tree and named explicitly in the test rather than blanket-skipped). CASE_909's report deliberately **not** backfilled -- hand-filling `source_refs` would be me guessing at an agent's judgment, not a run. 18 new tests (2077 total), each verified by reintroducing its defect. | schemas/screening_report, agents/screening-report, tests/test_review_point_schema.py (new) | A tool was built before its schema was read; the array that already did the job needed structure, not a competitor |


| 2026-08-04 | **Policy-clause normalization becomes opt-IN, and a split denial/acceptance outcome becomes expressible.** Two independent gaps, both found by RUNNING the pipeline on CASE_907 rather than by reading it. (1) *Normalization.* The LLM polarity layer cost 813 provider calls per 145-page bundle and **never once completed** (batch died from job 35 on timeouts; CASE_112 was 203 required / 0 produced and cleared only by `MANUAL_OVERRIDE_NO_REAL_SNAPSHOT`) -- while nothing downstream consumed its output: `claim-analysis` and `denial-response` address a clause by `document_id` + page + verbatim quote, never by which bucket a condition was filed under. Layer deleted; `insurance_policy` now defaults to `text_only_no_normalization` (processed, redacted, chunked, citable, owing no clause contract) and `dao.py promote-policy-document --disputed-by` opts a specific disputed document IN, cascading the policy stage back to `failed` so it re-finalizes against the new obligation. Making that default *usable* took three more fixes, each a real defect: `policy-snapshot` refused to issue without a clause contract (the digest never needed one -- a missing contract hashes as JSON null, a distinguishable state, so the gate moved to text-processing and staleness detection was re-verified intact); `matched_clause_ref`/`clause_ref` accepted only a `clause_uid`, so `claim-analysis` FOUND three real clauses in DOC_004 (p11/p35/p38) and had to record `null` for all three -- schemas now take a source form `{document_id, page, quote}`, verified byte-for-byte against the processed text, with `null` narrowed back to 'no clause found'; and `_downstream_policy_ref_errors` cached per-document while the verdict depends per-reference on the addressing FORM, so one UID-form failure failed every source-form reference on the same document. (2) *Split outcome.* `decision_type`/`payment_status` were always scoped to 'one claim coverage', but nothing recorded WHICH -- so CASE_907's insurer denying 배상책임 while notifying 구내치료비 ₩2,000,000 could record only the denial, and `payment_status: unpaid` read as 'nothing was paid'. Added `decided_coverage` + `accepted_coverages` (an acceptance has no `decision_type`, so it cannot be a reason), with acceptance-side `policy_matches` held to the identical verification and `upstream_hash` folding acceptances in only when non-empty -- CASE_112's two downstream contracts keep their exact recorded digest. **Live run through Stage 7 gate:** claim_analysis / denial_response / consistency_check all `passed` on the real case, every clause reference source-addressed and verbatim-verified (I re-checked all of them independently). Two agent judgments worth keeping: `denial-response` REFUSED my briefing's claim that ₩2,000,000 was *paid* -- the source says 기 안내 (notified) -- and recorded `payment_status: unknown`; `consistency-check` raised the project's second-ever P6 conflict on 사고 장소 규모, which I adjudicated `resolved` under user delegation (the two statements address different units -- DOC_001 the stairs, DOC_002 the whole facility -- so both can be true; the unit-switch itself was handed to denial-validation as a live issue, not buried). 19 new tests (1849 total). | tools/dao.py+_cross_contract.py, schemas/denial_reason_result+coverage_result+requirement_matching_result+document_manifest, agents/denial-response+denial-validation+claim-analysis, tests/test_source_addressed_policy_refs.py (new)+test_denial_decision_contract.py, pipeline.md | Normalization had never completed on any case; running the pipeline surfaced what reading it had not |

| 2026-08-04 | **The three CASE_907 findings whose common shape is "a guard on one path, nothing on the other" -- closed** (`docs/pipeline-findings-2026-08-04.md` §1/§2-#4/§10-2). (1) *Search asymmetry.* `_cross_contract._normalize_ws` made proving a quote PRESENT whitespace-robust, but proving a term ABSENT had only raw substring search -- and Korean legal text spells one term several ways (`직접청구`/`직접 청구`) with mid-word line breaks on top (722 in DOC_004 alone), so a raw search returns 0 hits for a term printed on the page. It did, three times in one day, once recording a **fabrication finding against a correct draft sentence** (the policy DOES grant 직접 청구, DOC_004 p15 보통약관 제12조 -- item 38's worked example is now corrected; its structural finding stands and is sharpened, since an untagged paragraph is where a *true* claim goes unverified too). New read-only `dao.py search-document-text CASE_ID DOC_ID <term> [--all-docs]`: NFKC + all-whitespace-removed matching, reporting each hit's **source** spelling, page, and context, plus `searched_normalized` so a 0-hit result documents the search instead of asserting a conclusion. Hits are candidates, never verdicts -- folding can join words, so context is the caller's to judge; an unreadable document is `documents_unsearched`, never a silent 0. Building it found a real bug in itself: normalizing the page wholesale misaligned spans (NFKC can change length), reporting `연현상으` for a `자연현상` match -- fixed by mapping each raw char to the normalized run it produces, verified 0 boundary-spanning artifacts across 710 real hits. `critic` must now use it before any absence claim and state which variants it searched. (2) *Acceptance-owned matches.* `check_denial_validation_result` built `owner_of_match` from `denial_reasons` alone, so an `accepted_coverage`'s match could not be filed under a reason (`^DR_[0-9]+$` rejected it) and raised **nothing when omitted** -- the contract reported full verification while a match was never checked, the exact failure its own message names. `acceptance_match_validations` (keyed `^AC_[0-9]+$`, reusing `policy_match_validation`) added rather than widening `reason_id`, because a `validation` also carries a `verdict` on the insurer's *reasoning* and an acceptance has none. Ownership now enforced both directions + omission caught. Verified on shipped data, not fixtures: CASE_112 clean, **CASE_907 fails on exactly AC_1/PM_3** -- the real unverified match (needs rewriting before that stage re-finalizes). (3) *Scanned answer key.* D1 exempts evaluation, but the OCR pipeline is closed to ground truth and the Read deny-glob closed direct opening, leaving a scan unreadable by any sanctioned route -- which forced the CASE_907 bypass (render to `.tmp/`, read there: escapes a *path*-bound glob, no gate, no record, evaluator contaminated). `read-ground-truth --transcribe` renders to a `TemporaryDirectory`, vision-transcribes, and removes it in a `finally`. The `_gt_cache/` alternative was rejected for recreating the same persistent-path-plus-convention that had just failed; the value is not access (the bypass had access) but that access is gated, attributable, and leaves nothing to leak. Real 12-page scan end-to-end: exit 0, 0 leftovers, nothing in `data/processed/`. 28 new tests (1889 total; one pre-existing lock-timing flake in `test_dao_replace_manifest.py`, confirmed unrelated -- passes clean-tree, isolated, and on re-run). | tools/dao.py+_cross_contract.py, schemas/denial_validation_result, agents/critic+denial-validation+evaluation, tests/test_dao_search_document_text.py+test_acceptance_match_validation.py+test_dao_ground_truth_transcribe.py (all new), known-gaps items 38/39/40b | Running the pipeline surfaced three instances of one shape: a guard on the sanctioned path, nothing on the alternative, and no detector for the difference |

| 2026-08-04 | **The untagged-claim floor (findings §3 / known-gaps 38).** `read-evidence-tags` compares the tags PRESENT against the sidecar, so a paragraph with zero tags is in neither set and is structurally invisible -- making an untagged fabrication *less* detectable than a badly-tagged one. New record-only `dao.py check-untagged-claims DOC_PATH --template KEY` floors it the way `check-forbidden-expressions` floors the semantic P3 pass. Scope is declared by a new `analytical_heading_patterns` in `templates/registry.json`, not hardcoded -- section structure is already the registry's job (open-decisions #2), so a template that renumbers its analysis sections updates one file; an unknown key returns `NO_ANALYTICAL_SECTIONS` rather than falling back to a whole-file scan that would read as "the check ran". **Measuring against the real drafts corrected item 38's own recommendation:** the "cheapest" keyword rule it proposed would have caught only ONE of the two real instances -- CASE_907's IV-1-나 3) restates the insurer's argument uncited and contains no statutory word at all. Hence two signals: `statutory_ref_no_citation`, and `untagged_among_cited_siblings` (an untagged numbered item whose siblings at the same indent are cited -- a list whose other items all cite evidence declares by its own structure what the uncited one owes). That second signal is what catches the instance critic v1 missed entirely. Two real bugs found while measuring, both fixed: the exemption list was line-wide, so `위 1항` suppressed the CF-1 line itself -- the single most important hit -- and exemptions now never override the statutory signal; and a bare `- 나. 특별약관의 적정성 여부` sub-heading was flagged as a claim (CASE_021 v2) while the same marker introducing a full sentence legitimately is one. Measured on every draft on disk: CASE_907 v1 3 / v2 2, CASE_021 v1 0 / v2 3. `critic_result` gains optional `untagged_claim_candidate_count` (additive, matching `forbidden_literal_hit_count` -- pre-2026-08-04 outputs still validate); `critic.md` runs the tool, records the count, and promotes only what survives its own reading -- a non-zero count with no finding is valid, skipping the tool is not. Deliberately not attempted: deciding in general which sentences owe evidence; the tool sees two shapes and the critic remains the ceiling. 16 new tests (1905 total). | tools/dao.py, templates/registry.json, schemas/critic_result, agents/critic, tests/test_dao_untagged_claims.py (new) | The floor existed for forbidden expressions but not for the citation gap that had already produced one false fabrication finding and one real miss |

| 2026-08-04 | **`known-gaps.md` reference integrity, enforced rather than tidied (findings §6).** Items are cited by NUMBER from this changelog and `open-decisions.md`, but two numbers (20, 21) named two items each and one item had no number at all -- so a citation was ambiguous and one item was uncitable. Broken by renumbering **the side nothing cites**: all live references to "item 20" mean the 2026-07-22 denial-family item, and item 21 had no external reference at all, so the Part 11A/11B items moved to 41/42 (the Part 11 series ran 11A→20 … 11J→29, colliding with the earlier run) and the unnumbered item became 43. The three headers with no status token (10, 12, 18 -- tool-addition and review records) got one after reading their bodies: RESOLVED / RESOLVED / PARTIAL. **Found while auditing: three headers contradicted their own bodies** -- items 38/39/40 were fixed earlier the same day with only the body updated, so the header still said OPEN above a body announcing the fix, which is worse than no status because it is confidently wrong. A one-time cleanup would just restart the drift, so the invariants are now tests (`tests/test_known_gaps_index.py`): unique numbers, every item numbered, every header carrying a status token, no header marked OPEN above a body announcing a fix, the new "Still open" index table agreeing with the headers, and every `known-gaps.md item N` citation resolving. Each test was verified to actually fail by reintroducing its defect. 43 items, 43 unique numbers, 10 genuinely open. 6 new tests (1911 total). | known-gaps.md, tests/test_known_gaps_index.py (new) | Numbers are the citation key; nothing checked them, so collisions accumulated silently and a same-day fix left three headers stale |

| 2026-08-04 | **`core_field_accuracy` split into fact-extraction vs discretionary agreement (findings §9).** One number was silently measuring different things depending on what the answer key happened to be: a 손해액 산정서 ground truth puts normative values (과실률, which figure counts as 손해액, an income basis substituted under 입증 곤란) into the denominator, while a 진단 case's key holds almost none. Each `field_comparisons` entry now carries `field_kind` (`fact` -- stated in the case material, a mismatch is a misread / `discretionary` -- arrived at by judgment), and `fact_extraction_score` + `discretionary_agreement_score` sit alongside the blended `score`. Additive: `field_kind` and both sub-scores are optional, so pre-2026-08-04 files validate untouched, and unclassified comparisons are counted separately rather than guessed into a bucket -- inferring one would move the very numbers the split exists to keep honest. `dao.py split-core-field-accuracy` computes them (read-only) instead of hand-arithmetic, which the CASE_112 notes already record getting wrong once. **The split inverts the ranking:** CASE_112 0.75 headline / **1.00 fact (12/12)**, CASE_907 0.80 / **1.00 (8/8)** -- the two lowest headlines have perfect extraction, and discretionary agreement is **0/9 across every case**, a systematic result invisible in the blend (and mostly the P3-mandated refusal to assert, i.e. scoring 0 for being right). Backfilled all 6 shipped results by READING each comparison, not pattern-matching names -- two keyword guesses were wrong and corrected. **Two errors surfaced by the reconciliation check:** `CASE_024/evaluation_result_v1.json` recorded 0.7 for 6 matches of 10 (corrected to 0.60, hand-verified); and the 기왕증 case cited everywhere as "CASE_022" **does not exist on disk** -- it is `CASE_024`, so that case had three different numbers in circulation (0.875 in docs, 0.7 in the file, 0.60 actual) and the widely-quoted 0.875 has no file behind it. §9's own "0.80 reads worse than 0.875" therefore rested on a value that was never real. 8 new tests (1919 total), including one that reconciles every shipped result against its own comparison list. | schemas/evaluation_result, tools/dao.py, agents/evaluation, outputs/CASE_{021,024,112,907} (backfill + CASE_024 score fix), tests/test_eval_accuracy_split.py (new) | A blended accuracy number made input-scoping and judgment differences look like extraction failures, and nothing checked the headline against its own field list |

| 2026-08-04 | **Run-state timing made honest, scoped to start/end legibility (findings §4); template per-coverage slot deferred with recorded grounds (§2-#3).** `started_at`/`completed_at` measure when the ORCHESTRATOR moved a marker, not how long a stage took -- CASE_907 showed all four failure modes (998 minutes for a stage retried overnight, a null start, two overlapping spans, 0.0 for a stage finalized right after the marker moved). **Deliberately NOT built: real work measurement.** That needs instrumentation at the call sites, and adding fields without it would only produce another inaccurate number that looks authoritative. Built instead: `current_attempt_started_at` (overwritten each `in_progress`, so a retried stage's latest run is distinguishable from its first -- `started_at` keeps the origin and is why the overnight gap read as duration); a `timing_note` the DAO sets when a stage reaches a terminal status with no start marker ever recorded, because a hole reads as missing data when the truth is that the marker was never moved and no start can be invented afterwards; and field descriptions stating plainly that these are marker times, carrying the 998-minute counter-example and an explicit "do not use as a cost figure" -- a test fails if that warning is ever dropped, since §4's real risk is a wrong number justifying a design decision. Overlapping spans left alone: as a record of marker movement they are accurate, and "were these concurrent" is not a question this field can answer. Both new fields additive; every pre-existing run-state validates unchanged. Noted, out of scope: 5 `_run_state.json` files fail schema on `backup_path: null` for skipped stages, confirmed pre-existing on a clean tree. **§2-#3** (변형 A has no per-coverage split slot; CASE_907's insurer denied 배상책임 while accepting 구내치료비) kept as-is per user decision, with grounds recorded in `open-decisions.md` #2: the prose workaround is verified (critic v2 found no dependency misreading) and the split IS structurally recorded upstream, so no output states anything false -- unlike the other three instances of that propagation shape, which are all now closed. The slot is not added because `templates/` was extracted from 4 real completed 손해사정서, none of which splits its assessment section per coverage; adding one would emit a shape working adjusters do not produce. To resolve: ask a 손해사정사 how a split outcome is actually laid out. 7 new tests (1926 total). | schemas/run_state, tools/dao.py, open-decisions.md, tests/test_run_state_timing.py (new) | Timing numbers were being read as durations they never were; the template gap is a domain question, not a defect |

| 2026-08-04 | **The new checkers verified by RUNNING them, not by reading them** -- `critic` re-invoked on CASE_907 `draft_report_v1.md` (RUN_20260804_016, written to `critic_result_v1_rerun.json`; the original `critic_result_v1.json` kept untouched as a control and confirmed unread by hash + mtime). The draft has two known real instances, so it is a case with an answer key for the checkers themselves. **The false-fabrication finding did not recur.** The original pass searched `직접청구` raw, got 0 hits, and recorded draft line 154 as `fabrication_unlinked_claim` -- a correct sentence called an invention. This pass searched six normalized variants across all five documents via `search-document-text` (`직접청구`/`직접 청구`/`직접청구권`/`손해배상청구권자`/`제724조`/`724조`, `documents_unsearched` empty), found the clause really does exist at DOC_004 p15 제12조 제1항, and recorded the defect as a **missing citation, not a fabrication** -- adding the nuance that 상법 제724조's only occurrence (DOC_001 p6) sits *inside a quoted precedent*, so the eventual tag must distinguish that. It also caught line 127, which the original pass **missed entirely**, via `untagged_among_cited_siblings` -- the signal added because the keyword rule known-gaps 38 originally proposed cannot see it. Discipline held where it matters: `untagged_claim_candidate_count: 3` recorded but only 2 promoted to findings (line 115 correctly left as a candidate), which is the "hits are candidates, not verdicts" contract working. Both of its load-bearing factual claims were re-verified here independently rather than taken on report. **One new finding the tag-consistency check structurally cannot see:** draft line 150 asserts the insurer cited no policy clause and anchors it to `[E61]`, whose quote is merely DOC_002's *title line* -- tags and sidecar agree perfectly (orphaned 0 / unused 0) while the claim and its evidence do not connect. The agent also volunteered that four spellings do not exhaust the space and the prose should state its search scope -- the §1 discipline generalizing on its own. No code changed: this pass confirms existing behaviour. | outputs/CASE_907/critic_result_v1_rerun.json (gitignored), no code changes | Specs had been updated to require the new tools, but no agent had ever actually been observed following those instructions |

| 2026-08-11 | **T13 closes the analysis-agent timing blind spot without relabeling it as reasoning.** Explicit orchestrator `in_progress` transitions now emit one idempotent stage-attempt start; successful P10 finalization or explicit failed/partial close emits the terminal marker. Contract checkpoints only ensure progress and no longer inflate retry counts; the segmentation manifest replacement no longer finalizes `document_processing` halfway through the stage. `aggregate-trace` pairs the cross-process wall markers into separate `stage_attempts`/`by_stage`/`stage_coverage`, subtracts human-wait and the union of observed tool intervals, reports the remainder as `unattributed_active_s`, and fails closed when stage windows overlap or markers are incomplete. Ordinary T0 self-time rollups remain unchanged. | tools/trace.py+trace_aggregate.py+dao.py, timing_summary schema, pipeline skill, policy agent, tests | Existing traces covered Python tools but had no whole-attempt observation for claim_analysis/consistency_check/other subagents; run-state marker times were explicitly unsafe as cost figures |


| 2026-08-14 | **Stage 2 stops materializing child PDFs, and dispatch boundaries become measurable.** (1) *Child PDFs.* CASE_142's raw directory went from 11.4MB to 44.0MB because every single-page child of the 19-page DOC_005 weighed 1.34MB -- the same as the whole parent -- page selection keeps the parent's unreferenced fonts and images and `save()` was not asked to collect them. Nothing ever opened one: under the post-2026-08-05 order the bundle is OCR'd and redacted BEFORE segmentation, children are handed the parent's pages, and `run_document_stage.select_documents` skips any entry whose `ocr_status` is already completed -- verified by running it, which returns an EMPTY list on CASE_142's manifest. The write is now conditional on the same record `_redistribute_parent_ocr` keys off; the split-first order (where the child IS the OCR input) still writes them, now with `garbage=4`. Such a child stays `document_role: physical` with a null `file_path` -- it IS a page range of a real file, named by `source_file_name` + `source_page_start/end`. Deliberately NOT `document_role: segment`, whose contract needs a DAO-issued `page_map` verified against the parent PDF page by page and which a scan cannot satisfy at all (an `ocr_segment` gets no receipt, since UID verification may not re-run OCR). **The null exposed a real regression, caught by running not reading:** `registered_source_pdf_sha256` had nothing to hash, so `record-source-digest` returned 'no readable registered raw source' and blocked `enable-canonical-uids` -- the exact sequence CASE_142's policy stage ran. Fixed by resolving a fileless child to the parent that owns its bytes; siblings share a digest because they ARE the same file, and the page range is what separates them. `_raw_source_path` deliberately did NOT get the same fallback: its callers open the PDF to read specific PAGES. Measured on CASE_144 (fresh intake of the same source, single clean run): **43.99MB -> 11.40MB**, 22 documents and 367 chunks both unchanged, type distribution identical, 281s -> 261s. (2) *Dispatch boundaries.* A stage's marker window minus the agent's reported duration left 578.3s unexplained across nine CASE_142 stages, of which finalize and snapshot -- the obvious suspects -- were **2.79s**. New `dao.py record-dispatch` records the interval from asking for work to holding its result, with `--agent-reported-s` so round trip stays visible instead of collapsing into one number. `dispatch` is a new span category EXCLUDED from tool intervals: it CONTAINS the agent's work, so folding it in would report model thinking as observed tool time. (3) *A cascade now closes the attempt it ends* -- `enable-canonical-uids` demoted an `in_progress` stage without emitting a marker, leaving the attempt open forever and the run at incomplete coverage. | tools/segment_case.py+dao.py+trace.py+trace_aggregate.py, schemas/document_manifest+timing_summary, tests | Running the pipeline end to end surfaced three things reading it had not: 32.6MB of files nothing opens, a residual nothing explained, and a normal invalidation indistinguishable from a crash |
| 2026-08-14 | **The medical-appropriateness gate restored, cross-platform, and scoped forward.** Merge `3569d50` discarded parent2's `dao.py` wholesale (known-gaps 48); `medical_repository.py`/`medical_review_ledger.py`/`medical_contracts.py` (5,443 lines) survived intact but called 18 DAO symbols that no longer existed, so the whole flow was dead code. **Nothing failed, which is the problem:** a census found **53 cases on disk, zero medical artifacts, 9 with `claim_analysis` `passed`** through a gate that was not running -- and `claim_analysis` is gated regardless of `medical_review_adopted`, because the medical variables are an INPUT to it. `medical_review_adopted` was written by three call sites and read by NONE: the same "flag nobody reads" shape as the 2026-08-05 `raw_page_text` flag. **Parent2 could not simply be restored:** it does `import fcntl` unguarded, and `fcntl` does not exist on Windows (`os.supports_dir_fd` is empty here, `O_DIRECTORY`/`O_NOFOLLOW` absent) -- the other developer's environment is Linux, which is why the divergence was invisible on their side. That constraint was not previously recorded anywhere. Restored instead: 4 config constants, 5 path/revision helpers, 6 write-layer functions + `AtomicWriteCommittedError`, 12 `cmd_*` commands with argparse registrations, and 3 lock functions as a **shim over this branch's `O_EXCL` model** rather than a port of parent2's `flock` model -- the kernel-lock decision stands, the handle is opaque at all four call sites, and nothing here forks workers so the fork-safety those semantics bought protects nothing in this codebase. `dir_fd`-relative symlink guards are re-expressed with `lstat` (**a narrower window than a pinned handle, not an equivalent one** -- stated in the code, not glossed); directory-fsync durability is genuinely POSIX-only and reports False rather than silently skipping. **Gate scoped FORWARD by user decision:** `run_state` gains `medical_gate_status`, `enforced` for run states born after the restore and `never_evaluated` stamped once on every pre-existing one -- not a pass and not an exemption, but a record that the question was never asked, which `medical_review_adopted: false` alone cannot distinguish from "asked and not required". Retro-enforcing would fail finished runs, several with recorded evaluation results, for a check that was not running when they executed. **Wired at TWO independent points** (`_update_run_state` and `_finalize_stage` -- separate routes to `passed`; finalize does not delegate), each verified by deleting it and confirming exactly one test fails. That mattered: the finalize test first passed with the gate deleted, because the command refused for an unrelated policy-layer reason and the assertion was only on the return code -- it now asserts the medical reason. Also closed: the medical-ownership guards had no consumer, so `write-text` could replace `medical_variables.json`/`extracted_claim_fields.json`/a `_medical_variable_revisions/` file (`_PROTECTED_CONTRACT_FILES` matches basenames only and cannot see the last), and `read_contract_data` -- the in-process path pipeline tools import -- could read protected state the CLI refused. 139 -> 104 suite failures, **35 newly passing, zero regressions**, 30 new tests. Remaining 11 medical failures are test-environment limits (5 `os.symlink`/WinError 1314, 1 `os.mkfifo`, 3 injecting into a directory-fsync Windows never performs), expected to pass on Linux. | tools/dao.py, schemas/run_state, known-gaps.md, tests/test_medical_gate_restoration.py (new) | A flag nobody reads is not a gate; 53 cases passed a check that had not existed since the merge |
| 2026-08-15 | **Policy clause normalization retired, and the policy stage stops being dispatched.** `policy_clause_processing` spent **370.9s** on CASE_142 doing **0.69s** of work -- finalize, snapshot and locks, with zero provider calls and zero output files. Its span timeline has 75s and 91s gaps containing no tool activity at all: an agent reading the case to conclude there was nothing to do, when the manifest states that outright. Two attempts, because `enable-canonical-uids` fired at 09:34:09 *inside* the stage and demoted it mid-run -- the preflight that exists to move that outside the attempt was never called. **Grounds for retirement are the shipped corpus, not a projection:** 211 policy documents across 4 pre-2026-08-04 cases carry `automated_text_pipeline` and **5 clause files were ever produced** (2.4%), all in CASE_030, whose policy stage is `failed` regardless; CASE_112 marked 203 and produced 0, reading `passed` only through a hand-edited `manual_override` that says so in its own text. A gate nothing can satisfy is not satisfied, it is bypassed, and the bypass is what ends up in the record. Normalization also never replaced source addressing -- a normalized clause still carries `evidence_references: [{document_id, page, quote}]` internally (CASE_030 DOC_004 `CI-a5a6241c6e63996f`), so the UID layered on top of the addressing it was meant to supersede; it classifies clauses, it does not select them, and selection is what the analysis stages actually spend time on. **The census also corrected the premise:** the 211 are not an unwired feature but a clean 2026-08-04 cutoff -- every case created before carries the value, every case after carries none, and CASE_133 (2026-08-12, **174** policy documents, all `text_only_no_normalization`) is the control at CASE_112's scale. So step 1 of the plan, "fix the default", was already done; the enum value stays readable because four legacy cases record it and CASE_112 is still forked from. **P0-8's table gate went with it, and that was the non-obvious half.** Mid-change it was moved to the wider text-processed scope, on the reasoning that "does this PDF contain an unaccounted table" survives independently -- which is true of DETECTION and false of the GATE: the gate demands every candidate be dispositioned into a `reference_table` contract, and the stage that wrote one is gone. Measuring settled it: on CASE_142's 323 born-digital policy pages the detector finds 55 candidates and extracts all 55 cleanly, but 24 are single-row and 44 two-column -- term-definition boxes (`치료비 | 치료비라 함은...`) and blank intake forms (`시설명세 | 명칭: 용도:`) that are tables only typographically. Seven have four or more rows; exactly one is substantive (DOC_010 p13's 지연이자율표). **Detection is worth keeping and is deliberately NOT retired**: it is the only thing that recovers row/column structure from a born-digital policy, whose embedded-text extraction flattens that table into `기 간 / 지 급 이 자 / 지급기일의... / 보험계약대출이율` and loses which value belongs to which period. Ironically the scans are better served -- vision OCR reproduces a 진료비 세부산정내역 as a 10-column pipe table -- so the loss is specific to the born-digital path. That belongs in a derived index, planned with the clause index (26 of CASE_142's 29 `search-document-text` calls ran in one stage window and **13 exited 0-hit**). **Found by running, not reading:** emptying the normalization scope silently switched the table gate off, surfaced only by a name-level failure diff against a stashed clean tree -- 19 new failures the targeted runs had reported as green. Measured after: preflight 0.83s + attempt 1.01s + driver 3.77s + finalize 1.24s = **6.9s**, real P10 snapshot on disk, no override. 104 suite failures, unchanged from baseline, zero regressions. | tools/dao.py+policy_roles.py+run_policy_pipeline_driver.py, agents/policy-pipeline+denial-response, skills/loss-adjustment-pipeline, tests/ | A stage that dispatched an agent to be told there was nothing to do, guarding an obligation no stage could discharge |
| 2026-08-16 | **Token counts recorded, because tool spans do not explain an agent stage.** Measured on CASE_022: `claim_analysis` spent **1.20s in tools across 773.0s of wall (0.15%)**, `denial_response` 9.12s across 483.9s (1.89%) -- while token volume tracked wall time closely in both (**277** and **327 tok/s**). Nothing recorded that, so the only figure on disk was `unattributed_active_s`, which T13 states explicitly is *not* a claim about model reasoning. Reading it as one is precisely what produced the document index: built because CASE_142 showed "13 of 29 search calls exited 0-hit", when those 29 calls cost **0.15 seconds** and there was never time there to recover. `record-dispatch` now takes `--input-tokens`/`--output-tokens`/`--total-tokens`/`--tool-uses`, harness-reported exactly like `--agent-reported-s`: this process never sees a model's context, so it records what it is told and derives nothing. A total BELOW input+output is refused as a transcription slip; ABOVE is allowed, because cache-read and cache-creation tokens belong to neither named component -- refusing that would reject the common case. Counts only: an int cannot carry prose past `_clean_attrs`, pinned by a test that puts a Korean prompt string on the same span and confirms it is dropped and counted. Aggregated per stage under the null-not-zero rule the round trip already uses (0 tokens would read as a stage that did no model work, when what is missing is the count), with `dispatch_tokens_per_s` derived over the stage's SUMMED dispatches rather than averaging per-dispatch rates, which would weight a short retry equally. **The larger finding was that the command had no caller.** `record-dispatch` shipped 2026-08-14 with tests, schema support and a full docstring, and a census found **zero dispatch spans on disk across every case** and no reference in any agent spec, skill or driver -- the same shape as the 2026-08-05 `raw_page_text` flag and the `medical_review_adopted` flag written by three call sites and read by none. Wired into the pipeline skill's T13 lifecycle, between agent return and `finalize-stage`. Each guard verified by reintroducing its defect; **one test had to be corrected to be worth having** -- the null-not-zero assertion only drove the accumulator path, so defaulting the `setdefault` path to 0 left it green, and it now exercises a dispatched AND an undispatched stage. Additive: every shipped `_timing_summary.json` still validates untouched (`stage_rollup` sets `additionalProperties: false`, so the fields had to be declared or every summary would fail). 8 new tests. Pre-existing and unrelated: `test_trace.py`'s CLI-configures-tracing test fails on a clean tree for `provider_smoke.py`/`run_policy_preflight.py`. | tools/trace.py+trace_aggregate.py+dao.py, schemas/timing_summary, skills/loss-adjustment-pipeline, tests/test_dispatch_boundary_timing.py | Wall time was being attributed by inference; the one command that could have measured it had never been called |

| 2026-08-16 | **A permission prompt is not model work, and an unapproved medical policy cannot enforce an unclearable gate.** Both found by RUNNING the first real measurement (CASE_027, fork of CASE_142). (1) *Contaminated rate.* `denial_response` recorded 862.2s and 170 tok/s; **506.2s of that -- 59% -- was a permission prompt**, so the stage actually ran at **411 tok/s**. A `human_wait` category already existed and `compute_sla` already subtracted it, but a prompt raised INSIDE a dispatch emits no span, so the summary read `human_wait_s: 0.0` while 506 seconds of waiting had happened. `record-dispatch --human-wait-s` subtracts it before any rate and emits a real `human_wait` span so the existing arithmetic applies rather than a second mechanism growing beside it; refused when it exceeds the dispatch, which would make work time negative. (2) *A gate nobody could clear.* `_medical_clearance_required` gates `claim_analysis` **unconditionally** (Rekhet 2026-08-10: the medical variables are an INPUT to that stage) -- preserved unchanged -- while the shipped operator policy is `operations_enabled: false`/`actors: []`/`approval: null`, which is **not a defect but the approved state**: `docs/medical-appropriateness-screening-deferrals.md` calls itself the *activation guard* and all **18 deferrals remain open**. `record-medical-referral-decision` is the only route to the non-blocking `do_not_refer` state and authenticates through `operator_auth`, which fails closed on that policy -- so an enforced run blocked `claim_analysis` **forever, for every case, with no action any human could take**. A gate nothing can satisfy is not satisfied, it is bypassed: the failure that retired clause normalization on 2026-08-15. `_medical_gate_applies` now also requires the policy to be approved, read with the **identical rule its own consumers use** (`operations_enabled is not True or approval is None` -- `medical_contracts`, `medical_review_ledger`, `operator_auth`); the register says configuration presence alone cannot ACTIVATE behaviour, and the converse has to hold too. Fails closed toward unapproved on any read error. **Defers enforcement, does not remove it** -- approving the policy restores the gate with no code change, pinned by a test that fails if that property is lost; the 53 pre-restore runs stay ungated either way. The five 2026-08-14 tests run under an autouse fixture that approves the policy, so they keep measuring the gate's own logic instead of passing vacuously once it stops applying. **Measured result:** rate is stable across four dispatches on two cases (411/316 here, 327/277 on CASE_022) while tool time is 0.16%/0.28% of work -- wall time is token volume over a near-constant, which closes the question the document index was built on. Phase 1 through `claim_analysis` is **984s of work against a 5-10 minute target, and `claim_analysis` is 70% of it**. Still unmeasured: the input/output split, since the harness reports one total and the agent cannot see its own counts (claim-analysis self-counted 17 tool calls against a harness-reported 70 -- exactly why the orchestrator records this). **Found and NOT fixed:** `medical_review_adopted` survives a FAILED publication, because `publish()` sets it before the validation commit point (deliberate -- a crash must not make P11 vanish); on CASE_027 it flipped `check-medical-reviews-clear` from exit 0 to exit 1 with an identical JSON body, contained only by the same-day gate fix. 14 new tests, no regressions (11/11 and 18/18 against clean-tree baselines taken by file copy). | tools/dao.py+trace.py+trace_aggregate.py, schemas/timing_summary, skills/loss-adjustment-pipeline, tests/test_medical_gate_policy_activation.py (new)+test_dispatch_boundary_timing.py+test_medical_gate_restoration.py | The first number the instrumentation produced was wrong, and the stage it was meant to measure could not have run at all |
| 2026-08-16 | **Claim-analysis turn budget: the stage's wall time is its TURN count, and batching reads bought 22-35% without touching what is read.** The CASE_027/028 A/B left on disk read as a negative result for page-narrowing -- arm B (per-page-group policy reads) ran 818s/83 tool uses/241,252 tokens against arm A's 692s/70/218,589 -- and the per-turn arithmetic explains it: both arms cost ~9.9s per tool use, so 13 extra turns ate the payload saving. Arm C (CASE_029, same stage-cut fork of CASE_142's processed state) ran under a spec that mandates ONE grouped `read-redacted-text-bundle` for all short documents, ONE narrowed `--pages` read per policy document, and no re-reads: **528.2s agent-reported / 47 tool uses / 201,062 tokens / 353 tok/s** -- 578.5s attempt-active vs 740.3 (A) and 883.2 (B), semantically equivalent contracts (same axes/template/KCD/13 requirements at 6 met/0/7 uncertain; one standardized-name label differs). Four more P0s shipped alongside: (1) `check-medical-reviews-clear` now applies `_medical_gate_applies` -- the same rule as the transitions it fronts -- instead of failing closed on a missing ledger while finalize passes; the waiver is deliberately narrower than the transition gate's, requiring the case to carry NO medical artifacts on disk, because an artifact-bearing case owes ledger integrity regardless of policy activation (two tests pinned that and correctly failed the first, broader cut); CASE_028 stalled `in_progress` forever on exactly that contradiction (agent wrote all four contracts, reported "blocked" per spec, nothing could finalize). Verified by defect reintroduction; fail-closed retained when the gate applies. (2) `intake_case.py --no-content-scan` (default ON): the user-directed (2026-08-14) opt-out of the D2 vision pre-check for re-intakes -- 41.3s of CASE_144's intake -- with the per-file human gate untouched. (3) `CLAUDE.md` 110KB -> 15KB: the changelog moved to root `CHANGELOG.md` (docs/ is gitignored), because the root file is injected into every subagent dispatch and ~90KB of history was per-dispatch context cost for agents that need none of it. (4) Read-only DAO allowlist in `settings.local.json` -- NOT `settings.json`, whose deny-only shape is pinned by a fleet-review test -- against the in-dispatch permission-prompt tail (506.2s of CASE_027's denial_response). Found, reported, not fixed: `_validate_generic_ledger` binds operation digests over bytes that include `case_id`, so a forked ledger can never validate and `check-source-ledger-clear` cannot emit `sla.phase1.start` on any fork (arms A/B/C all show `sla_wall_clock_s: null`; `tests/test_sla_markers.py` fails 6 on clean HEAD too). Remaining path to the 5-minute P50: claim_analysis 578s is still 70% of input->claim_analysis; the write side (composing four contracts) and checkpoint round structure are the next levers, with the two-round write discipline added to the spec this session still unmeasured. | tools/dao.py+intake_case.py, agents/claim-analysis, CLAUDE.md+CHANGELOG.md (new), settings.local.json, tests/test_medical_gate_policy_activation.py+test_intake_case.py | The first A/B looked like "narrowing failed"; per-turn cost showed it was the call count, and cutting calls -- not payload -- moved the stage |
| 2026-08-16 | **The two-round write discipline measured: no effect (n=1 per arm).** Arm D (CASE_032, fourth stage-cut fork of CASE_142's processed state, briefing identical to arm C's) ran claim_analysis under the spec that added two-round contract composition (31ddc80) on top of arm C's read batching: **562.2s agent-reported / 45 tool uses / 221,721 tokens / 377 tok/s**, against arm C's 528.2s / 47 / 201,062. Turns barely moved (-2) and wall/tokens rose within run variance -- the 348s of "contract-writing intervals" observed on CASE_027 was composing/thinking time distributed across the dispatch, not extra turns, so grouping the composition had nothing structural to remove: the four writes are ~4 turns under either discipline. Contracts remain semantically consistent (same type/template/coverages/KCD S6280); requirement decomposition varied 13->12 items (6 met/7 uncertain -> 7 met/5 uncertain), ordinary judgment variance between runs, no status flipped to/from not_met. The spec section stays (it is correct about ordering and costs nothing) but must not be cited as a speedup. Cumulative honest position after four arms: read batching is the one lever with a measured win (740.3 -> 578.5s attempt-active); write grouping is measured neutral; payload narrowing without call merging is measured harmful. | outputs/CASE_032 (gitignored), CHANGELOG.md | A committed optimization carried a prediction; the fork A/B existed to check it, and the answer is no |
| 2026-08-16 | **Claim-analysis driver v2, step 1: the CP1 grouped-call skeleton.** `tools/run_claim_analysis.py` implements the plan's first slice -- one DAO manifest read, one grouped `read-redacted-text-bundle` for every processed non-policy claim document (policy stays CP2 input; superseded bundles and `non_text_image` dispositions excluded, the 2026-08-06 double-count boundary), ONE structured provider call (`structured_with_one_correction`, bounded transport schema for the argv limit with the materialized public `fields` schema as the authoritative local gate -- the public $defs ride along so fragment refs resolve), local whitespace-normalized quote verification before candidate persistence, DAO `verify-evidence-references` binding, governed `write-contract`, and receipt/candidate resume via `driver_runtime`. Refuses unless the orchestrator opened a `claim_analysis` attempt (T13), never moves markers. 7 tests, each verified by defect reintroduction (policy-exclusion filter, whitespace rule, candidate reuse); integration smoke against CASE_029 exercises the real DAO refusal path. CP2-4 + medical publication are step 2; no routing change -- the agent path still runs the stage until arm E measures this one. | tools/run_claim_analysis.py (new), tests/test_claim_analysis_driver.py (new) | plans/driverization/claim-analysis-v2.md step 1 |
| 2026-08-16 | **Arm E-cp1: the CP1 driver works; the hybrid does not compose.** CASE_033 (fifth stage-cut fork of CASE_142's processed state) ran checkpoint 1 through `run_claim_analysis.py` for real -- **137.0s total: one 131.9s opus call, zero correction rounds, 2.6s of glue** -- publishing a schema-PASS `extracted_claim_fields.json` with 50 fields (agent arms produced 28), correct KCD S6280, and every redaction-blanked fact (환자명, 보험사, 증권번호, 사고일자) null with review flags rather than substituted. Against the agent path's ~370s CP1 block (CASE_028 mtimes) that is ~37%. **But the remaining CP2-4 agent dispatch cost 517.3s / 59 tool uses / 191,210 tokens -- within noise of arm C doing ALL FOUR checkpoints (528.2s / 47)** -- so the hybrid total of 654s is WORSE than the plain agent. The lesson generalizes: an agent dispatch's cost is dominated by its fixed context-establishment and discipline overhead, not by how many checkpoints remain, so incremental driverization buys nothing; the v2 plan's value hypothesis now rests entirely on step 2 (all four checkpoints driven, no agent dispatch at all), projected 4-6 calls x 40-132s. Also observed, worth carrying: run-to-run judgment variance on `coverage_result.applicable` -- three arms recorded the liability coverage `true` (mechanism applies, requirements uncertain), arm E's agent recorded `false` (conditions not demonstrated) with an explicit warning that this is not an adoption of the insurer's denial; the schema's non-nullable boolean forces one reading of an unresolved question, a design observation for the coverage_result schema, not an agent defect. | outputs/CASE_033 (gitignored), CHANGELOG.md, plans/driverization/claim-analysis-v2.md | The fork A/B existed to answer "can we migrate incrementally" -- the answer is no, and it cost one run to learn |
| 2026-08-16 | **Arm E: the full-stage claim-analysis driver ran to completion and killed its own plan.** CASE_034 (sixth stage-cut fork) published all four contracts through `run_claim_analysis.py` -- DAO-validated, clause refs verbatim-verified, snapshot-gated, stage finalized -- and the clean composite is **~944s** (CP1 253.5s + select 29.2s + judge 75.3s + CP3 248.0s + CP4 326.0s + glue; ~822s substituting the best-observed CP1), against agent arm C's 528.2s: the pre-registered kill criterion (>= 528s) is met and the plan is closed as measured-negative. Root cause is call economics, not architecture: a cold monolithic `claude -p` structured call at opus costs 200-330s nearly regardless of payload (CP3's small output still cost 248s), while the agent's warm conversation pays ~10s per turn -- five cold calls cannot beat 47 warm turns, and v1's 16x40s pilot plus v2's 5x50-330s bracket the same wall from both sides. Getting the driver to complete surfaced four generation-time defects, each fixed and pinned by test: a scalar `confidence` inside `fields` (transport schema now forces object members), a legitimate continuation page refused by an over-strict selection rule (read plans now clamp to indexed ceilings and drop rather than refuse), the right report_profile semantics under wrong key names (`mechanism`/`depth` -- transport now carries exact keys, additionalProperties false), and a real verbatim quote refused by the local grounding prefilter (validators now fall back to the DAO's own verification, whose refusal reason feeds the P4 correction). Per-unit receipts worked exactly as designed: five debugging invocations re-paid zero completed calls. Semantic output plausible but more variant than agent runs (19 requirements vs 10-13, first-ever not_met). **The agent path with the turn-budget spec remains the production route**; reassessment requires anthropic-api-with-caching, a faster extraction model, or a <=2-call design -- each a separate P2 decision. | tools/run_claim_analysis.py, tests/test_claim_analysis_driver.py, plans/driverization/claim-analysis-v2.md SS6, outputs/CASE_034 (gitignored) | The plan wrote its kill number before implementing; the measurement hit it, and the record says so with the same prominence a win would have gotten |
| 2026-08-16 | **Driver implementation removed; tree restored to the pre-driver state with the records kept.** `tools/run_claim_analysis.py` and `tests/test_claim_analysis_driver.py` deleted per user direction after the arm E verdict -- the measured-negative path stays out of the tree so nothing routes through it by accident, while the measurement record (this changelog, plan SS6) and the retrieval point (commit 198baf6) are preserved, the same removal shape as the 2026-07-21 local-model-stack purge. claim_analysis's production route is unchanged: agent dispatch under the turn-budget spec (arm C, 528.2s). | tools/run_claim_analysis.py (deleted), tests/test_claim_analysis_driver.py (deleted), plans/driverization/claim-analysis-v2.md | A dead path left in the tree is a path someone eventually takes; the evidence it was dead is what must survive, not the code |
| 2026-08-16 | **P2-(a) opened: the anthropic-api structured seam is real code now; the measurement is paused on a missing key.** `AnthropicApiProvider` was a selectable stub whose every method raised not-implemented; `analyze_text_structured` is now live -- Messages API with the schema riding as one forced tool (`tool_choice` pinned, `_cli_json_schema`'s keyword strip applied so the two transports stay byte-comparable), `cache_control: ephemeral` on the prompt block, `HARNESS_ANTHROPIC_MAX_TOKENS` (default 16,000) with stop_reason `max_tokens` refused even when a parseable tool input is present (a truncated tool input is silently partial; stop_reason is the only honest signal), text-only responses refused, `usage` -- including `cache_read_input_tokens`, the ground truth for whether caching actually fired -- preserved in raw_metadata. Image/OCR paths stay unimplemented on purpose: nothing sends raw page images to this backend, so P8's provider-exposure notes are untouched. 7 new tests (transport shape, both fail-closed paths, env validation). A paired one-call CP1 bench (scratchpad, DAO-read, redacted layer only -- the same material every CLI call already carries to the same API family) ran its CLI arm on CASE_034's exact 198baf6 prompt: **227.2s for 15 documents / 52,499 chars**, third point inside the 131.9-253.5s bracket, and a probe call pinned the CLI's default model as `claude-fable-5`, which is therefore the parity model for any API arm. **The API arm did not run: no `ANTHROPIC_API_KEY` exists in this environment and the user chose to stop rather than provide one** -- so the question P2-(a) exists to answer (is 200-330s CLI-child overhead or model latency?) is still open, the arm E verdict stands, and the agent path under arm C's turn-budget spec remains the production route. Recorded so the next attempt starts at the measurement, not the plumbing: the cache-prefix order (tools->system->messages) means per-checkpoint transport schemas get zero cross-checkpoint cache reuse -- a real caching win requires a shared-tool or schema-out-of-tool design, noted in plan §6. | tools/llm_providers.py, tests/test_llm_providers.py, plans/driverization/claim-analysis-v2.md §6 | The reassessment path now costs one env var and one command to test, instead of a session of provider work |
| 2026-08-16 | **P2-(b) measured across four models, P2-(c) assessed: the CLI default is not the frontier.** With the API arm paused, the one-call CP1 bench (CASE_034's exact 198baf6 prompt, 15 documents / 52,499 chars, cold `claude -p`, gate = the driver's own materialized-schema + verbatim-quote check run on each output) walked the model axis: **claude-opus-5 146.0/149.2s with 51/59 fields (n=2, spread 3.2s); claude-fable-5 -- the CLI default, hence what every prior "opus 200-330s" record actually measured, pinned by a probe call -- 227.2/237.4s with 36/35 fields (n=2; 131.9/253.5 historically); claude-sonnet-5 95.3s / 26 fields; claude-haiku-4.5 71.1s / 21 fields (n=1 each)**. Three answers fall out. (1) Per-call cost is model latency, not CLI overhead -- a 3.3x spread under the identical child process -- which answers P2-(a)'s question from the side, without the key. (2) First-shot shape errors are not a small-model trait: fable and opus each failed the gate once in two runs (a `normalized_value` type slip; an all-null period), sonnet and haiku failed their single runs -- P4's one correction is simply part of any driver's expected cost, at every tier. (3) The real differentiator is recall, and it is monotone in model class: **sonnet drops 21 of fable's 36 fields -- `disability_rate`, both `liability_opinion_*`/`victim_fault_opinion_*` pairs, `surgery_date`, the expense totals and limit/deductible CP4 consumes -- haiku drops more and collapses a P6-shaped disagreement into one prose value, while opus-5 extracts MORE than the default (51/59 vs 35-50) in ~62% of its wall time.** Where models agree (diagnosis, KCD S6280, redacted-fact nulls) they agree exactly; the axis is recall, not contradiction. **(c) is mechanically feasible at exactly 2 calls** (call 1 = CP1+page-selection, call 2 = CP2-judge+CP3+CP4; no data-dependency blocker; merged transports fit the 8,000-char argv cap) but projects 500-700s on default-model call costs against the agent's 528s, and on sonnet inherits the recall loss. **Verdict: downgrade-(b) rejected on measured recall; (c) not built alone.** The opened question is the opposite of the one asked: opus-5 dominating the CLI default on this prompt (faster AND fuller, n=2) makes a model *switch* -- for the extraction call or the whole agent dispatch -- the strongest live candidate alongside (a), and a 2-call opus spine (~2x147s + correction margin) is the first (c) configuration whose projection lands under 528s. Both remain user-authorized accuracy decisions requiring a stage-level A/B at n>1; nothing is rewired here. | scratchpad bench only (no tree change), CHANGELOG.md, plans/driverization/claim-analysis-v2.md §6 | The bench was built to test a downgrade and instead falsified the premise that the default model was the expensive-accurate end of the curve |
| 2026-08-16 | **Arms F and G: the opus switch measured twice, rejected twice -- and the cost model got its third correction.** Per user direction the two live candidates ran in sequence on fresh stage-cut forks of CASE_027. **Arm F (CASE_035): the claim-analysis AGENT dispatched on opus-5 under arm C's briefing, verbatim.** 620.7s agent-reported / 63 tool uses / 184,393 tokens (645.4s dispatch wall, 286 tok/s) against arm C's 528.2s/47 -- kill criterion met. Per-turn cost is **9.85s, identical to the default model's ~9.9s**, and opus spent 16 MORE turns; the single-call speed advantage does not transfer to an agent loop because the loop's cost is turns, and turn count is behavioral discipline, not model speed. Contracts semantically equivalent (same axes/template/KCD; 12 reqs at 7 met/5 uncertain; no not_met flip). **Arm G (CASE_036): the driver restored from 198baf6 and rebuilt as a 2-call spine** -- M1 = CP1 grouped extraction + policy page selection in one response, M2 = CP2 judgment + CP3 case type + CP4 requirements as three shelled sections of one response, contracts byte-compatible, receipts per contract, M1's selection persisted as its own candidate, merged transports 661/2,295 chars against the 8,000 argv cap, 14 driver tests updated and passing. Run on claude-opus-5: **971.9s wall -- M1 134.2s, M2 822.5s** -- kill criterion met by any reading. M1 alone VINDICATES the model half of the projection: CP1+selection on opus in 134s, faster than the default doing CP1 alone (227/237s). M2 kills the call-merging half: whether 822.5s was one call or a P4 correction pair (~2x411s) is **not determinable from retained records** -- provider_wait wraps both attempts and no per-attempt boundary is traced, the same instrumentation gap the arm E entry recorded -- but even the charitable clean reading (134+411+glue = ~560s) fails. The correction to the cost model: "a cold call costs 200-330s nearly regardless of payload" conflated INPUT payload (which is indeed nearly free) with OUTPUT volume, which is what a structured call actually pays for -- merging three checkpoints into one response serializes their combined output into one stream, so the merge saves per-call fixed overhead while giving back everything on output serialization, and opus's characteristic thoroughness (48 fields, 15 requirements, the run's first not_met, `premises_owner_operator_liability` applicable=false -- the documented non-nullable-boolean variance axis) inflates exactly that. **Cumulative P2 position after seven arms: every measured configuration loses to the agent path under arm C's turn-budget spec (528.2s), which remains the production route.** (a) api+caching stays the only path that attacks the fixed cost AND allows streaming/parallel fan-out, and it stays blocked on a key by user decision; a CP3-parallel-CP4 fan-out variant (M1 -> judge -> CP3 ∥ CP4, bounded by max instead of sum) is the one unmeasured shape left, projected borderline (~450-550s) and not built. v3 driver committed as the measured artifact; whether it stays in the tree is the user's call, per the v2 precedent. | tools/run_claim_analysis.py (restored+rebuilt), tests/test_claim_analysis_driver.py, outputs/CASE_035+CASE_036 (gitignored), plans/driverization/claim-analysis-v2.md §6 | Two kill criteria pre-registered, two measurements, two rejections -- and each rejection taught a different half of where the time actually goes |
| 2026-08-16 | **Arm G postmortem: the 822.5s was a correction pair around output serialization, and "output volume" was still not the whole story.** The isolated rerun of arm G's exact M2 call (same persisted cp1 contract, same 60-page selection, provider now KEEPING the CLI envelope's `usage`/`modelUsage` instead of discarding it -- the observability fix this question forced) measured **288.1s for one clean call: 25,479 output tokens, `thinking_tokens: 0`, ~88 tok/s** -- so M2's cost is genuine output serialization (the user's read), not reasoning, and arm G's 822.5s was that call plus a P4 correction re-pay. The decomposition that prompted the rerun: M1 and M2 had near-identical visible input (~77k/82k chars) AND output (~20k chars each) against a 6x time gap, falsifying the previous entry's output-volume-alone reading; the resolution is that the persisted 20k was the VALIDATED result while the failing attempt generated ~37k chars. The correction cause reproduced exactly: a descriptive free-string in `secondary_case_types` through the loose transport -- **fixed by pinning the case-type/axis enums natively in the cp3 transport** (fourth instance of the generation-time-shape pattern; test verified by defect reintroduction). Clean composite projection: M1 134.2 + M2 288.1 + glue ≈ **437s < 528s** -- the 2-call spine is live again IF M1 stops burning corrections. It hasn't yet: arm H (CASE_037, v0.3.1) halted twice in M1 on the SAME opus tendency -- an undatable period fact -- first as an all-null period object (232.4s), then, after a prompt rule steered it to `{value: null}`, as a single-value shape the public schema forbids for named period slots (248.3s): the schema pins `treatment_period`/`admission_period` to the period variant with `start_date` required, so the only lawful encoding of "8주 가료 필요, 시작일 미상" is OMITTING the slot and carrying the duration in `warnings`. Prompt now says exactly that; not yet re-measured (usage-limit checkpoint). Standing: agent path (arm C, 528.2s) remains production; the (c) spine needs one clean arm H run to claim its ~437s. | tools/llm_providers.py (usage kept), tools/run_claim_analysis.py (cp3 enums pinned, period-slot rule, v0.3.1), tests/test_claim_analysis_driver.py (+1, defect-verified), outputs/CASE_036+037 (gitignored) | The unexplainable 6x gap was two instrumentation gaps deep: discarded usage hid the token count, and an untraced correction hid the second call |
| 2026-08-16 | **Sonnet-5 1-pass CP1: recall closes, the validation gate does not -- rejected at the pre-registered 3-attempt budget.** The question was whether `claude-sonnet-5` could do CP1 extraction in ONE call at its ~95s speed without the recall loss that rejected downgrade-(b), where supplementary calls, a 2-pass shape, or leaning on P4's correction all count as failure. First correction to the earlier record: **the "sonnet drops 21 of fable's 36 fields" figure was a field-NAME diff, and it overstated the gap.** Scored by 46 semantic slots reconciled by FACT across the opus/fable outputs (`slots.py`, pre-registered before any variant call; derived only from bench outputs of DAO-served redacted text -- no `data/ground_truth/`, no `source-cases/`), the baselines are opus2 46, opus 45, fable-run1 42, **fable-run2 40 (the pass bar)**, **sonnet 31**, haiku 21 -- a real gap of 9, not 21, since sonnet had already captured the disability rate, evaluation item, coverage limit and hospital totals under its own names. Three prompt revisions, all bench-only (`tools/run_claim_analysis.py` and the agent spec untouched): **v1** (exhaustive 8-block checklist naming every observed slot, each requiring a value OR a `warnings` line stating why not) moved recall **31 -> 43/46** at **90.3s / 57 fields / 63 refs** -- the recall bar cleared on the first try, and the speed held. It failed the gate on one systematic shape: **38 of 57 fields omitted `review_required`**, which `field_common` requires, while `confidence` and `evidence_references` were present on all 57. **v2** pinned the three common member keys in the transport schema (lever 2, the cp3-enum precedent) and got them on all 56 fields -- **and lost `value` on all 56**, because an enumeration listing only the common keys reads as the complete member spec; recall scored 36 purely as that artifact, and the call cost 168.5s. **v3** declared the payload keys (`value`/`normalized_value`/`start_date`/`end_date`/`days`) alongside the required three: **99.6s / 59 fields / 86 refs / 44 of 46 slots**, with `review_required` and `value` both correct everywhere -- the two scored misses are **matcher false negatives** (`diagnosis_name_official` and `victim_fault_opinion_claimant_side` are present and correctly cited; the pre-registered regexes demanded literal phrasings the model wrote differently), and the slot list was deliberately NOT loosened afterward to manufacture a pass. v3 still fails the first-shot gate on **two** independent axes: `normalized_value` as a NUMBER on 2 of 59 fields (`coverage_limit` 500000000, `deductible` 100000) where the public schema requires a string, and **3 of 86 quotes not verbatim** -- paraphrases and a reconstructed table row, checked independently since the schema error short-circuits the quote pass. **Verdict: rejected.** Recall is a prompt problem and sonnet solves it; first-shot schema+citation compliance is not, and each fixed shape exposed the next (three distinct shapes in three runs, the fifth through seventh instances of the generation-time-shape pattern). Budget was pre-registered at 3 revisions precisely so this stopped here: a fourth fixing `normalized_value` would be tuning to n=1 on one source case, and the non-verbatim quotes are not schema-blockable at all. Nothing rewired; production route unchanged (agent dispatch, arm C, 528.2s). | scratchpad bench only (no tree change), CHANGELOG.md, plans/driverization/claim-analysis-v2.md §6 | The pre-registered budget and slot list did their job twice: they caught that the original recall gap was a naming artifact, and they stopped the experiment at a real failure instead of at a tuned pass |
| 2026-08-16 | **Arm v4 (over-budget, user-directed) and the correction it forced: the agent path never had these failures at all.** The sonnet 1-pass experiment was reopened past its pre-registered 3-revision budget on user direction. **v4** pinned `normalized_value` to string in the transport (the v3 failure) and added verbatim rules aimed at the two real quote defects -- which the source text identified precisely: one quote changed `보험자는` to `피보험자는`, a single word that inverts the legal subject, and one reconstructed a row across an ASCII-art table's box-drawing columns, a string that exists nowhere contiguously. Result: **95.4s / 61 fields / 78 refs / 43 of 46 slots, gate FAIL** -- `normalized_value` was correctly `"13"`, and the type error had **moved next door into `value`** (`13` as a number). That is the fourth consecutive revision where blocking one shape surfaced another beside it (v1 `review_required` -> v2 `value` -> v3 `normalized_value` -> v4 `value` again), which makes the rejection rest on the PATTERN rather than on budget exhaustion: a fifth revision has no evidential basis for closing where four did not. **The larger correction is to this session's own explanation.** Asked whether these were pre-existing pipeline defects, the assistant asserted the agent path hits the same rejections and silently absorbs them via a retry loop -- inferred from code structure, never checked. `read-timing-summary` across all three agent arms shows **0 validate errors** (CASE_029 31 validate calls, CASE_032 30, CASE_035 29; the single CASE_029 error is `io`, unrelated), and `dao.py`'s own docstring states `write-contract` "makes exactly one write+validate attempt" and explicitly "does not implement P4's retry-once-then-halt loop". **No such absorption mechanism exists, and the agent arms produced no schema failures to absorb** -- so the four failures are specific to the single-call path under test, not a latent defect the agent path was hiding. The invented mechanism would have buried exactly the finding that matters. Rule added to `CLAUDE.md` hard rules and `harness-guardrails` P1 (commit d5c1ba1): system-behaviour claims come from executed records, with "not measured" required over a plausible mechanism. Standing unchanged: agent dispatch under arm C's turn-budget spec (528.2s) is production; sonnet 1-pass rejected. | scratchpad bench only (no tree change), CHANGELOG.md, CLAUDE.md, .claude/skills/harness-guardrails | Reopening the budget produced one more failure and one real correction -- and the correction was to the assistant's own unverified explanation, not to the measurement |
| 2026-08-16 | **The normalizer works, robustness holds on a second processing -- and the "fabricated citation" finding was wrong: it was a page-boundary artifact.** Three results, the third correcting this session twice over. **(1) A deterministic shape normalizer clears the schema axis entirely.** Replaying the stored v1-v4 outputs through a repair layer (default-inject a missing required flag, coerce number->string where the schema declares string, unwrap a member double-wrapped inside `value`, drop a member with no payload or no evidence; `quote` never touched) with NO new model call: **all four pass the public `fields` schema, with zero slot-recall loss** (v1 43->43, v3 44->44, v4 43->43; v2 collapses 56->0 fields because its members never had a payload to repair). v1 needed 81 repairs, v4 only 8. **(2) Robustness confirmed on a different processing of the same case.** CASE_133 (19 CP1 documents against CASE_034's 15 -- different segmentation, same underlying matter) ran v1 at **87.3s / 56 fields / 71 refs / 44 of 46 slots**, schema PASS after normalize, recall loss 0. This is NOT generalization and must not be cited as such: a census of all 30 CP1-capable cases in `outputs/` found **every one of them is the same underlying accident** (identical opening sentence: 소추원 계단, 오전 11시경, 우측 손목 요골 원위부 골절), so no second matter exists in this repo to test against; `source-cases/` was not readable to confirm the raw corpus count. What CASE_133 does establish is stability under a different document decomposition. **(3) The citation defects were mostly not defects.** Classifying every failing quote against the source by whitespace-stripped containment splits them in two. A `LINEBREAK-ONLY` class exists where the quote IS in the source but a line wrap splits a word mid-token (`이 사건 사고에 관하\n여`), which `_normalize_ws` cannot bridge since it collapses runs of whitespace rather than removing them -- **v1's single CASE_034 "non-verbatim" quote is this, so v1 after normalization has ZERO real citation defects.** Worse for the earlier record: the quote reported two entries ago as a one-character fabrication inverting the legal subject (`보험자는` -> `피보험자는`) is **the same artifact across a PAGE boundary** -- CASE_034 DOC_008 ends page 3 with `...보이므로, 피` and starts page 4 with `보험자는 이 사건 피해자에 대하여...`, and the surrounding argument is about 피보험자's premises, so `피보험자는` is the correct reading and the model recovered it from context. The verifier matches per page and refused it. Only the ASCII-table row reconstructions remain as genuine non-verbatim quotes (2 on CASE_133, both `DOC_195`-style `date | code | name` rows assembled across box-drawing columns). **Net standing:** v1 + normalizer produces schema-PASS output at 43-44/46 slots on both processings, ~87-90s, with real citation defects of 0 (CASE_034) and 2 (CASE_133). Nothing rewired -- normalizer is scratchpad-only, production driver untouched, and the open question is now generalization to a genuinely different matter, which this repo cannot answer. | scratchpad only (normalize.py/test_normalize.py), CHANGELOG.md | Two consecutive sessions read a verifier limitation as a model defect; classifying the failures against the source instead of trusting the refusal is what separated them |
| 2026-08-16 | **CP2-4 on sonnet, measured rather than predicted: CP2 and CP3 pass first-shot; CP4 fails on a defect the normalizer must NOT repair.** The previous entry predicted from schema shape that the CP1 normalizer would not extend to CP2-4; running it replaces that prediction with a reading, and splits it. One isolated M2 call on CASE_036's exact persisted inputs (arm G's M1 receipt, cp1 contract, same 60-page selection, `bench_m2_call.py` recovered from the prior session) on `claude-sonnet-5`, validated section-by-section against each checkpoint's own authoritative body schema: **CP2 PASS, CP3 PASS, CP4 FAIL.** CP3 -- the section the earlier record flagged as the enum risk -- came back completely clean: `case_type` 배상책임, `secondary_case_types` `["후유장해"]`, `candidate_types` with confidences, and a fully-keyed `report_profile` (`format_contract_version`/`family`/`claim_mechanism`/`mode`/`support_status`), i.e. **v0.3.1's natively-pinned cp3 enums are doing their job on this model too**, and the free-string failure that motivated them did not recur. CP2 produced two coverages with `standardized_coverage_name` (`facility_owner_liability`, `premises_medical_expense`) and clause refs. **CP4's failure is structural, not cosmetic:** the schema nests `coverage_requirements` as GROUPS (`{coverage_name, standardized_coverage_name, requirements: [...]}`) and the model emitted six FLAT requirement items with `requirements: []` and no coverage key at all -- a shape defect in the same family as CP1's, except that repairing it means deciding which coverage each requirement belongs to. Reading the content, REQ-1/2/3/6 are liability and REQ-4/5 are the premises-medical-expense exclusions, but that attribution comes from interpreting the requirement text, and **a normalizer that guessed it would be inventing the join `_validate_m2` exists to enforce** (the driver already refuses a group naming an unknown coverage). So the CP1 lever stops exactly at the boundary the previous entry drew, but for a sharper reason than "enums": **CP1's repairs are content-preserving defaults and type coercions; CP4's would be a semantic assignment.** The honest fix for CP4 is generation-time -- pin the nesting in the cp4 transport (`requirements` non-empty, `standardized_coverage_name` required, joined to CP2's values), the fifth application of the pattern -- not a repair layer. Measured cost: the call ran on sonnet at 10,373 output tokens with 94,000 cache-read; a haiku sub-call of 24 output tokens also appears in `model_usage`, unexplained and worth noting rather than theorizing about. Not built, not rewired: this is one measurement on one case, and CP4's transport change is a production driver edit requiring its own authorization and A/B. | scratchpad bench only, CHANGELOG.md | The prediction was right about the boundary and wrong about the reason -- CP3's enums held, and CP4 broke on a join no repair layer is allowed to guess |
| 2026-08-16 | **CP4's cause was the transport schema, not a semantic limit -- the previous entry's diagnosis was inference from output and it was wrong.** The entry above concluded from the FAILED OUTPUT ALONE that CP4's flat-item shape was unrepairable because attributing each requirement to a coverage is "a semantic assignment no repair layer is allowed to guess". Testing it inverted the finding. `_transport_schema_cp4` declares `coverage_requirements` as `{"type": "array", "items": {"type": "object"}}` -- **entirely unconstrained**, unlike cp2/cp3 whose keys are pinned -- so the nesting was never stated at generation time even though `_m2_prompt` says in prose to group "joining EXACTLY on the `standardized_coverage_name` values from coverage_section". Holding case, persisted M1/cp1 inputs, prompt and model constant and pinning ONLY cp4's item shape (group keys required, `requirements` minItems 1): **CP2/CP3/CP4 all PASS, twice (90.6s and 90.7s), with correct per-coverage grouping (4 + 3 requirements over two coverages) and `standardized_coverage_name` joining exactly onto coverage_section both times.** The model could do the attribution all along; the schema never asked. This is the same generation-time-shape pattern as CP1's four failures and v0.3.1's cp3 enums -- **sixth instance, and the first one this session diagnosed backwards.** What survives from the previous entry: a post-hoc normalizer still must NOT guess this join (a model assigning it from the clause text it read is not the same act as code inferring it from strings), and the CP1 repair layer remains CP1-only -- but for the opposite reason than recorded. CP4 needs no repair layer because a pinned transport prevents the defect entirely. Also recorded for the fan-out question: CP3 consumes no CP4 output and CP4 consumes no CP3 output, so the true dependency is CP1 -> {CP2 -> CP4, CP3}; the current M2 merges all three into one call, so parallelising means splitting calls, whose net effect is unmeasured -- arm G established that a structured call's cost follows OUTPUT serialization (~88 tok/s), so splitting trades smaller per-call output against more fixed overhead, and `_m2_prompt` additionally tells CP3 to classify "from the coverages you identified in coverage_section", a documented dependency whose necessity is untested. | scratchpad bench only (bench_m2_cp4.py), CHANGELOG.md | The record now contains the wrong diagnosis and its refutation one row apart, which is the point -- the wrong one was reasoning about an output, the right one ran the experiment |
| 2026-08-16 | **Fan-out measured: CP2 -> (CP3 ∥ CP4) beats the merged M2 by ~10%, and the two case-type axes agree across arms.** The dependency `CP1 -> {CP2 -> CP4, CP3}` was read off `_m2_prompt` and `_validate_m2` (CP3 consumes no CP4 output and CP4 consumes no CP3 output), then tested rather than projected. Same case (CASE_036), same persisted M1/cp1 inputs, same checkpoint wording -- the split prompts are SLICED from `_m2_prompt`'s own CHECKPOINT A/B/C text and CP3 is still handed CP2's result, so the arms differ in packaging only, not in task or inputs. **Merged (cp4 pinned): 90.6s and 90.7s, all sections PASS (n=2). Fan-out: 81.1s wall -- CP2 31.7s serial, then CP3 33.3s concurrent with CP4 49.4s -- all sections PASS**, against a serial sum of 114.4s, so the parallel leg hides CP3 entirely behind the longer CP4. Net ~9.5s / ~10% under the merged call, consistent with arm G's cost model: splitting shrinks each call's OUTPUT (the thing a structured call actually pays for) but triples fixed per-call overhead, and the concurrency is what leaves a surplus. **A first fan-out run showed 71.8s but is not counted** -- its CP2 leg failed schema on a missing `reviewer_role` because the bench's own split CP2 transport omitted the conditional requirement the merged shell inherits, so that arm was measuring the bench's gap and produced less output; fixing it cost ~9s and is the honest number. **Semantics equivalent where it matters:** `coverage_basis` (배상책임) and `loss_type` (후유장해) are IDENTICAL across arms and `report_profile` is byte-identical (liability_damages/insured_liability/full/supported) -- the template selection downstream therefore does not move. Legacy single-value `case_type` differed (배상책임 merged vs 후유장해 fan-out) with the other axis landing in `secondary_case_types` each time: no information is lost, and this is the documented judgment-variance axis the non-nullable single value forces, not a fan-out defect. CP4's join onto CP2's `standardized_coverage_name` held in every arm. **Limits, stated rather than smoothed:** fan-out is n=1 valid, merged n=2; this measures the M2 segment ONLY -- M1 has one measurement (opus, 134.2s, arm G) and none on sonnet, so any end-to-end figure would mix models and none is claimed here; and `standardized_coverage_name` varies run to run (`premises_liability_special_clause` / `facility_owner_liability` / `premises_liability_facility_owner`), self-consistent within a run but a real cross-run comparability axis worth tracking. Nothing rewired. | scratchpad bench only (bench_fanout.py), CHANGELOG.md | The fan-out shape had been projected at "~450-550s, borderline, not built" since arm G; built and measured, it wins by 10% and the arms agree on both axes that drive the template |
| 2026-08-16 | **denial_response: NOT improved, and it carries the same unconstrained-transport defect plus a driver-ordering bug the claim-analysis work never had.** Asked whether the denial driver had received any of this session's fixes, the answer from `git log` is no -- `tools/run_denial_response_driver.py` is untouched since its introduction (`8920d61`), still `v0.3`, and `_transport_schema` declares **both** collections as `{"type": "array", "items": {"type": "object"}}`, the exact shape that made CP4 emit ungrouped items. The public `denial_reasons` item requires **fourteen** keys, three of them enums (`decision_type`, `payment_status`, `taxonomy_code`), none stated at generation time. Measured on CASE_027's first insurer bundle, sonnet-5, base vs pinned transport (required keys + enums lifted from the public schema, 629 -> 2,109 transport chars, well inside the argv limit the code comment cites as the reason for keeping it loose): **base 40.8s, gate FAIL; pinned 90.5s, gate FAIL.** Both produced 3 denial reasons and 1 accepted coverage, and **no required key was missing in either arm** -- so this is NOT CP4's defect after all, and pinning is not the fix here. The first refusal in both arms is `reason_id` failing `^DR_[0-9]+$` (`PROV-DEN-001`, `PROV-DENIAL-001`), which is **a driver-ordering bug, not a model error**: `_assign_ids` (line 212) deterministically renumbers `reason_id`/`basis_id`/`accepted_coverage_id` -- exactly the CP4 `_renumber_requirements` pattern -- but it runs at line 429, AFTER `_validate_bundle_output` has already rejected the response at line 386/401, and the prompt tells the model to "use provisional schema-valid IDs" without ever stating the pattern that makes an ID valid. Substituting the IDs the driver would have assigned anyway and re-validating exposes the real depth: **base still fails with 52 errors, pinned with 25** -- `amounts: []` where an object is required, `grounds` missing `contractual_basis`, `taxonomy_code` `UNVERIFIED` and `decision_type` `not_determined` outside their enums, `accepted_amount` as an object where a number is required, and (pinned only, a defect the pinning INTRODUCED) `candidate_codes` flattened to bare strings because the lifted item shape kept only scalar/enum properties and dropped the nested object shape. Pinning halves the error count and fixes none of the semantic ones. **Conclusion: denial_response needs its own investigation, not a port of the CP1/CP4 fixes** -- the ordering bug should be fixed first (validate after deterministic ID assignment, or state the pattern in the prompt), since it currently guarantees a wasted P4 correction on every single run regardless of output quality. Nothing changed in the tree; this is a measurement and a defect report. | scratchpad bench only (bench_denial.py), CHANGELOG.md | The question was "did denial get the improvements too" -- it did not, and measuring it found a bug that burns a correction round on every run before the model's actual output is ever assessed |
| 2026-08-16 | **M1 measured WITH this session's fixes applied (the first two runs measured the unimproved path and were worthless).** Two sonnet M1 runs were first taken against the stock production prompt and transport -- 56.9s and 53.7s at 31-32 of 46 slots -- and reported as "sonnet M1 is fast but low-recall". That was measuring the very thing this session had already fixed: the checklist prompt (recall 31 -> 43 on CASE_034) and the payload/required-key transport pinning existed and were simply not applied. **Rerun on CASE_036 with the CP1 half of `_m1_prompt` replaced by v1's checklist and `fields` members pinned per v3, selection half untouched: 100.8s, 56 fields, 64 refs, 42 of 46 slots** -- recall +10 for +45s, which is the same trade the CP1 bench showed (v1 90.3s vs the stock sonnet's 95.3s at 26 fields). Schema is fully absorbed by the normalizer (25 repairs, PASS); what remains is citations, and classifying them against the source puts **5 real mismatches and 1 linebreak-only artifact out of 64**, with **4 of the 5 being ASCII-table row reconstructions** (`2023-12-05 | G6404 | 수관절 4매 | 10,675`, a `합계` row of box-drawing pipes, a 민법 제750조 block quoted across `│` borders) and the fifth a citation to a page that was never served. Table quoting is now the single dominant citation defect across every arm this session -- CASE_034 v3/v4, CASE_133, and now M1 -- and it is not a schema-blockable shape. **Composite, all sonnet, model-call time only: M1 100.8s + M2 fan-out 81.1s ≈ 182s**, against the agent path's measured `dispatch_wall_s` **median 603.8s (n=7: 557.1/569.2/587.5/603.8/645.4/691.8/817.6, mean 638.9, stdev 91.5)** -- roughly 30%, i.e. ~3.3x. Two corrections to earlier framing come with it: the 528.2s figure quoted all session is arm C's `agent_reported_s`, while the same run's `dispatch_wall_s` is **569.2s**, and the pipeline-level number is the latter; and 15 of 22 cases carrying a `claim_analysis` stage have null dispatch timing (no `record-dispatch` caller), so the n=7 is biased toward recently-instrumented runs. Still excluded from the 182s: P4 correction re-pay (the gate does not pass first-shot), DAO write/verify glue (2.6s in arm E), and `sla_wall_clock_s` remains null on every fork, so Phase 1 end-to-end is still unmeasurable. | scratchpad bench only (bench_m1.py), CHANGELOG.md | Measuring the unfixed path and reporting its weakness as a finding wasted two runs -- the fixes were sitting in the same directory |
| 2026-08-16 | **The fixes shipped (v0.4) and the driver ran in the REAL pipeline -- where it is blocked by the verifier, not by anything the fixes address.** Everything measured this session was ported into `tools/run_claim_analysis.py`: the exhaustive checklist prompt, the required-key/`normalized_value`-string/table-quoting rules, cp1 member pinning, cp4 group nesting, and `normalize_cp1_shape`. Tests 15 -> 19; **full suite 75 failures before and after, lists byte-identical against a file-copy baseline**, so no regression. Three refusals inside the normalizer are pinned by tests because each was a live temptation: it never rewrites a `quote` (that would automate fabrication), never defaults `reviewer_role` (의사 vs 손해사정사 is the substantive half of the review flag -- an earlier cut DID default it and silently defeated an existing guard, caught by that guard's test), and never drops an uncited or payload-less member (dropping converts a loud refusal into silent data loss; an earlier cut dropped them and made the uncited-fact test pass vacuously). **Then it ran for real on CASE_038, a fresh fork of arm C's CASE_029 with the attempt opened by `update-run-state`: 173.3s and 338.0s, both exit 1, zero candidates persisted.** The normalizer worked exactly as designed -- 7 shape repairs on the first run (`disability_rate`, `coverage_limit`, `deductible` and four expense totals all coerced from number to string) -- and both runs died at the same place: **`_validate_cp1_output` refusing a quote, after the P4 correction had already been spent.** Run 1 on `DOC_006 p1`, run 2 on `DOC_019 p1`. Checked against the served text, `DOC_006 p1` is a page whose line wraps cut words mid-token (`우\n측 손목`, `과실 비\n율`, `하겠\n습니다`) -- the accident-circumstance sentence CANNOT be quoted without crossing one, and `_normalize_ws` collapses whitespace runs rather than removing them, so the quote fails while whitespace-stripped containment succeeds. This is the artifact class identified earlier today, now shown to be **a hard blocker in a real run rather than a scoring nuisance**: no prompt rule avoids it, because the fact only exists across the break. Net position: the recall and schema problems are solved and shipped; **the citation verifier is now the single thing standing between this driver and a completed stage**, and fixing it is a P1-adjacent decision (whitespace-insensitive matching weakens the fabrication check that caught the real ASCII-table reconstructions) rather than a driver change. No end-to-end timing is claimed: neither run completed, and 173.3s/338.0s are failure paths that include a correction re-pay. | tools/run_claim_analysis.py, tests/test_claim_analysis_driver.py, outputs/CASE_038 (gitignored), CHANGELOG.md | Shipping the fixes moved the failure from "the model cannot produce the right shape" to "the verifier cannot accept a correct quote" -- which only a real run could show |
| 2026-08-17 | **The stage completed in the real pipeline: 362.9s against the agent path's 569.2s, unblocked by telling the correction WHERE the quote is.** Two verifier fixes and one error-message change closed the gap between a driver that produced good content and a driver that could publish. (1) *Korean mid-word line wraps.* 311 of CASE_038's 367 pages (85%) contain a `\n` between two Hangul syllables, 1,374 in total, and `_normalize_ws` collapses whitespace runs rather than removing them, so `\uad00\ud558\n\uc5ec` could never match a correct `\uad00\ud558\uc5ec`. `quote_matches_page` now accepts a wrap under BOTH readings (as nothing and as a space) -- healing only one way REFUSED a citation the old rule accepted (`\uc694\uace8\n\uacfc\uc808` heals to `\uc694\uace8\uacfc\uc808` while the quote writes `\uc694\uace8 \uacfc\uc808`), caught by an existing driver test. (2) *Page-boundary spans.* DOC_008 ends page 3 with `...\ubcf4\uc774\ubbc0\ub85c, \ud53c` and opens page 4 with `\ubcf4\ud5d8\uc790\ub294`, so `\ud53c\ubcf4\ud5d8\uc790\ub294` exists in the document but on no single page (105 of 350 boundaries continue Hangul). `quote_spans_page_pair` is tried ONLY after page N fails alone, strips the running footer at the join, and REFUSES a quote lying wholly on N+1 -- that is a mis-numbered citation, and refusing it is what keeps the rule from becoming "page numbers may be off by one". (3) *The actual unblocker.* With both fixes the run still halted: the model cited DOC_019's N1611 row on page 1, the correction re-cited it on page 1, then on page 6, while it sits on page 2. **The refusal said only "not present on page 1" -- it never said where the text was**, so P4's single correction was spent guessing. `locate_quote_hint` searches the SERVED REDACTED pages (load-bearing, not incidental: the hint goes into a prompt, so sourcing it pre-redaction would leak masked PII through an error message) and appends "that text is on page 2, not 1"; it returns nothing when the quote is nowhere, because that is the fabrication case. **Result: exit 0, all four contracts published** -- CP1 196.9s / CP2 159.1s / CP3 160.6s / CP4 163.0s, 362.9s total, against arm C's `dispatch_wall_s` of **569.2s on the same source case** (CASE_038 is a fork of arm C's CASE_029), i.e. **~64%, a 1.57x speedup**, with per-unit receipts confirmed (a rerun reuses all four in 6.6s). Semantically consistent with the agent: same KCD S6280, same `case_type` 배상책임, same 배상책임/후유장해 axes, same `배상책임_후유장해형` template, two coverages either way (names differ: `facility_owner_liability`/`on_premises_medical_expense` vs the agent's `premises_owner_operator_liability`/`premises_medical_payments_no_fault`), and **58 extracted fields against the agent's 28**. Requirement decomposition differs (6 at 3 met/3 uncertain vs 13 at 6/7) -- fewer, coarser groups, the ordinary judgment variance axis, no status flipped to not_met. Caveats kept explicit: **n=1, one source case, and every case in this repo is the same underlying accident**, so this is not generalization; the medical publication correctly refused under the deferred config; and 362.9s includes one P4 correction, so a clean run would be faster. | tools/_cross_contract.py+dao.py+run_claim_analysis.py+run_denial_response_driver.py, tests/test_cross_contract.py, outputs/CASE_038 (gitignored) | The driver had been producing publishable content for three runs; what blocked it was an error message that named the problem without naming the fix |
